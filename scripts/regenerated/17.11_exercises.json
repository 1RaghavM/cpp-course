[
  {
    "title": "Day Name Lookup",
    "prompt_md": "## Goal\nBuild a lookup table of day names as an array of `const char*` pointing at string literals, then print the name for a given day number. Use the array for the lookup -- not a `switch` or `if/else` chain over each day.\n\nDay numbering: `0 = Sunday`, `1 = Monday`, ..., `6 = Saturday`.\n\n## Input\nA single integer.\n\n## Output\n- If the number is 0..6, print the matching day name.\n- Otherwise print `invalid`.\n\nDo not use `std::string`, `std::string_view`, `std::array`, or `std::vector`.\n\n## Example\nInput `3` produces `Wednesday`.",
    "starter_code": "#include <iostream>\n\nint main()\n{\n    // TODO: build a lookup table as an array of const char* whose elements point\n    //   at the day-name string literals, in order: index 0 = \"Sunday\" through\n    //   index 6 = \"Saturday\".\n\n    int n{};\n    std::cin >> n;\n\n    // TODO: if n is in range 0..6, index your table by n and print the matching\n    //   day name. Otherwise print \"invalid\".\n\n    return 0;\n}\n",
    "solution_code": "#include <iostream>\n\nint main()\n{\n    const char* days[]{ \"Sunday\", \"Monday\", \"Tuesday\",\n        \"Wednesday\", \"Thursday\", \"Friday\", \"Saturday\" };\n\n    int n{};\n    std::cin >> n;\n\n    if (n >= 0 && n <= 6)\n        std::cout << days[n] << '\\n';\n    else\n        std::cout << \"invalid\" << '\\n';\n\n    return 0;\n}\n",
    "difficulty": "practice",
    "test_cases": [
      {
        "label": "Sample -- Wednesday lookup",
        "is_sample": true,
        "stdin": "3\n",
        "expected_stdout": "Wednesday\n"
      },
      {
        "label": "Hidden -- boundary Saturday",
        "is_sample": false,
        "stdin": "6\n",
        "expected_stdout": "Saturday\n"
      },
      {
        "label": "Hidden -- boundary Sunday",
        "is_sample": false,
        "stdin": "0\n",
        "expected_stdout": "Sunday\n"
      },
      {
        "label": "Hidden -- out of range",
        "is_sample": false,
        "stdin": "9\n",
        "expected_stdout": "invalid\n"
      },
      {
        "label": "Hidden -- negative is invalid",
        "is_sample": false,
        "stdin": "-2\n",
        "expected_stdout": "invalid\n"
      }
    ]
  },
  {
    "title": "Longest String Literal in Array",
    "prompt_md": "## Goal\nUsing a `const char*` array of color literals as a lookup table, process a sequence of color queries and report the longest color name seen, with its length (measured by `std::strlen`). On a tie, keep the one that appeared first.\n\nColor table: `0 = red`, `1 = orange`, `2 = yellow`, `3 = green`, `4 = blue`.\n\n## Input\n- Line 1: an integer `n` (1 <= n <= 5).\n- Next `n` lines: each a color index (0-4).\n\n## Output\n- Line 1: the longest queried color name.\n- Line 2: its length.\n\nDo not use `std::string`, `std::string_view`, `std::array`, or `std::vector`. All indices are valid.\n\n## Example\nInput `3` then `0`, `1`, `4` produces:\n```\norange\n6\n```",
    "starter_code": "#include <cstring>\n#include <iostream>\n\nint main()\n{\n    const char* colors[]{ \"red\", \"orange\", \"yellow\", \"green\", \"blue\" };\n\n    int n{};\n    std::cin >> n;\n\n    // TODO: set up tracking for the longest name seen so far -- a const char* for\n    //   the best name and its length (start the length at 0 so the first query\n    //   always wins).\n\n    // The query loop is provided; it reads each color index for you. Inside it,\n    // you supply the comparison.\n    for (int i{ 0 }; i < n; ++i)\n    {\n        int c{};\n        std::cin >> c;\n\n        // TODO: measure the length of colors[c] with the <cstring> length\n        //   function. If it is STRICTLY greater than the best length so far,\n        //   update both the best length and the best name pointer. (Strictly\n        //   greater keeps the first one on ties.)\n    }\n\n    // TODO: print the longest queried color name on line 1 and its length on\n    //   line 2.\n\n    return 0;\n}\n",
    "solution_code": "#include <cstring>\n#include <iostream>\n\nint main()\n{\n    const char* colors[]{ \"red\", \"orange\", \"yellow\", \"green\", \"blue\" };\n\n    int n{};\n    std::cin >> n;\n\n    const char* longest{ \"\" };\n    std::size_t maxLen{ 0 };\n\n    for (int i{ 0 }; i < n; ++i)\n    {\n        int c{};\n        std::cin >> c;\n\n        std::size_t len{ std::strlen(colors[c]) };\n        if (len > maxLen)\n        {\n            maxLen = len;\n            longest = colors[c];\n        }\n    }\n\n    std::cout << longest << '\\n';\n    std::cout << maxLen << '\\n';\n\n    return 0;\n}\n",
    "difficulty": "practice",
    "test_cases": [
      {
        "label": "Sample -- three colors",
        "is_sample": true,
        "stdin": "3\n0\n1\n4\n",
        "expected_stdout": "orange\n6\n"
      },
      {
        "label": "Hidden -- single color",
        "is_sample": false,
        "stdin": "1\n2\n",
        "expected_stdout": "yellow\n6\n"
      },
      {
        "label": "Hidden -- tie keeps first",
        "is_sample": false,
        "stdin": "2\n1\n2\n",
        "expected_stdout": "orange\n6\n"
      },
      {
        "label": "Hidden -- all short names",
        "is_sample": false,
        "stdin": "2\n0\n3\n",
        "expected_stdout": "green\n5\n"
      }
    ]
  }
]
