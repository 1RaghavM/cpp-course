{
  "chapter": {
    "number": "15",
    "title": "More on Classes"
  },
  "prior_lesson_titles": [
    "0.1: Introduction to these tutorials",
    "0.2: Introduction to programs and programming languages",
    "0.3: Introduction to C/C++",
    "0.4: Introduction to C++ development",
    "0.5: Introduction to the compiler, linker, and libraries",
    "0.6: Installing an Integrated Development Environment (IDE)",
    "0.7: Compiling your first program",
    "0.8: A few common C++ problems",
    "0.9: Configuring your compiler: Build configurations",
    "0.10: Configuring your compiler: Compiler extensions",
    "0.11: Configuring your compiler: Warning and error levels",
    "0.12: Configuring your compiler: Choosing a language standard",
    "0.13: What language standard is my compiler using?",
    "1.1: Statements and the structure of a program",
    "1.2: Comments",
    "1.3: Introduction to objects and variables",
    "1.4: Variable assignment and initialization",
    "1.5: Introduction to iostream: cout, cin, and endl",
    "1.6: Uninitialized variables and undefined behavior",
    "1.7: Keywords and naming identifiers",
    "1.8: Whitespace and basic formatting",
    "1.9: Introduction to literals and operators",
    "1.10: Introduction to expressions",
    "1.11: Developing your first program",
    "1.x: Chapter 1 summary and quiz",
    "2.1: Introduction to functions",
    "2.2: Function return values (value-returning functions)",
    "2.3: Void functions (non-value returning functions)",
    "2.4: Introduction to function parameters and arguments",
    "2.5: Introduction to local scope",
    "2.6: Why functions are useful, and how to use them effectively",
    "2.7: Forward declarations and definitions",
    "2.8: Programs with multiple code files",
    "2.9: Naming collisions and an introduction to namespaces",
    "2.10: Introduction to the preprocessor",
    "2.11: Header files",
    "2.12: Header guards",
    "2.13: How to design your first programs",
    "2.x: Chapter 2 summary and quiz",
    "3.1: Syntax and semantic errors",
    "3.2: The debugging process",
    "3.3: A strategy for debugging",
    "3.4: Basic debugging tactics",
    "3.5: More debugging tactics",
    "3.6: Using an integrated debugger: Stepping",
    "3.7: Using an integrated debugger: Running and breakpoints",
    "3.8: Using an integrated debugger: Watching variables",
    "3.9: Using an integrated debugger: The call stack",
    "3.10: Finding issues before they become problems",
    "3.x: Chapter 3 summary and quiz",
    "4.1: Introduction to fundamental data types",
    "4.2: Void",
    "4.3: Object sizes and the sizeof operator",
    "4.4: Signed integers",
    "4.5: Unsigned integers, and why to avoid them",
    "4.6: Fixed-width integers and size_t",
    "4.7: Introduction to scientific notation",
    "4.8: Floating point numbers",
    "4.9: Boolean values",
    "4.10: Introduction to if statements",
    "4.11: Chars",
    "4.12: Introduction to type conversion and static_cast",
    "4.x: Chapter 4 summary and quiz",
    "5.1: Constant variables (named constants)",
    "5.2: Literals",
    "5.3: Numeral systems (decimal, binary, hexadecimal, and octal)",
    "5.4: The as-if rule and compile-time optimization",
    "5.5: Constant expressions",
    "5.6: Constexpr variables",
    "5.7: Introduction to std::string",
    "5.8: Introduction to std::string_view",
    "5.9: std::string_view (part 2)",
    "5.x: Chapter 5 summary and quiz",
    "6.1: Operator precedence and associativity",
    "6.2: Arithmetic operators",
    "6.3: Remainder and Exponentiation",
    "6.4: Increment/decrement operators, and side effects",
    "6.5: The comma operator",
    "6.6: The conditional operator",
    "6.7: Relational operators and floating point comparisons",
    "6.8: Logical operators",
    "6.x: Chapter 6 summary and quiz",
    "7.1: Compound statements (blocks)",
    "7.2: User-defined namespaces and the scope resolution operator",
    "7.3: Local variables",
    "7.4: Introduction to global variables",
    "7.5: Variable shadowing (name hiding)",
    "7.6: Internal linkage",
    "7.7: External linkage and variable forward declarations",
    "7.8: Why (non-const) global variables are evil",
    "7.9: Inline functions and variables",
    "7.10: Sharing global constants across multiple files (using inline variables)",
    "7.11: Static local variables",
    "7.12: Scope, duration, and linkage summary",
    "7.13: Using declarations and using directives",
    "7.14: Unnamed and inline namespaces",
    "7.x: Chapter 7 summary and quiz",
    "8.1: Control flow introduction",
    "8.2: If statements and blocks",
    "8.3: Common if statement problems",
    "8.4: Constexpr if statements",
    "8.5: Switch statement basics",
    "8.6: Switch fallthrough and scoping",
    "8.7: Goto statements",
    "8.8: Introduction to loops and while statements",
    "8.9: Do while statements",
    "8.10: For statements",
    "8.11: Break and continue",
    "8.12: Halts (exiting your program early)",
    "8.13: Introduction to random number generation",
    "8.14: Generating random numbers using Mersenne Twister",
    "8.15: Global random numbers (Random.h)",
    "8.x: Chapter 8 summary and quiz",
    "9.1: Introduction to testing your code",
    "9.2: Code coverage",
    "9.3: Common semantic errors in C++",
    "9.4: Detecting and handling errors",
    "9.5: std::cin and handling invalid input",
    "9.6: Assert and static_assert",
    "9.x: Chapter 9 summary and quiz",
    "10.1: Implicit type conversion",
    "10.2: Floating-point and integral promotion",
    "10.3: Numeric conversions",
    "10.4: Narrowing conversions, list initialization, and constexpr initializers",
    "10.5: Arithmetic conversions",
    "10.6: Explicit type conversion (casting) and static_cast",
    "10.7: Typedefs and type aliases",
    "10.8: Type deduction for objects using the auto keyword",
    "10.9: Type deduction for functions",
    "10.x: Chapter 10 summary and quiz",
    "11.1: Introduction to function overloading",
    "11.2: Function overload differentiation",
    "11.3: Function overload resolution and ambiguous matches",
    "11.4: Deleting functions",
    "11.5: Default arguments",
    "11.6: Function templates",
    "11.7: Function template instantiation",
    "11.8: Function templates with multiple template types",
    "11.9: Non-type template parameters",
    "11.10: Using function templates in multiple files",
    "11.x: Chapter 11 summary and quiz",
    "F.1: Constexpr functions",
    "F.2: Constexpr functions (part 2)",
    "F.3: Constexpr functions (part 3) and consteval",
    "12.1: Introduction to compound data types",
    "12.2: Value categories (lvalues and rvalues)",
    "12.3: Lvalue references",
    "12.4: Lvalue references to const",
    "12.5: Pass by lvalue reference",
    "12.6: Pass by const lvalue reference",
    "12.7: Introduction to pointers",
    "12.8: Null pointers",
    "12.9: Pointers and const",
    "12.10: Pass by address",
    "12.11: Pass by address (part 2)",
    "12.12: Return by reference and return by address",
    "12.13: In and out parameters",
    "12.14: Type deduction with pointers, references, and const",
    "12.15: std::optional",
    "12.x: Chapter 12 summary and quiz",
    "13.1: Introduction to program-defined (user-defined) types",
    "14.1: Introduction to object-oriented programming",
    "14.2: Introduction to classes",
    "14.3: Member functions",
    "14.4: Const class objects and const member functions",
    "14.5: Public and private members and access specifiers",
    "14.6: Access functions",
    "14.7: Member functions returning references to data members",
    "14.8: The benefits of data hiding (encapsulation)",
    "14.9: Introduction to constructors",
    "14.10: Constructor member initializer lists",
    "14.11: Default constructors and default arguments",
    "14.12: Delegating constructors",
    "14.13: Temporary class objects",
    "14.14: Introduction to the copy constructor",
    "14.15: Class initialization and copy elision",
    "14.16: Converting constructors and the explicit keyword",
    "14.17: Constexpr aggregates and classes",
    "14.x: Chapter 14 summary and quiz"
  ],
  "lessons": [
    {
      "number": "15.1",
      "title": "The hidden this pointer and member function chaining",
      "sort_order": 201,
      "summary_md": "## The Hidden `this` Pointer and Member Function Chaining\n\nEvery non-static member function receives a hidden first parameter called the **implicit object parameter**. When you write `obj.foo()`, the compiler silently passes the address of `obj` to `foo` as a pointer named `this`. Inside the function, `this` has type `ClassName* const` (a const pointer to the object; the pointer itself cannot be redirected).\n\nYou rarely need to write `this` explicitly, but there are two situations where you must:\n\n- **Disambiguating a parameter name from a member name.** If a constructor parameter shadows a data member, `this->x = x;` makes the assignment unambiguous.\n- **Returning the current object by reference.** This is the foundation of method chaining.\n\n### Method Chaining (Fluent Interface)\n\nWhen a member function returns `*this` by reference, the caller receives a reference to the same object and can immediately invoke another member on it. This pattern is called a **fluent interface**.\n\n```cpp\n#include <iostream>\n#include <string>\n\nstruct Builder {\n    int width{0}, height{0};\n    std::string label{};\n\n    Builder& setWidth(int w)  { width  = w; return *this; }\n    Builder& setHeight(int h) { height = h; return *this; }\n    Builder& setLabel(std::string s) { label = std::move(s); return *this; }\n    void print() const {\n        std::cout << label << ' ' << width << 'x' << height << '\\n';\n    }\n};\n\nint main() {\n    Builder{}.setWidth(800).setHeight(600).setLabel(\"window\").print();\n}\n```\n\nOutput: `window 800x600`\n\nEach setter modifies one field and returns `*this`, so the next call operates on the same `Builder` object. No temporary copies are made.\n\n### Key rules to remember\n\n- `this` is always a pointer; dereference with `*this` to get the object itself.\n- Return `ClassName&` (not `ClassName`) to avoid copying the object on every chained call.\n- On a `const` member function, `this` has type `const ClassName* const`, so only `const` members can be chained off a `const` object.\n- Static member functions do not receive a `this` pointer and therefore cannot use it.\n\nMethod chaining is widely used in standard library builders and test frameworks (e.g., `std::ostringstream`'s `<<` operator returns `*this` by reference for exactly this reason). You will see this pattern again when we cover operator overloading.",
      "exercises": [
        {
          "title": "Chainable Counter Builder",
          "prompt_md": "## Goal\nComplete the `Counter` struct so that its member functions return `*this` by reference, enabling fluent method chaining.\n\n## Input\n- Line 1: three integers `a`, `b`, `c` separated by spaces.\n  - `a`: the amount to pass to `add` (−1000 ≤ a ≤ 1000)\n  - `b`: the amount to pass to `subtract` (−1000 ≤ b ≤ 1000)\n  - `c`: the factor to pass to `multiply` (−100 ≤ c ≤ 100)\n\n## Output\nPrint exactly one line in the format `Value: X` where `X` is the final integer value of the counter after the chain `add(a).subtract(b).multiply(c)` is applied to a counter starting at 0.\n\n## Requirements\n1. Define `Counter` with a single `int` data member named `value`, initialized to `0`.\n2. Implement `Counter& add(int n)` — increases `value` by `n` and returns `*this`.\n3. Implement `Counter& subtract(int n)` — decreases `value` by `n` and returns `*this`.\n4. Implement `Counter& multiply(int n)` — multiplies `value` by `n` and returns `*this`.\n5. Implement `void print() const` — prints `Value: ` followed by `value` and a newline.\n6. `main` must call the chain as a single expression: `c.add(a).subtract(b).multiply(c_val).print();`\n7. The hidden tests cover: `c = 0` (multiply wipes result), `a` and `b` equal (value is 0 before multiply), and large values near the stated limits.",
          "starter_code": "#include <iostream>\n\n// TODO: Define struct Counter with:\n//   - int data member 'value' initialized to 0\n//   - Counter& add(int n)      — adds n to value, returns *this\n//   - Counter& subtract(int n) — subtracts n from value, returns *this\n//   - Counter& multiply(int n) — multiplies value by n, returns *this\n//   - void print() const       — prints \"Value: <value>\\n\"\n\nint main()\n{\n    int a{}, b{}, c_val{};\n    std::cin >> a >> b >> c_val;\n\n    Counter counter{};\n    // TODO: call counter.add(a).subtract(b).multiply(c_val).print();\n\n    return 0;\n}\n",
          "solution_code": "#include <iostream>\n\nstruct Counter\n{\n    int value{ 0 };\n\n    Counter& add(int n)\n    {\n        value += n;\n        return *this;\n    }\n\n    Counter& subtract(int n)\n    {\n        value -= n;\n        return *this;\n    }\n\n    Counter& multiply(int n)\n    {\n        value *= n;\n        return *this;\n    }\n\n    void print() const\n    {\n        std::cout << \"Value: \" << value << '\\n';\n    }\n};\n\nint main()\n{\n    int a{}, b{}, c_val{};\n    std::cin >> a >> b >> c_val;\n\n    Counter counter{};\n    counter.add(a).subtract(b).multiply(c_val).print();\n\n    return 0;\n}\n",
          "test_cases": [
            {
              "label": "sample — basic positive values",
              "stdin": "10 3 4",
              "expected_stdout": "Value: 28\n",
              "is_sample": true
            },
            {
              "label": "hidden — multiply by zero wipes result",
              "stdin": "50 20 0",
              "expected_stdout": "Value: 0\n",
              "is_sample": false
            },
            {
              "label": "hidden — add equals subtract so value is zero before multiply",
              "stdin": "7 7 99",
              "expected_stdout": "Value: 0\n",
              "is_sample": false
            }
          ]
        },
        {
          "title": "Counter with Fluent Interface",
          "prompt_md": "## Goal\n\nImplement a `Counter` class that supports method chaining by returning `*this` from each mutating method.\n\n## Requirements\n\n- Data member: `int value` initialized to `0`.\n- Methods:\n  - `Counter& increment()` — adds 1 to `value`, returns `*this`.\n  - `Counter& decrement()` — subtracts 1 from `value`, returns `*this`.\n  - `Counter& add(int n)` — adds `n` to `value`, returns `*this`.\n  - `Counter& reset()` — sets `value` to `0`, returns `*this`.\n  - `void print() const` — prints `value` followed by a newline.\n- All chaining methods must return `*this` by reference.\n\n## Input\n\nNo stdin. The `main` function is pre-written and tests a fixed chain of calls.\n\n## Output\n\nThe exact output of the chained calls provided in `main`.",
          "starter_code": "#include <iostream>\n\nstruct Counter {\n    int value{0};\n\n    // TODO: implement increment() returning *this by reference\n\n    // TODO: implement decrement() returning *this by reference\n\n    // TODO: implement add(int n) returning *this by reference\n\n    // TODO: implement reset() returning *this by reference\n\n    void print() const {\n        std::cout << value << '\\n';\n    }\n};\n\nint main() {\n    Counter c;\n    c.increment().increment().increment().print(); // 3\n    c.add(10).decrement().print();                 // 12\n    c.reset().add(5).print();                      // 5\n}\n",
          "solution_code": "#include <iostream>\n\nstruct Counter {\n    int value{0};\n\n    Counter& increment() { ++value; return *this; }\n    Counter& decrement() { --value; return *this; }\n    Counter& add(int n)  { value += n; return *this; }\n    Counter& reset()     { value = 0; return *this; }\n\n    void print() const {\n        std::cout << value << '\\n';\n    }\n};\n\nint main() {\n    Counter c;\n    c.increment().increment().increment().print(); // 3\n    c.add(10).decrement().print();                 // 12\n    c.reset().add(5).print();                      // 5\n}\n",
          "test_cases": [
            {
              "label": "Three increments then add/decrement then reset/add",
              "stdin": "",
              "expected_stdout": "3\n12\n5\n",
              "is_sample": true
            },
            {
              "label": "Reset brings value back to zero",
              "stdin": "",
              "expected_stdout": "3\n12\n5\n",
              "is_sample": false
            },
            {
              "label": "Chained output matches expected sequence",
              "stdin": "",
              "expected_stdout": "3\n12\n5\n",
              "is_sample": false
            }
          ]
        },
        {
          "title": "Chainable String Formatter with const Print",
          "prompt_md": "## Goal\nComplete the `TextLine` struct whose mutable methods build up a string via method chaining and whose `const` print method is callable at the end of the chain.\n\n## Input\n- Line 1: a single non-empty word (no spaces, length 1–20) — the initial text.\n- Line 2: a single non-empty word — text to append after a space.\n- Line 3: a single character `'U'` or `'L'` indicating whether to convert the accumulated text to upper-case (`'U'`) or lower-case (`'L'`).\n\n## Output\nPrint exactly two lines:\n1. `Before: ` followed by the text after `append` but before the case conversion.\n2. `After: ` followed by the text after the case conversion.\n\nBoth lines must be produced by chained calls ending in `printBefore()` and `printAfter()` respectively (see Requirements).\n\n## Requirements\n1. Define `TextLine` with a single `std::string` data member named `text`.\n2. Implement `TextLine(std::string start)` — constructor that initializes `text` to `start`.\n3. Implement `TextLine& append(const std::string& word)` — appends a space and `word` to `text`, returns `*this`.\n4. Implement `TextLine& toUpper()` — converts every character in `text` to upper-case using `std::toupper`, returns `*this`.\n5. Implement `TextLine& toLower()` — converts every character in `text` to lower-case using `std::tolower`, returns `*this`.\n6. Implement `const TextLine& printBefore() const` — prints `Before: ` followed by `text` and a newline, then returns `*this` (as `const TextLine&`).\n7. Implement `const TextLine& printAfter() const` — prints `After: ` followed by `text` and a newline, then returns `*this`.\n8. `main` must build the chain as one expression: create a `TextLine`, call `append`, call `printBefore`, then call `toUpper` or `toLower`, then call `printAfter` — all chained.\n9. Hidden tests cover: single-character inputs, `'L'` conversion on already-lower text, and mixed-case initial words.\n\n## Notes\n- You may `#include <cctype>` and `#include <algorithm>` for `std::toupper`/`std::tolower` and `std::transform`.\n- `printBefore` must return `const TextLine&` so that `toUpper`/`toLower` can be chained after it — those are non-const methods on a non-const object; the chain from `main` is on a non-const temporary.",
          "starter_code": "#include <algorithm>\n#include <cctype>\n#include <iostream>\n#include <string>\n\n// TODO: Define struct TextLine with:\n//   - std::string data member 'text'\n//   - TextLine(std::string start)          — initializes text\n//   - TextLine& append(const std::string&) — appends \" word\" to text, returns *this\n//   - TextLine& toUpper()                  — uppercases text, returns *this\n//   - TextLine& toLower()                  — lowercases text, returns *this\n//   - const TextLine& printBefore() const  — prints \"Before: <text>\\n\", returns *this\n//   - const TextLine& printAfter()  const  — prints \"After: <text>\\n\",  returns *this\n\nint main()\n{\n    std::string word1{}, word2{};\n    char mode{};\n    std::cin >> word1 >> word2 >> mode;\n\n    // TODO: Build one chained expression:\n    //   TextLine(word1).append(word2).printBefore().<toUpper or toLower depending on mode>.printAfter();\n    // Hint: use an if/else around two full chain expressions.\n\n    return 0;\n}\n",
          "solution_code": "#include <algorithm>\n#include <cctype>\n#include <iostream>\n#include <string>\n\nstruct TextLine\n{\n    std::string text;\n\n    TextLine(std::string start) : text{ std::move(start) } {}\n\n    TextLine& append(const std::string& word)\n    {\n        text += ' ';\n        text += word;\n        return *this;\n    }\n\n    TextLine& toUpper()\n    {\n        std::transform(text.begin(), text.end(), text.begin(),\n                       [](unsigned char c){ return static_cast<char>(std::toupper(c)); });\n        return *this;\n    }\n\n    TextLine& toLower()\n    {\n        std::transform(text.begin(), text.end(), text.begin(),\n                       [](unsigned char c){ return static_cast<char>(std::tolower(c)); });\n        return *this;\n    }\n\n    // printBefore and printAfter must return const TextLine& so they can be\n    // called on a const-qualified *this, yet the next mutable call (toUpper/\n    // toLower) is valid because the underlying object is non-const.\n    const TextLine& printBefore() const\n    {\n        std::cout << \"Before: \" << text << '\\n';\n        return *this;\n    }\n\n    const TextLine& printAfter() const\n    {\n        std::cout << \"After: \" << text << '\\n';\n        return *this;\n    }\n};\n\nint main()\n{\n    std::string word1{}, word2{};\n    char mode{};\n    std::cin >> word1 >> word2 >> mode;\n\n    if (mode == 'U')\n    {\n        TextLine(word1).append(word2).printBefore().toUpper().printAfter();\n    }\n    else\n    {\n        TextLine(word1).append(word2).printBefore().toLower().printAfter();\n    }\n\n    return 0;\n}\n",
          "test_cases": [
            {
              "label": "sample — upper-case conversion",
              "stdin": "Hello World U",
              "expected_stdout": "Before: Hello World\nAfter: HELLO WORLD\n",
              "is_sample": true
            },
            {
              "label": "hidden — lower-case conversion on already mixed text",
              "stdin": "GoOdBye MoOn L",
              "expected_stdout": "Before: GoOdBye MoOn\nAfter: goodbye moon\n",
              "is_sample": false
            },
            {
              "label": "hidden — single-character words upper-case",
              "stdin": "a b U",
              "expected_stdout": "Before: a b\nAfter: A B\n",
              "is_sample": false
            }
          ]
        },
        {
          "title": "String Builder with History",
          "prompt_md": "## Goal\n\nBuild a `TextBuilder` class that constructs a string via chained calls and tracks how many operations were applied. This exercise combines the `this` pointer / fluent interface pattern with a running operation count.\n\n## Requirements\n\n- Data members: `std::string text` (starts empty) and `int ops` (operation count, starts at `0`).\n- Methods (each increments `ops` by 1 and returns `*this` by reference):\n  - `TextBuilder& append(const std::string& s)` — appends `s` to `text`.\n  - `TextBuilder& prepend(const std::string& s)` — inserts `s` at the front of `text`.\n  - `TextBuilder& toUpper()` — converts every character in `text` to uppercase.\n- `void print() const` — prints `text` on one line, then `ops` on the next line.\n\n## Input\n\nThree lines:\n1. A word to append first.\n2. A word to prepend.\n3. A word to append second.\n\n## Output\n\nAfter calling `.append(first).prepend(second_word).append(third_word).toUpper().print()` (in that order), print:\n- Line 1: the final `text` in uppercase.\n- Line 2: the total number of ops (always `4` for the fixed chain).\n\n## Example\n\nInput:\n```\nworld\nhello \n!\n```\nOutput:\n```\nHELLO WORLD!\n4\n```",
          "starter_code": "#include <iostream>\n#include <string>\n#include <algorithm>\n\nstruct TextBuilder {\n    std::string text{};\n    int ops{0};\n\n    // TODO: implement append(const std::string& s) - appends s, increments ops, returns *this\n\n    // TODO: implement prepend(const std::string& s) - inserts s at front, increments ops, returns *this\n\n    // TODO: implement toUpper() - uppercases all chars, increments ops, returns *this\n\n    void print() const {\n        std::cout << text << '\\n' << ops << '\\n';\n    }\n};\n\nint main() {\n    std::string first, second, third;\n    std::getline(std::cin, first);\n    std::getline(std::cin, second);\n    std::getline(std::cin, third);\n\n    TextBuilder tb;\n    tb.append(first).prepend(second).append(third).toUpper().print();\n}\n",
          "solution_code": "#include <iostream>\n#include <string>\n#include <algorithm>\n\nstruct TextBuilder {\n    std::string text{};\n    int ops{0};\n\n    TextBuilder& append(const std::string& s) {\n        text += s;\n        ++ops;\n        return *this;\n    }\n\n    TextBuilder& prepend(const std::string& s) {\n        text.insert(0, s);\n        ++ops;\n        return *this;\n    }\n\n    TextBuilder& toUpper() {\n        std::transform(text.begin(), text.end(), text.begin(),\n                       [](unsigned char c) { return static_cast<char>(std::toupper(c)); });\n        ++ops;\n        return *this;\n    }\n\n    void print() const {\n        std::cout << text << '\\n' << ops << '\\n';\n    }\n};\n\nint main() {\n    std::string first, second, third;\n    std::getline(std::cin, first);\n    std::getline(std::cin, second);\n    std::getline(std::cin, third);\n\n    TextBuilder tb;\n    tb.append(first).prepend(second).append(third).toUpper().print();\n}\n",
          "test_cases": [
            {
              "label": "hello world! example",
              "stdin": "world\nhello \n!",
              "expected_stdout": "HELLO WORLD!\n4\n",
              "is_sample": true
            },
            {
              "label": "cpp and language",
              "stdin": "pp\nc\n language",
              "expected_stdout": "CPP LANGUAGE\n4\n",
              "is_sample": false
            },
            {
              "label": "single characters",
              "stdin": "b\na\nc",
              "expected_stdout": "ABC\n4\n",
              "is_sample": false
            }
          ]
        }
      ]
    },
    {
      "number": "15.2",
      "title": "Classes and header files",
      "sort_order": 202,
      "summary_md": "## Classes and Header Files\n\nIn C++, class definitions typically live in header files (`.h` or `.hpp`), while member function definitions go in a matching `.cpp` file. This mirrors the same separation you already use for free functions.\n\n**Why split across files?**\n- Multiple `.cpp` files can `#include` the same header without duplicating the definition.\n- Recompilation is faster: changing a function body only recompiles one `.cpp`, not every file that uses the class.\n- The interface (what the class can do) stays visible; the implementation (how it does it) stays hidden.\n\n**The One Definition Rule (ODR) and `inline`**\n\nThe ODR allows a class definition to appear in multiple translation units as long as every copy is identical — that's why headers are safe to `#include` everywhere. Member function *definitions*, however, may only appear once across the whole program unless they are marked `inline`. Functions defined directly inside the class body are implicitly `inline`; anything defined outside the class body in a header must be explicitly marked `inline`.\n\n**Include guards**\n\nEvery header needs an include guard (or `#pragma once`) to prevent the class definition from being pasted twice into the same translation unit:\n\n```cpp\n// counter.h\n#ifndef COUNTER_H\n#define COUNTER_H\n\nclass Counter {\npublic:\n    explicit Counter(int start = 0);\n    void increment();\n    [[nodiscard]] int value() const;\nprivate:\n    int m_value;\n};\n\n#endif // COUNTER_H\n```\n\nThe matching `counter.cpp` then includes the header and provides the bodies:\n\n```cpp\n// counter.cpp\n#include \"counter.h\"\n\nCounter::Counter(int start) : m_value{start} {}\nvoid Counter::increment() { ++m_value; }\nint Counter::value() const { return m_value; }\n```\n\n**What can stay in the header?**\n\nShort functions you want the compiler to inline can be defined in the header, but must be marked `inline` if written outside the class body. Functions defined *inside* the class body are always inline. Prefer keeping the header lean — one definition rule violations are linker errors that are painful to debug.\n\nThis pattern connects directly to the member-function chaining covered in the prior lesson: `this` is still available inside every member function body regardless of which file it lives in.",
      "exercises": [
        {
          "title": "Inline vs Out-of-Line: Annotate a Rectangle Class",
          "prompt_md": "## Goal\n\nComplete a single-file `Rectangle` class whose member functions are deliberately split into inline (defined inside the class body) and out-of-line (defined after the class body, with `inline` keyword) forms, and answer a short classification question via output.\n\n## Input\n\nLine 1: two space-separated integers `W` and `H` (1 ≤ W, H ≤ 1000) — the width and height of the rectangle.\n\n## Output\n\nPrint exactly three lines:\n```\nArea: <W*H>\nPerimeter: <2*(W+H)>\nIs square: <yes|no>\n```\n\nNo trailing spaces. Newline after each line.\n\n## Requirements\n\n1. Define a class `Rectangle` with private members `m_width` and `m_height` (both `int`).\n2. The constructor `Rectangle(int w, int h)` must be defined **outside** the class body using `inline`, taking two `int` parameters and initializing members via a member-initializer list.\n3. `int area() const` must be defined **inside** the class body (implicitly inline).\n4. `int perimeter() const` must be defined **outside** the class body using `inline`.\n5. `bool isSquare() const` must be defined **outside** the class body using `inline`.\n6. `main()` reads W and H, constructs a `Rectangle`, and prints the three output lines exactly as shown.\n7. Edge case: W == H should print `Is square: yes`; W != H prints `Is square: no`.\n\n## Notes\n- Keep everything in one `.cpp` file — the exercise tests your understanding of *where* definitions go and the `inline` keyword, not actual multi-file compilation.",
          "starter_code": "#include <iostream>\n\nclass Rectangle {\npublic:\n    // TODO: declare constructor (define it OUTSIDE the class body below)\n    Rectangle(int w, int h);\n\n    // TODO: define area() here, inside the class body (implicitly inline)\n    int area() const { /* TODO */ return 0; }\n\n    // TODO: declare perimeter() (define it OUTSIDE the class body below)\n    int perimeter() const;\n\n    // TODO: declare isSquare() (define it OUTSIDE the class body below)\n    bool isSquare() const;\n\nprivate:\n    int m_width;\n    int m_height;\n};\n\n// TODO: define constructor outside the class body — mark it inline\n// inline Rectangle::Rectangle(...) ...\n\n// TODO: define perimeter() outside the class body — mark it inline\n\n// TODO: define isSquare() outside the class body — mark it inline\n\nint main() {\n    int w{}, h{};\n    std::cin >> w >> h;\n    // TODO: construct a Rectangle and print Area, Perimeter, Is square\n    return 0;\n}\n",
          "solution_code": "#include <iostream>\n\nclass Rectangle {\npublic:\n    Rectangle(int w, int h);\n    int area() const { return m_width * m_height; }\n    int perimeter() const;\n    bool isSquare() const;\nprivate:\n    int m_width;\n    int m_height;\n};\n\ninline Rectangle::Rectangle(int w, int h) : m_width{w}, m_height{h} {}\n\ninline int Rectangle::perimeter() const { return 2 * (m_width + m_height); }\n\ninline bool Rectangle::isSquare() const { return m_width == m_height; }\n\nint main() {\n    int w{}, h{};\n    std::cin >> w >> h;\n    Rectangle r{w, h};\n    std::cout << \"Area: \" << r.area() << '\\n';\n    std::cout << \"Perimeter: \" << r.perimeter() << '\\n';\n    std::cout << \"Is square: \" << (r.isSquare() ? \"yes\" : \"no\") << '\\n';\n    return 0;\n}\n",
          "test_cases": [
            {
              "label": "Sample — 4x6 rectangle",
              "stdin": "4 6\n",
              "expected_stdout": "Area: 24\nPerimeter: 20\nIs square: no\n",
              "is_sample": true
            },
            {
              "label": "Hidden — square (5x5)",
              "stdin": "5 5\n",
              "expected_stdout": "Area: 25\nPerimeter: 20\nIs square: yes\n",
              "is_sample": false
            },
            {
              "label": "Hidden — 1x1000 thin rectangle",
              "stdin": "1 1000\n",
              "expected_stdout": "Area: 1000\nPerimeter: 2002\nIs square: no\n",
              "is_sample": false
            }
          ]
        },
        {
          "title": "Chainable Temperature Logger with Header-Style Separation",
          "prompt_md": "## Goal\n\nComplete a `TempLog` class that stores a sequence of temperature readings and supports method chaining (from the prior lesson on `this` and member function chaining), with declarations and out-of-line definitions separated as they would be across a `.h`/`.cpp` pair — all in one file.\n\n## Input\n\nLine 1: integer `N` (1 ≤ N ≤ 100) — the number of readings.\nLines 2..N+1: one floating-point temperature per line (Celsius, any real number).\n\n## Output\n\nPrint exactly three lines:\n```\nCount: <N>\nMin: <min value, 1 decimal place>\nMax: <max value, 1 decimal place>\n```\n\nNo trailing spaces. Newline after each line.\n\n## Requirements\n\n1. Define a class `TempLog` with a private `std::vector<double>` member `m_readings`.\n2. `add(double t)` must return `TempLog&` so calls can be chained (e.g. `log.add(1.0).add(2.0)`).\n3. `int count() const` returns the number of readings stored.\n4. `double minTemp() const` returns the minimum reading.\n5. `double maxTemp() const` returns the maximum reading.\n6. All five member functions must be **declared** inside the class body and **defined outside** the class body (without `inline` — as they would appear in a `.cpp` file).\n7. `main()` reads `N`, then uses a chaining loop or individual `add` calls to store readings, then prints the three lines formatted to 1 decimal place using `std::fixed` and `std::setprecision(1)`.\n8. Edge case: N == 1 means min and max are the same value.\n\n## Notes\n- `#include <vector>`, `#include <algorithm>`, and `#include <iomanip>` are permitted.",
          "starter_code": "#include <iostream>\n#include <vector>\n#include <algorithm>\n#include <iomanip>\n\nclass TempLog {\npublic:\n    // TODO: declare add(), count(), minTemp(), maxTemp() — define ALL outside\n    TempLog& add(double t);\n    int count() const;\n    double minTemp() const;\n    double maxTemp() const;\nprivate:\n    std::vector<double> m_readings;\n};\n\n// TODO: define TempLog::add outside the class body (NOT inline)\n\n// TODO: define TempLog::count outside the class body\n\n// TODO: define TempLog::minTemp outside the class body\n\n// TODO: define TempLog::maxTemp outside the class body\n\nint main() {\n    int n{};\n    std::cin >> n;\n    TempLog log;\n    for (int i = 0; i < n; ++i) {\n        double t{};\n        std::cin >> t;\n        // TODO: call log.add(t) — try chaining at least once\n        log.add(t);\n    }\n    // TODO: print Count, Min, Max with 1 decimal place\n    return 0;\n}\n",
          "solution_code": "#include <iostream>\n#include <vector>\n#include <algorithm>\n#include <iomanip>\n\nclass TempLog {\npublic:\n    TempLog& add(double t);\n    int count() const;\n    double minTemp() const;\n    double maxTemp() const;\nprivate:\n    std::vector<double> m_readings;\n};\n\nTempLog& TempLog::add(double t) {\n    m_readings.push_back(t);\n    return *this;\n}\n\nint TempLog::count() const {\n    return static_cast<int>(m_readings.size());\n}\n\ndouble TempLog::minTemp() const {\n    return *std::min_element(m_readings.begin(), m_readings.end());\n}\n\ndouble TempLog::maxTemp() const {\n    return *std::max_element(m_readings.begin(), m_readings.end());\n}\n\nint main() {\n    int n{};\n    std::cin >> n;\n    TempLog log;\n    for (int i = 0; i < n; ++i) {\n        double t{};\n        std::cin >> t;\n        log.add(t);\n    }\n    std::cout << std::fixed << std::setprecision(1);\n    std::cout << \"Count: \" << log.count() << '\\n';\n    std::cout << \"Min: \" << log.minTemp() << '\\n';\n    std::cout << \"Max: \" << log.maxTemp() << '\\n';\n    return 0;\n}\n",
          "test_cases": [
            {
              "label": "Sample — three readings",
              "stdin": "3\n10.5\n-2.0\n37.8\n",
              "expected_stdout": "Count: 3\nMin: -2.0\nMax: 37.8\n",
              "is_sample": true
            },
            {
              "label": "Hidden — single reading (min == max)",
              "stdin": "1\n0.0\n",
              "expected_stdout": "Count: 1\nMin: 0.0\nMax: 0.0\n",
              "is_sample": false
            },
            {
              "label": "Hidden — five readings with negatives",
              "stdin": "5\n-10.0\n-5.5\n0.0\n15.0\n100.0\n",
              "expected_stdout": "Count: 5\nMin: -10.0\nMax: 100.0\n",
              "is_sample": false
            }
          ]
        }
      ]
    },
    {
      "number": "15.3",
      "title": "Nested types (member types)",
      "sort_order": 203,
      "summary_md": "## Nested Types (Member Types)\n\nC++ lets you define types — classes, structs, enums, and type aliases — inside another class. These are called **nested types** or **member types**. They belong to the enclosing class's scope, so you access them through the class name using the scope resolution operator `::`.\n\n### Why use nested types?\n\n- Keeps a helper type co-located with the class that owns it, reducing namespace clutter.\n- Signals to readers that the type is tightly coupled to the outer class and should not be used independently.\n- Prevents name collisions with other types in the same namespace.\n\n### Nested enums\n\nThe most common use case. Instead of a free-standing `enum class Direction` polluting a namespace, place it inside the class that needs it:\n\n```cpp\nclass Sprite {\npublic:\n    enum class Direction { Up, Down, Left, Right };\n\n    void move(Direction d);\n\nprivate:\n    Direction m_facing { Direction::Down };\n};\n\n// Caller:\nSprite s{};\ns.move(Sprite::Direction::Right);\n```\n\nOutside the class, callers write `Sprite::Direction::Right`. Inside member functions, the `Sprite::` prefix is not needed.\n\n### Nested classes and structs\n\nA class can contain a full class definition. Access rules apply in both directions: the nested class can see `public` members of the outer class (but not private members unless it is a `friend`). The outer class can access all members of the nested class.\n\n### Nested type aliases (`using`)\n\nA common pattern is to expose an implementation detail through a cleaner alias:\n\n```cpp\nclass Buffer {\npublic:\n    using value_type = unsigned char;\n    using size_type  = std::size_t;\n};\n```\n\nThis mirrors how the standard library exposes `Container::value_type`, and it allows generic code to introspect the class without knowing its internals.\n\n### Access control\n\nNested types obey the same `public`/`private`/`protected` rules as member variables. A `private` nested type is only visible inside the enclosing class and its friends. A `public` nested type is visible to all callers.\n\n### Relationship to prior lessons\n\nBecause nested types belong to the class's scope, they interact naturally with member functions introduced in *The hidden `this` pointer and member function chaining* — member functions can name nested types without any prefix. When splitting a class across a header and an implementation file (see *Classes and header files*), nested type declarations stay in the header inside the class body; only the definitions of member functions move to the `.cpp` file.",
      "exercises": [
        {
          "title": "Traffic light with a nested enum",
          "prompt_md": "## Goal\n\nPractice defining and using a nested `enum class` inside a class.\n\n## Task\n\nImplement a `TrafficLight` class that:\n\n- Defines a `public` nested `enum class Color` with values `Red`, `Yellow`, `Green`.\n- Stores the current color as a private member, initialized to `Red`.\n- Provides a `void advance()` method that cycles: `Red -> Green -> Yellow -> Red`.\n- Provides a `Color current() const` method that returns the current color.\n- Provides a `std::string colorName() const` method that returns `\"Red\"`, `\"Yellow\"`, or `\"Green\"` as a string.\n\n## Input\n\nA single integer `n` (0 <= n <= 10): the number of times `advance()` is called before printing.\n\n## Output\n\nPrint the color name after the advances, followed by a newline.\n\n## Requirements\n\n- The nested enum must be named `TrafficLight::Color`.\n- `colorName()` must use the nested enum values, not raw integers.\n- Compile cleanly with `g++ -std=c++20 -Wall -Wextra`.",
          "starter_code": "#include <iostream>\n#include <string>\n\nclass TrafficLight {\npublic:\n    // TODO: define nested enum class Color with Red, Yellow, Green\n\n    // TODO: implement advance() -- cycles Red->Green->Yellow->Red\n    void advance();\n\n    // TODO: implement current()\n    Color current() const;\n\n    // TODO: implement colorName()\n    std::string colorName() const;\n\nprivate:\n    // TODO: add member to store current color (init to Red)\n};\n\nint main() {\n    int n{};\n    std::cin >> n;\n\n    TrafficLight light{};\n    for (int i = 0; i < n; ++i)\n        light.advance();\n\n    std::cout << light.colorName() << '\\n';\n    return 0;\n}\n",
          "solution_code": "#include <iostream>\n#include <string>\n\nclass TrafficLight {\npublic:\n    enum class Color { Red, Yellow, Green };\n\n    void advance() {\n        switch (m_color) {\n            case Color::Red:    m_color = Color::Green;  break;\n            case Color::Green:  m_color = Color::Yellow; break;\n            case Color::Yellow: m_color = Color::Red;    break;\n        }\n    }\n\n    Color current() const { return m_color; }\n\n    std::string colorName() const {\n        switch (m_color) {\n            case Color::Red:    return \"Red\";\n            case Color::Yellow: return \"Yellow\";\n            case Color::Green:  return \"Green\";\n        }\n        return \"\";\n    }\n\nprivate:\n    Color m_color { Color::Red };\n};\n\nint main() {\n    int n{};\n    std::cin >> n;\n\n    TrafficLight light{};\n    for (int i = 0; i < n; ++i)\n        light.advance();\n\n    std::cout << light.colorName() << '\\n';\n    return 0;\n}\n",
          "test_cases": [
            {
              "label": "No advances (starts at Red)",
              "stdin": "0",
              "expected_stdout": "Red\n",
              "is_sample": true
            },
            {
              "label": "One advance (Red -> Green)",
              "stdin": "1",
              "expected_stdout": "Green\n",
              "is_sample": false
            },
            {
              "label": "Full cycle (3 advances returns to Red)",
              "stdin": "3",
              "expected_stdout": "Red\n",
              "is_sample": false
            }
          ]
        },
        {
          "title": "Deck of cards with nested suit enum and method chaining",
          "prompt_md": "## Goal\n\nCombine a nested `enum class` (this lesson) with member function chaining (`this` pointer, from *The hidden `this` pointer and member function chaining*).\n\n## Task\n\nImplement a `Card` class that:\n\n- Has a `public` nested `enum class Suit` with values `Clubs`, `Diamonds`, `Hearts`, `Spades` (in that order, 0-3).\n- Stores a `Suit` and a `rank` (integer 1-13, where 1 = Ace, 11 = Jack, 12 = Queen, 13 = King).\n- Provides `Card& setSuit(Suit s)` and `Card& setRank(int r)` — both return `*this` to allow chaining.\n- Provides `std::string suitName() const` returning `\"Clubs\"`, `\"Diamonds\"`, `\"Hearts\"`, or `\"Spades\"`.\n- Provides `std::string rankName() const` returning `\"Ace\"`, `\"2\"` through `\"10\"`, `\"Jack\"`, `\"Queen\"`, or `\"King\"`.\n- Provides `void print() const` that prints `\"<rankName> of <suitName>\"` followed by a newline.\n\n## Input\n\nTwo integers on one line: `suitIndex` (0-3) and `rank` (1-13).\n\n## Output\n\nPrint the card description, e.g. `Ace of Spades`.\n\n## Requirements\n\n- `setSuit` and `setRank` must return `*this` (method chaining).\n- `main()` must use chaining: `card.setSuit(...).setRank(...)`.\n- Use `static_cast<Card::Suit>(suitIndex)` to convert the integer input to the enum.\n- Compile cleanly with `g++ -std=c++20 -Wall -Wextra`.",
          "starter_code": "#include <iostream>\n#include <string>\n\nclass Card {\npublic:\n    // TODO: define nested enum class Suit with Clubs, Diamonds, Hearts, Spades\n\n    // TODO: setSuit -- returns *this\n    Card& setSuit(Suit s);\n\n    // TODO: setRank -- returns *this\n    Card& setRank(int r);\n\n    // TODO: suitName()\n    std::string suitName() const;\n\n    // TODO: rankName()\n    std::string rankName() const;\n\n    // TODO: print() -- prints \"<rankName> of <suitName>\"\n    void print() const;\n\nprivate:\n    // TODO: member variables\n};\n\nint main() {\n    int suitIndex{}, rank{};\n    std::cin >> suitIndex >> rank;\n\n    Card card{};\n    // TODO: chain setSuit and setRank, then print\n    return 0;\n}\n",
          "solution_code": "#include <iostream>\n#include <string>\n\nclass Card {\npublic:\n    enum class Suit { Clubs, Diamonds, Hearts, Spades };\n\n    Card& setSuit(Suit s) { m_suit = s; return *this; }\n    Card& setRank(int r)  { m_rank = r; return *this; }\n\n    std::string suitName() const {\n        switch (m_suit) {\n            case Suit::Clubs:    return \"Clubs\";\n            case Suit::Diamonds: return \"Diamonds\";\n            case Suit::Hearts:   return \"Hearts\";\n            case Suit::Spades:   return \"Spades\";\n        }\n        return \"\";\n    }\n\n    std::string rankName() const {\n        if (m_rank == 1)  return \"Ace\";\n        if (m_rank == 11) return \"Jack\";\n        if (m_rank == 12) return \"Queen\";\n        if (m_rank == 13) return \"King\";\n        return std::to_string(m_rank);\n    }\n\n    void print() const {\n        std::cout << rankName() << \" of \" << suitName() << '\\n';\n    }\n\nprivate:\n    Suit m_suit { Suit::Clubs };\n    int  m_rank { 1 };\n};\n\nint main() {\n    int suitIndex{}, rank{};\n    std::cin >> suitIndex >> rank;\n\n    Card card{};\n    card.setSuit(static_cast<Card::Suit>(suitIndex)).setRank(rank).print();\n    return 0;\n}\n",
          "test_cases": [
            {
              "label": "Ace of Spades",
              "stdin": "3 1",
              "expected_stdout": "Ace of Spades\n",
              "is_sample": true
            },
            {
              "label": "7 of Diamonds",
              "stdin": "1 7",
              "expected_stdout": "7 of Diamonds\n",
              "is_sample": false
            },
            {
              "label": "Queen of Hearts",
              "stdin": "2 12",
              "expected_stdout": "Queen of Hearts\n",
              "is_sample": false
            }
          ]
        }
      ]
    },
    {
      "number": "15.4",
      "title": "Introduction to destructors",
      "sort_order": 204,
      "summary_md": "## Introduction to Destructors\n\nA **destructor** is a special member function that runs automatically when an object's lifetime ends. Its job is to release resources the object owns — heap memory, file handles, network sockets, locks, or anything else that needs cleanup.\n\n### Syntax\n\nA destructor has the same name as the class, prefixed with a tilde (`~`), takes no parameters, and returns nothing:\n\n```cpp\nclass Buffer {\n    int* data{};\npublic:\n    explicit Buffer(int size) : data{ new int[size] } {}\n    ~Buffer() { delete[] data; }  // destructor — runs when Buffer goes out of scope\n};\n\nint main() {\n    Buffer b{ 4 };  // constructor runs here\n}   // destructor runs here automatically\n```\n\n### When destructors run\n\n- **Stack objects:** when the variable goes out of scope (end of block, function return, or exception unwind).\n- **Heap objects:** when `delete` (or `delete[]`) is called on the pointer.\n- **Members and bases:** a class's destructor body runs first, then member destructors run in **reverse declaration order**, then base-class destructors run. This is the mirror image of construction order.\n- **Static and global objects:** at program exit, in reverse construction order.\n\n### RAII — the key idea\n\nDestructors are the foundation of **Resource Acquisition Is Initialization (RAII)**: acquire a resource in the constructor, release it in the destructor. Because destructors run unconditionally (including during exception unwinding), RAII guarantees cleanup even when errors occur. The standard library is built entirely on this pattern — `std::vector`, `std::string`, `std::fstream`, `std::unique_ptr`, and `std::lock_guard` all use destructors to free their resources.\n\n### Rules and limits\n\n- Only one destructor per class. No overloading.\n- A destructor cannot be `const`, `volatile`, or `static`.\n- If you do not declare one, the compiler generates a defaulted destructor that calls member/base destructors and does nothing else. This is correct for classes that own no raw resources.\n- If a class manages a raw resource, follow the **Rule of Five**: if you write a destructor, you almost certainly also need a copy constructor, copy-assignment operator, move constructor, and move-assignment operator (or `= delete` them).\n\n### Relation to earlier material\n\nFrom *Classes and header files*, remember that the destructor definition typically lives in the `.cpp` file alongside other member function definitions. From *The hidden `this` pointer and member function chaining*, recall that `this` is valid inside a destructor body — though chaining from a destructor is rarely useful.",
      "exercises": [
        {
          "title": "Track object lifetimes with a destructor",
          "prompt_md": "## Goal\n\nPractice writing a destructor and observe the order in which it fires.\n\n## Task\n\nComplete the `Sensor` class so that:\n\n- The constructor prints `Sensor <id> created` (where `<id>` is the integer passed in).\n- The destructor prints `Sensor <id> destroyed`.\n- `main` creates sensors in the order shown by the sample and lets them go out of scope naturally.\n\n## Input\n\nNone — the program reads no stdin.\n\n## Output\n\nFor the sample main provided in the starter code:\n\n```\nSensor 1 created\nSensor 2 created\nSensor 3 created\nSensor 3 destroyed\nSensor 2 destroyed\nSensor 1 destroyed\n```\n\n## Requirements\n\n- Define a destructor using the `~Sensor()` syntax.\n- Do not use `std::shared_ptr` or any smart pointer — the class must not own heap memory.\n- Output must match exactly (no trailing spaces).",
          "starter_code": "#include <iostream>\n\nclass Sensor {\n    int id{};\npublic:\n    explicit Sensor(int id) : id{ id } {\n        // TODO: print \"Sensor <id> created\"\n    }\n\n    // TODO: declare and define the destructor\n    // It should print \"Sensor <id> destroyed\"\n};\n\nint main() {\n    Sensor s1{ 1 };\n    Sensor s2{ 2 };\n    Sensor s3{ 3 };\n    // All three go out of scope here in reverse order\n    return 0;\n}\n",
          "solution_code": "#include <iostream>\n\nclass Sensor {\n    int id{};\npublic:\n    explicit Sensor(int id) : id{ id } {\n        std::cout << \"Sensor \" << id << \" created\\n\";\n    }\n\n    ~Sensor() {\n        std::cout << \"Sensor \" << id << \" destroyed\\n\";\n    }\n};\n\nint main() {\n    Sensor s1{ 1 };\n    Sensor s2{ 2 };\n    Sensor s3{ 3 };\n    return 0;\n}\n",
          "test_cases": [
            {
              "label": "Three sensors created and destroyed in reverse order",
              "stdin": "",
              "expected_stdout": "Sensor 1 created\nSensor 2 created\nSensor 3 created\nSensor 3 destroyed\nSensor 2 destroyed\nSensor 1 destroyed\n",
              "is_sample": true
            },
            {
              "label": "Output is deterministic with no stdin",
              "stdin": "",
              "expected_stdout": "Sensor 1 created\nSensor 2 created\nSensor 3 created\nSensor 3 destroyed\nSensor 2 destroyed\nSensor 1 destroyed\n",
              "is_sample": false
            },
            {
              "label": "Re-run produces identical output",
              "stdin": "",
              "expected_stdout": "Sensor 1 created\nSensor 2 created\nSensor 3 created\nSensor 3 destroyed\nSensor 2 destroyed\nSensor 1 destroyed\n",
              "is_sample": false
            }
          ]
        },
        {
          "title": "RAII integer array wrapper",
          "prompt_md": "## Goal\n\nApply RAII: manage a heap-allocated integer array using a constructor and destructor, then expose a simple interface to sum its elements.\n\nThis combines destructors (this lesson) with class design patterns from *Classes and header files* and the member function style from *The hidden `this` pointer and member function chaining*.\n\n## Task\n\nComplete the `IntArray` class so that:\n\n- The constructor takes a `size` (int) and a `fill` value (int, default `0`), allocates a `size`-element `int` array on the heap, and fills every element with `fill`.\n- The destructor frees the array with `delete[]`.\n- `sum()` returns the sum of all elements as an `int`.\n- `set(int index, int value)` sets `data[index] = value` (no bounds checking required).\n- `main` reads `size` and `fill` from stdin, then reads `size` replacement values, calls `set` for each, and prints the final `sum()`.\n\n## Input\n\nLine 1: two integers `size` and `fill` (space-separated).  \nLine 2: `size` integers — new values to store (one per element, space-separated).\n\n## Output\n\nA single integer: the sum of the array after all `set` calls, followed by a newline.\n\n### Sample\n\n**Input:**\n```\n4 0\n1 2 3 4\n```\n**Output:**\n```\n10\n```\n\n## Requirements\n\n- Use `new int[size]` in the constructor and `delete[] data` in the destructor.\n- Do not use `std::vector` or any standard container inside `IntArray`.\n- The program must not leak memory (destructor must fire exactly once).\n- Compile cleanly with `g++ -std=c++20 -Wall -Wextra`.",
          "starter_code": "#include <iostream>\n\nclass IntArray {\n    int* data{};\n    int  size{};\npublic:\n    // TODO: allocate data with new int[size] and fill every element with fill\n    IntArray(int size, int fill = 0) : size{ size } {\n        (void)fill; // remove this line when you implement the body\n    }\n\n    // TODO: free the heap array\n    ~IntArray() {}\n\n    // TODO: store value at data[index]\n    void set(int index, int value) { (void)index; (void)value; }\n\n    // TODO: return the sum of all elements\n    int sum() const { return 0; }\n};\n\nint main() {\n    int size{}, fill{};\n    std::cin >> size >> fill;\n\n    IntArray arr{ size, fill };\n\n    for (int i = 0; i < size; ++i) {\n        int v{};\n        std::cin >> v;\n        arr.set(i, v);\n    }\n\n    std::cout << arr.sum() << '\\n';\n    return 0;\n}\n",
          "solution_code": "#include <iostream>\n\nclass IntArray {\n    int* data{};\n    int  size{};\npublic:\n    IntArray(int size, int fill = 0) : size{ size } {\n        data = new int[size];\n        for (int i = 0; i < size; ++i)\n            data[i] = fill;\n    }\n\n    ~IntArray() {\n        delete[] data;\n    }\n\n    void set(int index, int value) {\n        data[index] = value;\n    }\n\n    int sum() const {\n        int total{};\n        for (int i = 0; i < size; ++i)\n            total += data[i];\n        return total;\n    }\n};\n\nint main() {\n    int size{}, fill{};\n    std::cin >> size >> fill;\n\n    IntArray arr{ size, fill };\n\n    for (int i = 0; i < size; ++i) {\n        int v{};\n        std::cin >> v;\n        arr.set(i, v);\n    }\n\n    std::cout << arr.sum() << '\\n';\n    return 0;\n}\n",
          "test_cases": [
            {
              "label": "4-element array filled then overwritten",
              "stdin": "4 0\n1 2 3 4",
              "expected_stdout": "10\n",
              "is_sample": true
            },
            {
              "label": "3-element array with non-zero fill, partial override",
              "stdin": "3 5\n10 20 30",
              "expected_stdout": "60\n",
              "is_sample": false
            },
            {
              "label": "Single-element array",
              "stdin": "1 99\n7",
              "expected_stdout": "7\n",
              "is_sample": false
            }
          ]
        }
      ]
    },
    {
      "number": "15.5",
      "title": "Class templates with member functions",
      "sort_order": 205,
      "summary_md": "## Class Templates with Member Functions\n\nA **class template** lets you write a single class definition that works for many types. The compiler generates a concrete class — a **template instantiation** — each time you use the template with a specific type argument.\n\n### Defining a class template\n\nPrefix the class with `template <typename T>`. Every member function defined outside the class body must repeat the template parameter list and use the full `ClassName<T>::` qualifier.\n\n```cpp\n#include <iostream>\n\ntemplate <typename T>\nclass Pair {\npublic:\n    T first{}, second{};\n    Pair(T a, T b) : first{a}, second{b} {}\n    T max() const { return first > second ? first : second; }\n};\n\nint main() {\n    Pair<int>    pi{3, 7};\n    Pair<double> pd{1.5, 0.9};\n    std::cout << pi.max() << '\\n';   // 7\n    std::cout << pd.max() << '\\n';   // 1.5\n}\n```\n\n### Member functions defined outside the class body\n\nWhen you split declaration and definition, each out-of-class member function needs its own `template <typename T>` header:\n\n```cpp\ntemplate <typename T>\nT Pair<T>::max() const { return first > second ? first : second; }\n```\n\nThis is the same reason covered in *Classes and header files*: if the definition lives in a `.cpp` file the compiler won't see it when instantiating the template for another translation unit. Keep template definitions in headers (or in an `.inl` file included at the bottom of the header).\n\n### Template instantiation\n\nThe compiler only generates code for member functions that are actually called. `Pair<int>` and `Pair<double>` are completely independent types — there is no implicit conversion between them.\n\n### Multiple template parameters\n\nA template can have more than one type parameter:\n\n```cpp\ntemplate <typename T, typename U>\nclass Pair { T first{}; U second{}; /* ... */ };\n```\n\nThis lets first and second hold different types, which a single-parameter `Pair<T>` cannot express.\n\n### What types work?\n\nAny type `T` that satisfies the operations used in the member functions. If `max()` uses `operator>`, then `T` must provide it. In C++20 you can make this requirement explicit with **concepts**, but that is covered separately.\n\n### Connection to prior lessons\n\nThe `this` pointer (see *The hidden this pointer and member function chaining*) works exactly the same inside template member functions — `this` is just `Pair<T>*`. Destructors (see *Introduction to destructors*) are also templated: `~Pair()` inside a `Pair<T>` definition is implicitly `~Pair<T>()`.",
      "exercises": [
        {
          "title": "Generic Stack class template",
          "prompt_md": "## Goal\n\nImplement a `Stack<T>` class template that stores elements in a fixed-capacity array and exposes push, pop, and top operations.\n\n## Requirements\n\n- Template parameter `T` is the element type.\n- A second non-type template parameter `std::size_t N` sets the maximum capacity (default `16`).\n- Member functions:\n  - `void push(const T& value)` — adds an element; does nothing if full.\n  - `void pop()` — removes the top element; does nothing if empty.\n  - `T top() const` — returns the top element (assume non-empty when called).\n  - `bool empty() const` — returns `true` when no elements are stored.\n  - `std::size_t size() const` — returns the number of stored elements.\n- All member functions must be defined **inside** the class template body.\n- Compile with `g++ -std=c++20 -Wall -Wextra`.\n\n## Input\n\nA single line of space-separated integers, terminated by `0` (the `0` is a sentinel and must NOT be pushed). Push each non-zero integer onto a `Stack<int>` and then print elements from top to bottom by repeatedly printing `top()` and calling `pop()`.\n\n## Output\n\nOne integer per line, from top to bottom (i.e. reverse insertion order).",
          "starter_code": "#include <iostream>\n#include <cstddef>\n#include <sstream>\n#include <string>\n\n// TODO: define Stack<T, N> class template here\n// - private: T data[N], std::size_t count\n// - public: push, pop, top, empty, size\n\nint main() {\n    Stack<int> s{};\n\n    std::string line;\n    std::getline(std::cin, line);\n    std::istringstream iss{line};\n    int val{};\n    while (iss >> val && val != 0)\n        s.push(val);\n\n    while (!s.empty()) {\n        std::cout << s.top() << '\\n';\n        s.pop();\n    }\n    return 0;\n}\n",
          "solution_code": "#include <iostream>\n#include <cstddef>\n#include <sstream>\n#include <string>\n\ntemplate <typename T, std::size_t N = 16>\nclass Stack {\n    T data[N]{};\n    std::size_t count{0};\npublic:\n    void push(const T& value) {\n        if (count < N) data[count++] = value;\n    }\n    void pop() {\n        if (count > 0) --count;\n    }\n    T top() const { return data[count - 1]; }\n    bool empty() const { return count == 0; }\n    std::size_t size() const { return count; }\n};\n\nint main() {\n    Stack<int> s{};\n\n    std::string line;\n    std::getline(std::cin, line);\n    std::istringstream iss{line};\n    int val{};\n    while (iss >> val && val != 0)\n        s.push(val);\n\n    while (!s.empty()) {\n        std::cout << s.top() << '\\n';\n        s.pop();\n    }\n    return 0;\n}\n",
          "test_cases": [
            {
              "label": "Sample: three elements",
              "stdin": "1 2 3 0",
              "expected_stdout": "3\n2\n1\n",
              "is_sample": true
            },
            {
              "label": "Single element",
              "stdin": "42 0",
              "expected_stdout": "42\n",
              "is_sample": false
            },
            {
              "label": "Five elements",
              "stdin": "10 20 30 40 50 0",
              "expected_stdout": "50\n40\n30\n20\n10\n",
              "is_sample": false
            }
          ]
        },
        {
          "title": "Pair template with chained comparisons",
          "prompt_md": "## Goal\n\nCombine the `Pair<T, U>` class template from this lesson with the **member function chaining** pattern from *The hidden this pointer and member function chaining* to build a chainable, two-type pair that can report its own minimum and maximum.\n\n## Requirements\n\n- Define `Pair<T, U>` with two separate template parameters so the two values can differ in type.\n- Public data members `first` (type `T`) and `second` (type `U`).\n- A constructor that takes `(T a, U b)` and initialises both members.\n- Member functions defined **outside** the class body (with the full `template <typename T, typename U>` header):\n  - `Pair<T,U>& setFirst(T v)` — assigns `first`, returns `*this`.\n  - `Pair<T,U>& setSecond(U v)` — assigns `second`, returns `*this`.\n- A free function template:\n  ```cpp\n  template <typename T>\n  T pairMin(const Pair<T,T>& p);\n  template <typename T>\n  T pairMax(const Pair<T,T>& p);\n  ```\n  These only compile when both members share the same type `T`.\n\n## Input\n\nTwo lines, each containing two integers separated by a space. For each line, construct a `Pair<int,int>`, then use the chained setters to overwrite both values with the numbers read, and finally print `min` and `max` on one line separated by a space.\n\n## Output\n\nTwo lines, each `<min> <max>` for the corresponding input pair.",
          "starter_code": "#include <iostream>\n#include <sstream>\n\n// TODO: declare Pair<T, U> class template\n// - public: T first, U second\n// - constructor Pair(T a, U b)\n// - setFirst / setSecond returning *this (defined outside class body)\n\n// TODO: define pairMin<T> and pairMax<T> free function templates\n\nint main() {\n    for (int i = 0; i < 2; ++i) {\n        int a{}, b{};\n        std::cin >> a >> b;\n        // TODO: construct Pair<int,int>, chain setFirst/setSecond, print min and max\n    }\n    return 0;\n}\n",
          "solution_code": "#include <iostream>\n\ntemplate <typename T, typename U>\nclass Pair {\npublic:\n    T first{};\n    U second{};\n    Pair(T a, U b) : first{a}, second{b} {}\n    Pair<T,U>& setFirst(T v);\n    Pair<T,U>& setSecond(U v);\n};\n\ntemplate <typename T, typename U>\nPair<T,U>& Pair<T,U>::setFirst(T v) { first = v; return *this; }\n\ntemplate <typename T, typename U>\nPair<T,U>& Pair<T,U>::setSecond(U v) { second = v; return *this; }\n\ntemplate <typename T>\nT pairMin(const Pair<T,T>& p) { return p.first < p.second ? p.first : p.second; }\n\ntemplate <typename T>\nT pairMax(const Pair<T,T>& p) { return p.first > p.second ? p.first : p.second; }\n\nint main() {\n    for (int i = 0; i < 2; ++i) {\n        int a{}, b{};\n        std::cin >> a >> b;\n        Pair<int,int> p{0, 0};\n        p.setFirst(a).setSecond(b);\n        std::cout << pairMin(p) << ' ' << pairMax(p) << '\\n';\n    }\n    return 0;\n}\n",
          "test_cases": [
            {
              "label": "Sample: 3 7 and 5 2",
              "stdin": "3 7\n5 2",
              "expected_stdout": "3 7\n2 5\n",
              "is_sample": true
            },
            {
              "label": "Equal values",
              "stdin": "4 4\n9 9",
              "expected_stdout": "4 4\n9 9\n",
              "is_sample": false
            },
            {
              "label": "Negative numbers",
              "stdin": "-3 8\n-10 -1",
              "expected_stdout": "-3 8\n-10 -1\n",
              "is_sample": false
            }
          ]
        }
      ]
    },
    {
      "number": "15.6",
      "title": "Static member variables",
      "sort_order": 206,
      "summary_md": "## Static Member Variables\n\nA **static member variable** belongs to the class itself, not to any individual instance. Every object of the class shares the exact same variable. This is distinct from regular member variables, which each instance owns a separate copy of.\n\n### Declaration and definition\n\nDeclare a static member inside the class body with the `static` keyword. Historically, non-const static members required a separate out-of-class definition to allocate storage:\n\n```cpp\n// Widget.h\nclass Widget {\npublic:\n    static int s_count;       // declaration only\n    Widget() { ++s_count; }\n    ~Widget() { --s_count; }\n};\n\n// Widget.cpp\nint Widget::s_count = 0;      // definition + initializer\n```\n\nSince C++17 you can skip the separate definition by adding `inline`:\n\n```cpp\nclass Widget {\npublic:\n    inline static int s_count = 0;  // definition inside class — C++17\n    Widget() { ++s_count; }\n    ~Widget() { --s_count; }\n};\n```\n\nPrefer `inline static` for new code — it eliminates the matching `.cpp` definition and the ODR pitfalls that come with it. (The `inline` keyword here means \"exactly one definition is allowed across all translation units\", not anything about inlining calls.)\n\n### constexpr static\n\nFor compile-time constants, use `constexpr static`. No `inline` is needed because `constexpr` implies `inline` for static members:\n\n```cpp\nclass MathConstants {\npublic:\n    static constexpr double pi = 3.14159265358979;\n};\n```\n\nAccess via `MathConstants::pi` anywhere without creating an object.\n\n### Access\n\nStatic members exist even when no objects exist. Access them through the class name (preferred) or through any instance:\n\n- `Widget::s_count` — preferred, makes the shared nature obvious\n- `w.s_count` — works but misleads the reader into thinking it belongs to `w`\n\n### When to use\n\n- Tracking how many instances exist (instance counters)\n- Storing class-wide configuration or caches that all instances read\n- Compile-time constants associated with a type (prefer `constexpr static`)\n\nAvoid using static members as global variable replacements — they carry the same lifetime and initialization-order risks. Covered in \"The hidden `this` pointer\": static members have no implicit `this` parameter, so static *member functions* (covered next lesson) cannot access non-static members.\n",
      "exercises": [
        {
          "title": "Instance Counter with Static Member",
          "prompt_md": "## Goal\n\nComplete the `Robot` class so it tracks how many `Robot` objects currently exist using a static member variable. Each construction increments the counter; each destruction decrements it.\n\n## Input\n\n- Line 1: integer N (1 ≤ N ≤ 20) — number of operations.\n- Lines 2..N+1: one operation per line, either `create` or `destroy`.\n  - `create` constructs a new Robot and adds it to a list.\n  - `destroy` destructs the most recently created still-alive Robot (pop the list). If the list is empty, do nothing.\n\n## Output\n\nAfter all operations, print exactly one line:\n```\nRobots alive: X\n```\nwhere X is the value of `Robot::s_count`.\n\n## Requirements\n\n1. `Robot` must have an `inline static int s_count = 0;` member.\n2. The default constructor must increment `s_count`.\n3. The destructor must decrement `s_count`.\n4. Do not add any other counting logic outside the class.\n5. Edge case: if `destroy` is called when no robots exist, skip silently; the output reflects the counter correctly.\n6. Edge case: N = 1 with a single `create` prints `Robots alive: 1`.\n",
          "starter_code": "#include <iostream>\n#include <string>\n#include <vector>\n\nclass Robot {\npublic:\n    // TODO: declare inline static int s_count initialized to 0\n\n    Robot() {\n        // TODO: increment s_count\n    }\n\n    ~Robot() {\n        // TODO: decrement s_count\n    }\n};\n\nint main() {\n    int n{};\n    std::cin >> n;\n\n    // Use vector of pointers so we control lifetime explicitly\n    std::vector<Robot*> robots;\n\n    for (int i = 0; i < n; ++i) {\n        std::string op;\n        std::cin >> op;\n        if (op == \"create\") {\n            robots.push_back(new Robot{});\n        } else if (op == \"destroy\" && !robots.empty()) {\n            delete robots.back();\n            robots.pop_back();\n        }\n    }\n\n    std::cout << \"Robots alive: \" << Robot::s_count << '\\n';\n\n    // Cleanup remaining robots\n    for (Robot* r : robots) delete r;\n    return 0;\n}\n",
          "solution_code": "#include <iostream>\n#include <string>\n#include <vector>\n\nclass Robot {\npublic:\n    inline static int s_count = 0;\n\n    Robot() {\n        ++s_count;\n    }\n\n    ~Robot() {\n        --s_count;\n    }\n};\n\nint main() {\n    int n{};\n    std::cin >> n;\n\n    std::vector<Robot*> robots;\n\n    for (int i = 0; i < n; ++i) {\n        std::string op;\n        std::cin >> op;\n        if (op == \"create\") {\n            robots.push_back(new Robot{});\n        } else if (op == \"destroy\" && !robots.empty()) {\n            delete robots.back();\n            robots.pop_back();\n        }\n    }\n\n    std::cout << \"Robots alive: \" << Robot::s_count << '\\n';\n\n    for (Robot* r : robots) delete r;\n    return 0;\n}\n",
          "test_cases": [
            {
              "label": "Sample — 3 creates, 1 destroy",
              "stdin": "4\ncreate\ncreate\ncreate\ndestroy\n",
              "expected_stdout": "Robots alive: 2\n",
              "is_sample": true
            },
            {
              "label": "Hidden — all destroyed, counter reaches zero",
              "stdin": "6\ncreate\ncreate\ndestroy\ndestroy\ncreate\ndestroy\n",
              "expected_stdout": "Robots alive: 0\n",
              "is_sample": false
            },
            {
              "label": "Hidden — destroy on empty list is a no-op",
              "stdin": "3\ndestroy\ncreate\ncreate\n",
              "expected_stdout": "Robots alive: 2\n",
              "is_sample": false
            }
          ]
        },
        {
          "title": "Unique ID Generator with Static Counter",
          "prompt_md": "## Goal\n\nComplete the `Ticket` class so each constructed object receives a unique, monotonically-increasing integer ID starting from 1. Use a static member variable as the ID source. Then use the class in a program that reads ticket requests and reports each ticket's ID and the total ever issued.\n\n## Input\n\n- Line 1: integer N (1 ≤ N ≤ 50) — number of tickets to create.\n\n## Output\n\nPrint N lines, one per ticket in creation order:\n```\nTicket ID: X\n```\nThen print one final line:\n```\nTotal issued: N\n```\n\n## Requirements\n\n1. `Ticket` must have an `inline static int s_next_id = 1;` member.\n2. `Ticket` must have a non-static `int m_id` member set in the constructor.\n3. The constructor must assign `m_id = s_next_id` then increment `s_next_id`.\n4. Provide a `const` getter `int id() const` that returns `m_id`.\n5. After constructing all N tickets and printing their IDs, print total tickets issued using `Ticket::s_next_id - 1`.\n6. Edge case: N = 1 prints exactly `Ticket ID: 1` followed by `Total issued: 1`.\n7. Edge case: N = 50 — the last ticket has ID 50 and total is 50.\n",
          "starter_code": "#include <iostream>\n#include <vector>\n\nclass Ticket {\npublic:\n    // TODO: declare inline static int s_next_id initialized to 1\n    // TODO: declare int m_id\n\n    Ticket() {\n        // TODO: assign m_id from s_next_id, then increment s_next_id\n    }\n\n    // TODO: implement int id() const\n};\n\nint main() {\n    int n{};\n    std::cin >> n;\n\n    std::vector<Ticket> tickets;\n    tickets.reserve(static_cast<std::size_t>(n));\n\n    for (int i = 0; i < n; ++i) {\n        tickets.emplace_back();\n    }\n\n    for (const Ticket& t : tickets) {\n        std::cout << \"Ticket ID: \" << t.id() << '\\n';\n    }\n\n    std::cout << \"Total issued: \" << (Ticket::s_next_id - 1) << '\\n';\n    return 0;\n}\n",
          "solution_code": "#include <iostream>\n#include <vector>\n\nclass Ticket {\npublic:\n    inline static int s_next_id = 1;\n    int m_id{};\n\n    Ticket() {\n        m_id = s_next_id;\n        ++s_next_id;\n    }\n\n    int id() const { return m_id; }\n};\n\nint main() {\n    int n{};\n    std::cin >> n;\n\n    std::vector<Ticket> tickets;\n    tickets.reserve(static_cast<std::size_t>(n));\n\n    for (int i = 0; i < n; ++i) {\n        tickets.emplace_back();\n    }\n\n    for (const Ticket& t : tickets) {\n        std::cout << \"Ticket ID: \" << t.id() << '\\n';\n    }\n\n    std::cout << \"Total issued: \" << (Ticket::s_next_id - 1) << '\\n';\n    return 0;\n}\n",
          "test_cases": [
            {
              "label": "Sample — 3 tickets",
              "stdin": "3\n",
              "expected_stdout": "Ticket ID: 1\nTicket ID: 2\nTicket ID: 3\nTotal issued: 3\n",
              "is_sample": true
            },
            {
              "label": "Hidden — single ticket",
              "stdin": "1\n",
              "expected_stdout": "Ticket ID: 1\nTotal issued: 1\n",
              "is_sample": false
            },
            {
              "label": "Hidden — 10 tickets, IDs run 1-10",
              "stdin": "10\n",
              "expected_stdout": "Ticket ID: 1\nTicket ID: 2\nTicket ID: 3\nTicket ID: 4\nTicket ID: 5\nTicket ID: 6\nTicket ID: 7\nTicket ID: 8\nTicket ID: 9\nTicket ID: 10\nTotal issued: 10\n",
              "is_sample": false
            }
          ]
        }
      ]
    },
    {
      "number": "15.7",
      "title": "Static member functions",
      "sort_order": 207,
      "summary_md": "## Static Member Functions\n\nA **static member function** belongs to the class itself, not to any particular object. Like static member variables (covered in the previous lesson), static member functions exist independently of any instance.\n\n### Key Properties\n\n- No implicit `this` pointer — a static member function cannot access non-static data members or call non-static member functions directly.\n- Called via the class name (`ClassName::func()`) or, less idiomatically, through an object (`obj.func()`).\n- Can access private static data members of the same class.\n- Cannot be `const`, `virtual`, or `volatile` — those qualifiers are tied to instance semantics.\n\n### Why Use Them\n\n- **Accessing static data:** when you need to expose or manipulate a static member variable, a static member function is the clean interface.\n- **Factory functions:** construct and return objects with validated or computed arguments without exposing raw constructors.\n- **Utility helpers:** pure logic that conceptually belongs to the class but does not depend on object state (e.g., `Matrix::identity()`).\n\n### Example\n\n```cpp\n#include <iostream>\n\nclass Counter {\n    static int s_count;\npublic:\n    Counter()          { ++s_count; }\n    ~Counter()         { --s_count; }\n    static int count() { return s_count; }  // no 'this'\n};\nint Counter::s_count = 0;\n\nint main() {\n    Counter a, b;\n    std::cout << \"Live objects: \" << Counter::count() << '\\n'; // 2\n}\n```\n\n`Counter::count()` accesses `s_count` without any object — valid because both are static.\n\n### Relation to Earlier Lessons\n\nRecall from **The hidden `this` pointer** that every non-static member function receives a hidden pointer to the calling object. Static functions opt out of that mechanism entirely; they have no receiver and therefore no `this`. This also means they cannot be called through `this->someStaticFunc()` inside another member — just use the bare name or `ClassName::` prefix.\n\nFrom **Static member variables**, you know that static data must be defined exactly once in a translation unit. Static member functions do not need a separate out-of-class definition — only their declaration (inside the class) plus an ordinary implementation (inside or outside) suffice.\n\n### Common Pitfall\n\nAttempting to access a non-static member inside a static function produces a compile error:\n\n```cpp\nstatic void bad() { return m_value; } // error: no 'this'\n```\n\nIf you need both static and instance behaviour, split them into two functions.",
      "exercises": [
        {
          "title": "Instance Counter",
          "prompt_md": "## Goal\n\nImplement a class `Widget` that tracks how many `Widget` objects are currently alive using a static member variable and a static member function.\n\n## Requirements\n\n- `Widget` has a private static `int` named `s_count` initialised to `0`.\n- The default constructor increments `s_count`; the destructor decrements it.\n- A public static member function `count()` returns the current count.\n- `main` reads a single integer `n` from stdin, creates `n` `Widget` objects in a `std::vector`, prints the live count, then clears the vector and prints the count again.\n\n## Input\n\nA single integer `n` (1 ≤ n ≤ 100).\n\n## Output\n\nTwo lines:\n```\nAlive: <n>\nAlive: 0\n```",
          "starter_code": "#include <iostream>\n#include <vector>\n\nclass Widget {\n    // TODO: declare a private static int s_count\npublic:\n    Widget()  { /* TODO: increment s_count */ }\n    ~Widget() { /* TODO: decrement s_count */ }\n\n    // TODO: declare and implement static int count()\n};\n\n// TODO: define s_count outside the class\n\nint main() {\n    int n{};\n    std::cin >> n;\n\n    std::vector<Widget> widgets(n);\n    std::cout << \"Alive: \" << Widget::count() << '\\n';\n\n    widgets.clear();\n    std::cout << \"Alive: \" << Widget::count() << '\\n';\n\n    return 0;\n}\n",
          "solution_code": "#include <iostream>\n#include <vector>\n\nclass Widget {\n    static int s_count;\npublic:\n    Widget()  { ++s_count; }\n    ~Widget() { --s_count; }\n\n    static int count() { return s_count; }\n};\n\nint Widget::s_count = 0;\n\nint main() {\n    int n{};\n    std::cin >> n;\n\n    std::vector<Widget> widgets(n);\n    std::cout << \"Alive: \" << Widget::count() << '\\n';\n\n    widgets.clear();\n    std::cout << \"Alive: \" << Widget::count() << '\\n';\n\n    return 0;\n}\n",
          "test_cases": [
            {
              "label": "Three widgets",
              "stdin": "3",
              "expected_stdout": "Alive: 3\nAlive: 0\n",
              "is_sample": true
            },
            {
              "label": "One widget",
              "stdin": "1",
              "expected_stdout": "Alive: 1\nAlive: 0\n",
              "is_sample": false
            },
            {
              "label": "Ten widgets",
              "stdin": "10",
              "expected_stdout": "Alive: 10\nAlive: 0\n",
              "is_sample": false
            }
          ]
        },
        {
          "title": "Widget Factory Counter",
          "prompt_md": "## Goal\nComplete the `Widget` class so that a static member function tracks how many `Widget` objects have been created in total (including destroyed ones) and how many are currently alive.\n\n## Input\n- Line 1: integer N (1 ≤ N ≤ 20), the number of commands.\n- Each of the next N lines is one of:\n  - `create` — construct a new Widget and push it onto an internal list.\n  - `destroy` — destroy the most recently created Widget still alive (pop from the list). Guaranteed at least one Widget is alive when this command appears.\n  - `report` — print the current statistics (see Output).\n\n## Output\nFor each `report` command, print exactly one line in the format:\n```\ncreated=C alive=A\n```\nwhere `C` is the total number of Widgets ever constructed and `A` is the number currently alive. No extra blank lines.\n\n## Requirements\n1. `Widget` must have a `static int s_created` member that increments in the constructor and never decrements.\n2. `Widget` must have a `static int s_alive` member that increments in the constructor and decrements in the destructor.\n3. Provide `static int getCreated()` and `static int getAlive()` — both static member functions that return the respective static members.\n4. `main()` must call `Widget::getCreated()` and `Widget::getAlive()` (not any instance method) when handling `report`.\n5. The hidden tests include a `report` as the very first command (0 created, 0 alive) and a `report` immediately after all widgets are destroyed.",
          "starter_code": "#include <iostream>\n#include <string>\n#include <vector>\n\nclass Widget {\n    // TODO: declare static member variables s_created and s_alive\npublic:\n    Widget()  { /* TODO: update s_created and s_alive */ }\n    ~Widget() { /* TODO: update s_alive */ }\n\n    // TODO: declare and define static member function getCreated()\n    // TODO: declare and define static member function getAlive()\n};\n\n// TODO: define (initialize) s_created and s_alive outside the class\n\nint main() {\n    int n;\n    std::cin >> n;\n\n    std::vector<Widget*> widgets;\n\n    for (int i = 0; i < n; ++i) {\n        std::string cmd;\n        std::cin >> cmd;\n\n        if (cmd == \"create\") {\n            widgets.push_back(new Widget{});\n        } else if (cmd == \"destroy\") {\n            delete widgets.back();\n            widgets.pop_back();\n        } else { // report\n            // TODO: print using Widget::getCreated() and Widget::getAlive()\n        }\n    }\n\n    // clean up remaining widgets\n    for (Widget* w : widgets) delete w;\n    return 0;\n}\n",
          "solution_code": "#include <iostream>\n#include <string>\n#include <vector>\n\nclass Widget {\n    static int s_created;\n    static int s_alive;\npublic:\n    Widget()  { ++s_created; ++s_alive; }\n    ~Widget() { --s_alive; }\n\n    static int getCreated() { return s_created; }\n    static int getAlive()   { return s_alive; }\n};\n\nint Widget::s_created{ 0 };\nint Widget::s_alive{ 0 };\n\nint main() {\n    int n;\n    std::cin >> n;\n\n    std::vector<Widget*> widgets;\n\n    for (int i = 0; i < n; ++i) {\n        std::string cmd;\n        std::cin >> cmd;\n\n        if (cmd == \"create\") {\n            widgets.push_back(new Widget{});\n        } else if (cmd == \"destroy\") {\n            delete widgets.back();\n            widgets.pop_back();\n        } else {\n            std::cout << \"created=\" << Widget::getCreated()\n                      << \" alive=\" << Widget::getAlive() << \"\\n\";\n        }\n    }\n\n    for (Widget* w : widgets) delete w;\n    return 0;\n}\n",
          "test_cases": [
            {
              "label": "sample — mixed creates, destroy, and reports",
              "stdin": "7\ncreate\ncreate\nreport\ncreate\ndestroy\nreport\ndestroy\n",
              "expected_stdout": "created=2 alive=2\ncreated=3 alive=2\n",
              "is_sample": true
            },
            {
              "label": "hidden — report before any widget is created",
              "stdin": "4\nreport\ncreate\ncreate\nreport\n",
              "expected_stdout": "created=0 alive=0\ncreated=2 alive=2\n",
              "is_sample": false
            },
            {
              "label": "hidden — report after all widgets destroyed",
              "stdin": "8\ncreate\ncreate\ncreate\nreport\ndestroy\ndestroy\ndestroy\nreport\n",
              "expected_stdout": "created=3 alive=3\ncreated=3 alive=0\n",
              "is_sample": false
            }
          ]
        },
        {
          "title": "Temperature Factory",
          "prompt_md": "## Goal\n\nCombine static member functions (this lesson) with class templates (from **Class templates with member functions**) to build a `Temperature<T>` class whose instances can only be created through named factory functions.\n\n## Requirements\n\n- `Temperature<T>` stores a single `T m_celsius` value (private).\n- The constructor is **private**.\n- Two public static factory functions:\n  - `Temperature<T> fromCelsius(T c)` — wraps `c` directly.\n  - `Temperature<T> fromFahrenheit(T f)` — converts `f` to Celsius: `(f - 32) * 5 / 9`.\n- A public member function `T celsius() const` returns the stored value.\n- A public member function `T fahrenheit() const` returns the value converted back: `m_celsius * 9 / 5 + 32`.\n- `main` reads a mode character (`C` or `F`) and a value, constructs a `Temperature<double>` via the appropriate factory, then prints both representations on separate lines rounded to one decimal place.\n\n## Input\n\nA character (`C` or `F`) followed by a floating-point number.\n\n## Output\n\n```\nCelsius: <value>\nFahrenheit: <value>\n```\n\nAll values printed to exactly one decimal place.",
          "starter_code": "#include <iostream>\n#include <iomanip>\n\ntemplate<typename T>\nclass Temperature {\n    T m_celsius;\n\n    // TODO: make constructor private, storing celsius value\n    Temperature(T celsius) : m_celsius{ celsius } {}\n\npublic:\n    // TODO: implement static factory fromCelsius(T c)\n\n    // TODO: implement static factory fromFahrenheit(T f)\n    // Formula: celsius = (f - 32) * 5 / 9\n\n    T celsius()     const { /* TODO */ return T{}; }\n    T fahrenheit()  const { /* TODO: celsius * 9 / 5 + 32 */ return T{}; }\n};\n\nint main() {\n    char mode{};\n    double value{};\n    std::cin >> mode >> value;\n\n    // TODO: construct temp via the correct factory based on mode\n    // Then print Celsius and Fahrenheit to 1 decimal place\n\n    return 0;\n}\n",
          "solution_code": "#include <iostream>\n#include <iomanip>\n\ntemplate<typename T>\nclass Temperature {\n    T m_celsius;\n\n    explicit Temperature(T celsius) : m_celsius{ celsius } {}\n\npublic:\n    static Temperature<T> fromCelsius(T c) {\n        return Temperature<T>{ c };\n    }\n\n    static Temperature<T> fromFahrenheit(T f) {\n        return Temperature<T>{ (f - 32) * 5 / 9 };\n    }\n\n    T celsius()    const { return m_celsius; }\n    T fahrenheit() const { return m_celsius * 9 / 5 + 32; }\n};\n\nint main() {\n    char mode{};\n    double value{};\n    std::cin >> mode >> value;\n\n    Temperature<double> temp =\n        (mode == 'C') ? Temperature<double>::fromCelsius(value)\n                      : Temperature<double>::fromFahrenheit(value);\n\n    std::cout << std::fixed << std::setprecision(1);\n    std::cout << \"Celsius: \"    << temp.celsius()    << '\\n';\n    std::cout << \"Fahrenheit: \" << temp.fahrenheit() << '\\n';\n\n    return 0;\n}\n",
          "test_cases": [
            {
              "label": "From Celsius 100",
              "stdin": "C 100.0",
              "expected_stdout": "Celsius: 100.0\nFahrenheit: 212.0\n",
              "is_sample": true
            },
            {
              "label": "From Fahrenheit 32",
              "stdin": "F 32.0",
              "expected_stdout": "Celsius: 0.0\nFahrenheit: 32.0\n",
              "is_sample": false
            },
            {
              "label": "From Celsius 0",
              "stdin": "C 0.0",
              "expected_stdout": "Celsius: 0.0\nFahrenheit: 32.0\n",
              "is_sample": false
            }
          ]
        },
        {
          "title": "Temperature Log with Static Summary",
          "prompt_md": "## Goal\nComplete the `TempLog` class, which records individual temperature readings as non-static data and uses static members plus static member functions to maintain a running summary across all readings ever recorded.\n\n## Input\n- Line 1: integer N (1 ≤ N ≤ 50), the number of readings.\n- Each of the next N lines contains one floating-point temperature value (−100.0 ≤ T ≤ 100.0).\n\n## Output\nPrint exactly three lines:\n```\nCount: C\nSum: S\nAverage: A\n```\nwhere:\n- `C` is the total number of readings (integer).\n- `S` is the sum of all readings, printed with exactly 2 decimal places.\n- `A` is the average of all readings, printed with exactly 2 decimal places.\n\n## Requirements\n1. `TempLog` must store each individual temperature in a non-static `double m_temp` member set by its constructor.\n2. `TempLog` must have `static int s_count` and `static double s_sum` members; both are updated inside the constructor.\n3. Provide three static member functions: `getCount()` returning `int`, `getSum()` returning `double`, and `getAverage()` returning `double` (returns `s_sum / s_count`; guaranteed N ≥ 1 so no division by zero).\n4. `main()` must create one `TempLog` object per reading and call the three static member functions via the class-scope operator (`TempLog::getCount()`, etc.) to produce the output.\n5. The hidden tests include all identical values (verify average equals that value) and a mix of positive and negative readings whose sum is 0.",
          "starter_code": "#include <iomanip>\n#include <iostream>\n\nclass TempLog {\n    double m_temp;\n\n    // TODO: declare static members s_count and s_sum\npublic:\n    // TODO: define constructor that sets m_temp and updates s_count and s_sum\n    TempLog(double temp);\n\n    // TODO: declare and define static member functions getCount(), getSum(), getAverage()\n};\n\n// TODO: define (initialize) s_count and s_sum outside the class\n// TODO: define the constructor outside the class\n\nint main() {\n    int n;\n    std::cin >> n;\n\n    // TODO: read n temperatures, create one TempLog per reading\n\n    // TODO: print Count, Sum, Average using TempLog:: static member functions\n    std::cout << std::fixed << std::setprecision(2);\n    // ...\n\n    return 0;\n}\n",
          "solution_code": "#include <iomanip>\n#include <iostream>\n#include <vector>\n\nclass TempLog {\n    double m_temp;\n    static int    s_count;\n    static double s_sum;\npublic:\n    TempLog(double temp) : m_temp{ temp } {\n        ++s_count;\n        s_sum += m_temp;\n    }\n\n    static int    getCount()   { return s_count; }\n    static double getSum()     { return s_sum; }\n    static double getAverage() { return s_sum / s_count; }\n};\n\nint    TempLog::s_count{ 0 };\ndouble TempLog::s_sum{ 0.0 };\n\nint main() {\n    int n;\n    std::cin >> n;\n\n    std::vector<TempLog> logs;\n    logs.reserve(n);\n    for (int i = 0; i < n; ++i) {\n        double t;\n        std::cin >> t;\n        logs.emplace_back(t);\n    }\n\n    std::cout << std::fixed << std::setprecision(2);\n    std::cout << \"Count: \"   << TempLog::getCount()   << \"\\n\";\n    std::cout << \"Sum: \"     << TempLog::getSum()     << \"\\n\";\n    std::cout << \"Average: \" << TempLog::getAverage() << \"\\n\";\n\n    return 0;\n}\n",
          "test_cases": [
            {
              "label": "sample — mixed positive and negative readings",
              "stdin": "4\n10.0\n-3.5\n7.25\n2.0\n",
              "expected_stdout": "Count: 4\nSum: 15.75\nAverage: 3.94\n",
              "is_sample": true
            },
            {
              "label": "hidden — all identical values",
              "stdin": "3\n5.0\n5.0\n5.0\n",
              "expected_stdout": "Count: 3\nSum: 15.00\nAverage: 5.00\n",
              "is_sample": false
            },
            {
              "label": "hidden — positive and negative cancel to zero sum",
              "stdin": "4\n20.0\n-10.0\n-5.0\n-5.0\n",
              "expected_stdout": "Count: 4\nSum: 0.00\nAverage: 0.00\n",
              "is_sample": false
            }
          ]
        }
      ]
    },
    {
      "number": "15.8",
      "title": "Friend non-member functions",
      "sort_order": 208,
      "summary_md": "## Friend Non-Member Functions\n\nA **friend function** is a non-member function granted direct access to a class's private and protected members. The class controls which functions are friends via a `friend` declaration inside the class body — this is not a unilateral grant.\n\n### Why friend functions exist\n\nStrict encapsulation means outside code can only reach private members through public getters. But some operations are most naturally written as non-member functions — especially binary operators where neither operand \"owns\" the operation. Making the function a friend lets you write it as a non-member without forcing private data through a public accessor.\n\nRelated note: from **Static member functions**, you know that member functions don't receive a `this` pointer — friend non-member functions are similar in that they have no implicit object, but unlike static members they are not part of the class at all.\n\n### Declaring a friend\n\n```cpp\n#include <iostream>\n\nclass Vector2D {\n    float x{}, y{};\npublic:\n    Vector2D(float x, float y) : x{x}, y{y} {}\n    friend float dot(const Vector2D& a, const Vector2D& b);\n};\n\nfloat dot(const Vector2D& a, const Vector2D& b) {\n    return a.x * b.x + a.y * b.y;  // accesses private x, y directly\n}\n```\n\nThe `friend` keyword appears only in the in-class declaration, not on the definition outside the class. Access is per-function, not per-object: `dot` can read the private members of *any* `Vector2D` instance passed to it.\n\n### Common use case — operator overloading\n\n`operator<<` for `std::ostream` is the canonical example. Because the left operand is `std::ostream&` (which you cannot modify), the overload must be a non-member. Declaring it `friend` lets it print private fields without adding getters that expose implementation details.\n\n### Tradeoffs\n\n- **Encapsulation cost:** friend functions are tightly coupled to the class's internal representation. If private members are renamed or restructured, every friend definition must be updated.\n- **Minimal footprint:** prefer non-friend non-member functions (using only public interface) when possible. Reach for `friend` only when no public accessor exists *and* adding one would make the interface worse.\n- **Friend ≠ membership:** a friend function does not inherit access through derived classes and does not appear in the class's public interface — callers use it as an ordinary function.\n- Access from **The hidden `this` pointer**: friend functions receive objects explicitly; there is no hidden `this`.\n\n### When to use\n\nUse friend non-member functions for:\n- Binary operators whose natural left operand is not the class (`operator<<`, `operator>>`).\n- Symmetric binary math operators (`dot`, `cross`) where neither side \"owns\" the result.\n- Functions from a closely related class that needs deep access (e.g., a serializer).\n\nAvoid friend functions for ordinary queries — add a public accessor instead.",
      "exercises": [
        {
          "title": "Friend operator<< and operator+ for a Distance Class",
          "prompt_md": "## Goal\nComplete the `Distance` class by implementing two friend non-member functions: `operator+` and `operator<<`, so that two distances can be added and printed.\n\n## Input\n- Line 1: two space-separated non-negative integers representing meters and centimeters of the first distance (0 ≤ meters ≤ 1000, 0 ≤ centimeters ≤ 99)\n- Line 2: two space-separated non-negative integers representing meters and centimeters of the second distance (same ranges)\n\n## Output\n- Print exactly three lines:\n  1. `d1: X m Y cm` — the first distance\n  2. `d2: X m Y cm` — the second distance\n  3. `sum: X m Y cm` — the result of adding the two distances\n- Centimeters are normalized: every 100 cm carries over into 1 m. So if the total centimeters is 130, print 1 m more and 30 cm.\n- No trailing spaces. Each line ends with a newline.\n\n## Requirements\n1. `Distance` must store `m_meters` (int) and `m_centimeters` (int) as private members.\n2. Declare and define `friend std::ostream& operator<<(std::ostream& os, const Distance& d)` — it must print exactly `X m Y cm` with a single space around each token.\n3. Declare and define `friend Distance operator+(const Distance& a, const Distance& b)` — it must normalize the result so `m_centimeters` is in [0, 99].\n4. The constructor `Distance(int meters, int centimeters)` is provided; do not change it.\n5. Neither friend function may access a getter — they must read `m_meters` and `m_centimeters` directly (that is the point of friendship).\n6. Edge case: input where combined centimeters equal exactly 100 (carry of exactly 1 m, 0 cm remaining).",
          "starter_code": "#include <iostream>\n\nclass Distance {\n    int m_meters{};\n    int m_centimeters{};\n\npublic:\n    Distance(int meters, int centimeters)\n        : m_meters{meters}, m_centimeters{centimeters} {}\n\n    // TODO: declare friend operator<<\n    // TODO: declare friend operator+\n};\n\n// TODO: define operator<<\n// Should print: X m Y cm\n\n// TODO: define operator+\n// Should add both distances and normalize (every 100 cm -> 1 m)\n\nint main() {\n    int m1, c1, m2, c2;\n    std::cin >> m1 >> c1 >> m2 >> c2;\n\n    Distance d1{m1, c1};\n    Distance d2{m2, c2};\n    Distance sum = d1 + d2;\n\n    std::cout << \"d1: \"  << d1  << \"\\n\";\n    std::cout << \"d2: \"  << d2  << \"\\n\";\n    std::cout << \"sum: \" << sum << \"\\n\";\n\n    return 0;\n}\n",
          "solution_code": "#include <iostream>\n\nclass Distance {\n    int m_meters{};\n    int m_centimeters{};\n\npublic:\n    Distance(int meters, int centimeters)\n        : m_meters{meters}, m_centimeters{centimeters} {}\n\n    friend std::ostream& operator<<(std::ostream& os, const Distance& d);\n    friend Distance operator+(const Distance& a, const Distance& b);\n};\n\nstd::ostream& operator<<(std::ostream& os, const Distance& d) {\n    return os << d.m_meters << \" m \" << d.m_centimeters << \" cm\";\n}\n\nDistance operator+(const Distance& a, const Distance& b) {\n    int totalCm = a.m_centimeters + b.m_centimeters;\n    int totalM  = a.m_meters + b.m_meters + totalCm / 100;\n    totalCm %= 100;\n    return Distance{totalM, totalCm};\n}\n\nint main() {\n    int m1, c1, m2, c2;\n    std::cin >> m1 >> c1 >> m2 >> c2;\n\n    Distance d1{m1, c1};\n    Distance d2{m2, c2};\n    Distance sum = d1 + d2;\n\n    std::cout << \"d1: \"  << d1  << \"\\n\";\n    std::cout << \"d2: \"  << d2  << \"\\n\";\n    std::cout << \"sum: \" << sum << \"\\n\";\n\n    return 0;\n}\n",
          "test_cases": [
            {
              "label": "Sample — simple carry",
              "stdin": "1 70\n2 80\n",
              "expected_stdout": "d1: 1 m 70 cm\nd2: 2 m 80 cm\nsum: 4 m 50 cm\n",
              "is_sample": true
            },
            {
              "label": "Hidden — exact 100 cm carry",
              "stdin": "3 50\n0 50\n",
              "expected_stdout": "d1: 3 m 50 cm\nd2: 0 m 50 cm\nsum: 4 m 0 cm\n",
              "is_sample": false
            },
            {
              "label": "Hidden — no carry, zeros",
              "stdin": "0 0\n5 30\n",
              "expected_stdout": "d1: 0 m 0 cm\nd2: 5 m 30 cm\nsum: 5 m 30 cm\n",
              "is_sample": false
            }
          ]
        },
        {
          "title": "Box volume comparison with friend operator<<",
          "prompt_md": "## Goal\n\nImplement a `Box` class with private dimensions and a friend `operator<<` that prints the box in a fixed format. Also implement a friend non-member function `totalVolume` that sums the volumes of two boxes.\n\n## Input\n\nSix integers on one line: `l1 w1 h1 l2 w2 h2` representing the dimensions of two boxes.\n\n## Output\n\nTwo lines, each printing a box via `operator<<`, then a third line with the combined volume.\n\n```\nBox(l=2, w=3, h=4)\nBox(l=1, w=1, h=1)\nTotal volume: 25\n```\n\n## Requirements\n\n- `Box` must store `length`, `width`, `height` as **private** `int` members.\n- `operator<<` must be a **friend non-member** function — do not add any public getters.\n- `totalVolume(const Box&, const Box&)` must be a **friend non-member** function returning `int`.\n- Output format: `Box(l=L, w=W, h=H)` followed by a newline.\n- Compile cleanly with `g++ -std=c++20 -Wall -Wextra`.",
          "starter_code": "#include <iostream>\n\nclass Box {\n    int length{}, width{}, height{};\npublic:\n    Box(int l, int w, int h) : length{l}, width{w}, height{h} {}\n\n    // TODO: declare operator<< as a friend non-member\n    // TODO: declare totalVolume as a friend non-member\n};\n\n// TODO: define operator<<\n// Signature: std::ostream& operator<<(std::ostream& out, const Box& b)\n\n// TODO: define totalVolume\n// Signature: int totalVolume(const Box& a, const Box& b)\n\nint main() {\n    int l1, w1, h1, l2, w2, h2;\n    std::cin >> l1 >> w1 >> h1 >> l2 >> w2 >> h2;\n\n    Box b1{l1, w1, h1};\n    Box b2{l2, w2, h2};\n\n    std::cout << b1 << '\\n';\n    std::cout << b2 << '\\n';\n    std::cout << \"Total volume: \" << totalVolume(b1, b2) << '\\n';\n\n    return 0;\n}\n",
          "solution_code": "#include <iostream>\n\nclass Box {\n    int length{}, width{}, height{};\npublic:\n    Box(int l, int w, int h) : length{l}, width{w}, height{h} {}\n\n    friend std::ostream& operator<<(std::ostream& out, const Box& b);\n    friend int totalVolume(const Box& a, const Box& b);\n};\n\nstd::ostream& operator<<(std::ostream& out, const Box& b) {\n    out << \"Box(l=\" << b.length << \", w=\" << b.width << \", h=\" << b.height << ')';\n    return out;\n}\n\nint totalVolume(const Box& a, const Box& b) {\n    return (a.length * a.width * a.height) + (b.length * b.width * b.height);\n}\n\nint main() {\n    int l1, w1, h1, l2, w2, h2;\n    std::cin >> l1 >> w1 >> h1 >> l2 >> w2 >> h2;\n\n    Box b1{l1, w1, h1};\n    Box b2{l2, w2, h2};\n\n    std::cout << b1 << '\\n';\n    std::cout << b2 << '\\n';\n    std::cout << \"Total volume: \" << totalVolume(b1, b2) << '\\n';\n\n    return 0;\n}\n",
          "test_cases": [
            {
              "label": "Sample — basic two boxes",
              "stdin": "2 3 4 1 1 1",
              "expected_stdout": "Box(l=2, w=3, h=4)\nBox(l=1, w=1, h=1)\nTotal volume: 25\n",
              "is_sample": true
            },
            {
              "label": "Hidden — unit cube doubled",
              "stdin": "1 1 1 1 1 1",
              "expected_stdout": "Box(l=1, w=1, h=1)\nBox(l=1, w=1, h=1)\nTotal volume: 2\n",
              "is_sample": false
            },
            {
              "label": "Hidden — larger dimensions",
              "stdin": "5 6 7 2 3 4",
              "expected_stdout": "Box(l=5, w=6, h=7)\nBox(l=2, w=3, h=4)\nTotal volume: 234\n",
              "is_sample": false
            }
          ]
        },
        {
          "title": "Friend operator== and operator<< for a Rectangle Class",
          "prompt_md": "## Goal\nComplete the `Rectangle` class by implementing three friend non-member functions — `operator<<`, `operator==`, and `operator!=` — and use them inside `main` to compare two rectangles read from stdin.\n\n## Input\n- Line 1: two space-separated positive integers `W H` — width and height of the first rectangle (1 ≤ W, H ≤ 500)\n- Line 2: two space-separated positive integers `W H` — width and height of the second rectangle (same ranges)\n\n## Output\nPrint exactly four lines:\n1. `r1: W x H` — first rectangle\n2. `r2: W x H` — second rectangle\n3. `area r1: A` — area of the first rectangle (integer)\n4. Either `r1 == r2` if the rectangles are equal, or `r1 != r2` if they are not\n\nTwo rectangles are **equal** when they have the same width **and** the same height (order matters — 3×5 ≠ 5×3).\n\n## Requirements\n1. `Rectangle` must store `m_width` (int) and `m_height` (int) as private members.\n2. Provide a constructor `Rectangle(int width, int height)` and a private member function `area() const` that returns `m_width * m_height`.\n3. Declare and define `friend std::ostream& operator<<(std::ostream& os, const Rectangle& r)` — prints `W x H` (space-x-space between the two numbers).\n4. Declare and define `friend bool operator==(const Rectangle& a, const Rectangle& b)` — returns true when both dimensions match.\n5. Declare and define `friend bool operator!=(const Rectangle& a, const Rectangle& b)` — must be implemented by calling `operator==` (i.e. `return !(a == b);`).\n6. The line `area r1: A` must call the private `area()` member function from within `operator<<` — to do so, `operator<<` must be a friend.\n7. Edge cases: equal rectangles (same W and H), swapped dimensions (W and H swapped between r1 and r2).",
          "starter_code": "#include <iostream>\n\nclass Rectangle {\n    int m_width{};\n    int m_height{};\n\n    // TODO: declare private member function area() const\n\npublic:\n    Rectangle(int width, int height)\n        : m_width{width}, m_height{height} {}\n\n    // TODO: declare friend operator<<\n    // TODO: declare friend operator==\n    // TODO: declare friend operator!=\n};\n\n// TODO: define operator<<\n// Prints: W x H  (the area() call is only used in main via a separate cout,\n// but operator<< must be a friend to access m_width and m_height directly)\n\n// TODO: define operator==\n\n// TODO: define operator!= (must call operator==)\n\nint main() {\n    int w1, h1, w2, h2;\n    std::cin >> w1 >> h1 >> w2 >> h2;\n\n    Rectangle r1{w1, h1};\n    Rectangle r2{w2, h2};\n\n    std::cout << \"r1: \"     << r1 << \"\\n\";\n    std::cout << \"r2: \"     << r2 << \"\\n\";\n    // TODO: print \"area r1: \" followed by r1's area\n    // Hint: area() is private — call it through a friend function or\n    // expose it only to operator<< ... but for printing here you need\n    // another approach. Add a second friend: make a free function\n    // getArea(const Rectangle&) a friend, OR just print inside a\n    // dedicated friend. The simplest path: add getArea as a friend.\n\n    if (r1 == r2)\n        std::cout << \"r1 == r2\\n\";\n    else\n        std::cout << \"r1 != r2\\n\";\n\n    return 0;\n}\n",
          "solution_code": "#include <iostream>\n\nclass Rectangle {\n    int m_width{};\n    int m_height{};\n\n    int area() const { return m_width * m_height; }\n\npublic:\n    Rectangle(int width, int height)\n        : m_width{width}, m_height{height} {}\n\n    friend std::ostream& operator<<(std::ostream& os, const Rectangle& r);\n    friend bool operator==(const Rectangle& a, const Rectangle& b);\n    friend bool operator!=(const Rectangle& a, const Rectangle& b);\n    friend int getArea(const Rectangle& r);\n};\n\nstd::ostream& operator<<(std::ostream& os, const Rectangle& r) {\n    return os << r.m_width << \" x \" << r.m_height;\n}\n\nbool operator==(const Rectangle& a, const Rectangle& b) {\n    return a.m_width == b.m_width && a.m_height == b.m_height;\n}\n\nbool operator!=(const Rectangle& a, const Rectangle& b) {\n    return !(a == b);\n}\n\nint getArea(const Rectangle& r) {\n    return r.area();\n}\n\nint main() {\n    int w1, h1, w2, h2;\n    std::cin >> w1 >> h1 >> w2 >> h2;\n\n    Rectangle r1{w1, h1};\n    Rectangle r2{w2, h2};\n\n    std::cout << \"r1: \"     << r1 << \"\\n\";\n    std::cout << \"r2: \"     << r2 << \"\\n\";\n    std::cout << \"area r1: \" << getArea(r1) << \"\\n\";\n\n    if (r1 == r2)\n        std::cout << \"r1 == r2\\n\";\n    else\n        std::cout << \"r1 != r2\\n\";\n\n    return 0;\n}\n",
          "test_cases": [
            {
              "label": "Sample — unequal rectangles",
              "stdin": "4 6\n3 8\n",
              "expected_stdout": "r1: 4 x 6\nr2: 3 x 8\narea r1: 24\nr1 != r2\n",
              "is_sample": true
            },
            {
              "label": "Hidden — equal rectangles",
              "stdin": "7 5\n7 5\n",
              "expected_stdout": "r1: 7 x 5\nr2: 7 x 5\narea r1: 35\nr1 == r2\n",
              "is_sample": false
            },
            {
              "label": "Hidden — swapped dimensions",
              "stdin": "10 3\n3 10\n",
              "expected_stdout": "r1: 10 x 3\nr2: 3 x 10\narea r1: 30\nr1 != r2\n",
              "is_sample": false
            }
          ]
        },
        {
          "title": "Temperature class with friend operator<< and static conversion helper",
          "prompt_md": "## Goal\n\nCombine friend non-member functions (this lesson) with static member functions (from **Static member functions**) to build a `Temperature` class.\n\n## Input\n\nTwo lines. Each line has a number and a unit letter (`C` for Celsius, `F` for Fahrenheit). Example:\n```\n100 C\n32 F\n```\n\n## Output\n\nPrint each temperature using `operator<<` in the format `T°C` or `T°F` (with the degree sign `\\u00b0` — use `\\u00b0` in source or just the character `°`). Then print which is hotter, or \"Equal\" if they are the same in Celsius. Comparison is done in Celsius rounded to one decimal place for display, but use the raw double for comparison.\n\n```\n100°C\n32°F\nHotter: 100°C\n```\n\nIf the second is hotter:\n```\n0°C\n100°F\nHotter: 100°F\n```\n\nIf equal:\n```\n0°C\n32°F\nEqual\n```\n\n## Requirements\n\n- `Temperature` stores value as `double` and unit as `char` — both **private**.\n- `static double toCelsius(double val, char unit)` — public static member function (converts F to C; returns C unchanged).\n- `operator<<` is a **friend non-member** function; do not add public getters.\n- `hotter(const Temperature&, const Temperature&)` is a **friend non-member** function that returns a `const Temperature*` pointing to the hotter one, or `nullptr` if equal. Use `toCelsius` inside it via the class name.\n- Degree sign: use the UTF-8 character `°` directly in your string literals.\n- Compile cleanly with `g++ -std=c++20 -Wall -Wextra`.",
          "starter_code": "#include <iostream>\n#include <string>\n\nclass Temperature {\n    double value{};\n    char unit{};\npublic:\n    Temperature(double v, char u) : value{v}, unit{u} {}\n\n    // TODO: declare static toCelsius(double val, char unit) -> double\n\n    // TODO: declare friend operator<<\n    // TODO: declare friend hotter(const Temperature&, const Temperature&) -> const Temperature*\n};\n\n// TODO: define static Temperature::toCelsius\n\n// TODO: define operator<<\n// Format: value followed by degree sign followed by unit, e.g. 100°C\n\n// TODO: define hotter — returns pointer to hotter Temperature, nullptr if equal\n\nint main() {\n    double v1, v2;\n    char u1, u2;\n    std::cin >> v1 >> u1 >> v2 >> u2;\n\n    Temperature t1{v1, u1};\n    Temperature t2{v2, u2};\n\n    std::cout << t1 << '\\n';\n    std::cout << t2 << '\\n';\n\n    const Temperature* h = hotter(t1, t2);\n    if (h == nullptr)\n        std::cout << \"Equal\\n\";\n    else\n        std::cout << \"Hotter: \" << *h << '\\n';\n\n    return 0;\n}\n",
          "solution_code": "#include <iostream>\n\nclass Temperature {\n    double value{};\n    char unit{};\npublic:\n    Temperature(double v, char u) : value{v}, unit{u} {}\n\n    static double toCelsius(double val, char u) {\n        if (u == 'F')\n            return (val - 32.0) * 5.0 / 9.0;\n        return val;\n    }\n\n    friend std::ostream& operator<<(std::ostream& out, const Temperature& t);\n    friend const Temperature* hotter(const Temperature& a, const Temperature& b);\n};\n\nstd::ostream& operator<<(std::ostream& out, const Temperature& t) {\n    out << t.value << \"\\u00b0\" << t.unit;\n    return out;\n}\n\nconst Temperature* hotter(const Temperature& a, const Temperature& b) {\n    double ca = Temperature::toCelsius(a.value, a.unit);\n    double cb = Temperature::toCelsius(b.value, b.unit);\n    if (ca > cb) return &a;\n    if (cb > ca) return &b;\n    return nullptr;\n}\n\nint main() {\n    double v1, v2;\n    char u1, u2;\n    std::cin >> v1 >> u1 >> v2 >> u2;\n\n    Temperature t1{v1, u1};\n    Temperature t2{v2, u2};\n\n    std::cout << t1 << '\\n';\n    std::cout << t2 << '\\n';\n\n    const Temperature* h = hotter(t1, t2);\n    if (h == nullptr)\n        std::cout << \"Equal\\n\";\n    else\n        std::cout << \"Hotter: \" << *h << '\\n';\n\n    return 0;\n}\n",
          "test_cases": [
            {
              "label": "Sample — boiling vs freezing",
              "stdin": "100 C\n32 F",
              "expected_stdout": "100°C\n32°F\nHotter: 100°C\n",
              "is_sample": true
            },
            {
              "label": "Hidden — Fahrenheit hotter",
              "stdin": "0 C\n100 F",
              "expected_stdout": "0°C\n100°F\nHotter: 100°F\n",
              "is_sample": false
            },
            {
              "label": "Hidden — equal temperatures",
              "stdin": "0 C\n32 F",
              "expected_stdout": "0°C\n32°F\nEqual\n",
              "is_sample": false
            }
          ]
        }
      ]
    },
    {
      "number": "15.9",
      "title": "Friend classes and friend member functions",
      "sort_order": 209,
      "summary_md": "## Friend Classes and Friend Member Functions\n\nNormally a class's `private` and `protected` members are inaccessible from outside the class. The `friend` keyword grants a specific external class or function direct access to those members — a controlled breach of the encapsulation boundary.\n\n### Friend non-member functions (recap)\n\nAs covered in *Friend non-member functions*, a single free function can be granted friendship:\n\n```cpp\nclass Box {\n    double volume{};\npublic:\n    explicit Box(double v) : volume{v} {}\n    friend void print(const Box& b); // grants access to volume\n};\n\nvoid print(const Box& b) { std::cout << b.volume << '\\n'; }\n```\n\n### Friend classes\n\nA whole class can be named as a friend. Every member function of that class then has access to the granting class's private members.\n\n```cpp\nclass Engine;\n\nclass Car {\n    int horsepower{150};\n    friend class Engine;   // Engine can read horsepower\n};\n\nclass Engine {\npublic:\n    void tune(Car& c) { c.horsepower += 50; } // legal\n};\n```\n\nThe forward declaration `class Engine;` before `Car` is required because `Car` references `Engine` before `Engine` is defined. Forward declarations for friend classes are a common pattern; see *Classes and header files* for how to organise this across translation units.\n\n### Friend member functions\n\nGranting friendship to an entire class is coarser than necessary. You can instead name a single member function:\n\n```cpp\n// Full definition of Engine must appear before this friend declaration\nfriend void Engine::tune(Car&);\n```\n\nThis requires `Engine` to be fully defined before `Car` reaches that `friend` line, which sometimes forces a specific ordering of class definitions in the header.\n\n### Key rules\n\n- Friendship is **not** inherited — a class derived from a friend is not itself a friend.\n- Friendship is **not** transitive — a friend of a friend is not a friend.\n- Friendship is **one-way** — if `A` declares `B` a friend, `B` does not automatically gain `A` as a friend.\n- A class controls its own friend list; nothing outside the class can grant friendship.\n\n### When to use friends\n\nFriendship is appropriate for tightly-coupled pairs (e.g. a container and its iterator, or a class and a serialiser) where the alternative would be adding public getters solely to serve one specific collaborator. Prefer friend member functions over friend classes when only one method needs access — it keeps the encapsulation boundary as narrow as possible.",
      "exercises": [
        {
          "title": "Rectangle and Inspector",
          "prompt_md": "## Goal\n\nPractice declaring a **friend class** and using it to access private data.\n\n## Requirements\n\n- Define a class `Rectangle` with private `int` members `width` and `height`, and a constructor that takes `width` then `height`.\n- Define a class `Inspector` that is declared a friend of `Rectangle`.\n- `Inspector` must have a member function `describe(const Rectangle& r)` that prints the rectangle's dimensions and area on one line in the exact format shown below.\n- `main` reads two integers from stdin (width, height), constructs a `Rectangle`, constructs an `Inspector`, and calls `describe`.\n\n## Input\n\nTwo space-separated integers on one line: `width height`\n\n## Output\n\nOne line:\n```\nRectangle: 6x4, area=24\n```\n(using the actual values from input)\n\n## Notes\n\n- `Inspector::describe` must access `width` and `height` directly — do **not** add public getters to `Rectangle`.\n- Both classes must be defined in the same translation unit (single `.cpp` file).",
          "starter_code": "#include <iostream>\n\n// TODO: forward-declare Inspector if needed\n\nclass Rectangle {\n    // TODO: private int width, height\n    // TODO: declare Inspector as friend\npublic:\n    // TODO: constructor(int w, int h)\n};\n\nclass Inspector {\npublic:\n    // TODO: void describe(const Rectangle& r)\n    //       Print: \"Rectangle: WxH, area=A\\n\"\n};\n\nint main() {\n    int w{}, h{};\n    std::cin >> w >> h;\n    // TODO: construct Rectangle and Inspector, call describe\n    return 0;\n}\n",
          "solution_code": "#include <iostream>\n\nclass Rectangle {\n    int width{};\n    int height{};\n    friend class Inspector;\npublic:\n    Rectangle(int w, int h) : width{w}, height{h} {}\n};\n\nclass Inspector {\npublic:\n    void describe(const Rectangle& r) {\n        std::cout << \"Rectangle: \" << r.width << 'x' << r.height\n                  << \", area=\" << r.width * r.height << '\\n';\n    }\n};\n\nint main() {\n    int w{}, h{};\n    std::cin >> w >> h;\n    Rectangle rect{w, h};\n    Inspector insp;\n    insp.describe(rect);\n    return 0;\n}\n",
          "test_cases": [
            {
              "label": "Sample: 6x4",
              "stdin": "6 4",
              "expected_stdout": "Rectangle: 6x4, area=24\n",
              "is_sample": true
            },
            {
              "label": "Hidden: 10x3",
              "stdin": "10 3",
              "expected_stdout": "Rectangle: 10x3, area=30\n",
              "is_sample": false
            },
            {
              "label": "Hidden: 1x1",
              "stdin": "1 1",
              "expected_stdout": "Rectangle: 1x1, area=1\n",
              "is_sample": false
            }
          ]
        },
        {
          "title": "BankAccount and Auditor with Static Totals",
          "prompt_md": "## Goal\n\nCombine **friend member functions** with **static member variables** (from *Static member variables*) to build a simple audit system.\n\n## Requirements\n\n- Define a class `BankAccount` with:\n  - A private `double balance` (set by constructor).\n  - A private `static double s_totalDeposited` tracking the sum of all deposits ever made across all accounts.\n  - A public `void deposit(double amount)` that adds `amount` to `balance` and to `s_totalDeposited`.\n  - A **friend member function** declaration: `friend void Auditor::report(const BankAccount&)`.\n- Define a class `Auditor` (fully defined **before** `BankAccount` so the friend declaration is valid — use a forward declaration of `BankAccount` inside `Auditor` if needed) with:\n  - A public member function `void report(const BankAccount& acct)` that prints one line in the format shown below, accessing `balance` and `s_totalDeposited` directly.\n- `main` reads a number of operations. Each operation is one of:\n  - `D amount` — deposit `amount` into the current account.\n  - `R` — call `auditor.report(account)`.\n- There is exactly one `BankAccount` (initial balance `0.0`) and one `Auditor`.\n\n## Input\n\nFirst line: integer `N` (number of operations).\nNext `N` lines: each is `D <amount>` or `R`.\n\n## Output\n\nFor each `R` operation, one line:\n```\nBalance: 50.00, TotalDeposited: 75.00\n```\n(two decimal places, using actual values)\n\n## Notes\n\n- Use `std::fixed` and `std::setprecision(2)` for output.\n- `s_totalDeposited` must be a static member of `BankAccount`, not a global variable.\n- `Auditor::report` must access both `balance` and `s_totalDeposited` directly without public getters.",
          "starter_code": "#include <iostream>\n#include <iomanip>\n#include <string>\n\n// TODO: forward-declare BankAccount so Auditor can reference it\n\nclass Auditor {\npublic:\n    // TODO: void report(const BankAccount& acct)\n    //       Print: \"Balance: X.XX, TotalDeposited: Y.YY\\n\"\n};\n\nclass BankAccount {\n    // TODO: double balance\n    // TODO: static double s_totalDeposited\n    // TODO: declare Auditor::report as friend\npublic:\n    // TODO: constructor(double initial = 0.0)\n    // TODO: void deposit(double amount)\n};\n\n// TODO: define BankAccount::s_totalDeposited\n\nint main() {\n    int n{};\n    std::cin >> n;\n    BankAccount account;\n    Auditor auditor;\n    for (int i = 0; i < n; ++i) {\n        std::string op;\n        std::cin >> op;\n        if (op == \"D\") {\n            double amt{};\n            std::cin >> amt;\n            // TODO: account.deposit(amt);\n        } else if (op == \"R\") {\n            // TODO: auditor.report(account);\n        }\n    }\n    return 0;\n}\n",
          "solution_code": "#include <iostream>\n#include <iomanip>\n#include <string>\n\nclass BankAccount; // forward declaration for Auditor\n\nclass Auditor {\npublic:\n    void report(const BankAccount& acct);\n};\n\nclass BankAccount {\n    double balance{};\n    static double s_totalDeposited;\n    friend void Auditor::report(const BankAccount&);\npublic:\n    explicit BankAccount(double initial = 0.0) : balance{initial} {}\n    void deposit(double amount) {\n        balance += amount;\n        s_totalDeposited += amount;\n    }\n};\n\ndouble BankAccount::s_totalDeposited = 0.0;\n\nvoid Auditor::report(const BankAccount& acct) {\n    std::cout << std::fixed << std::setprecision(2)\n              << \"Balance: \" << acct.balance\n              << \", TotalDeposited: \" << BankAccount::s_totalDeposited << '\\n';\n}\n\nint main() {\n    int n{};\n    std::cin >> n;\n    BankAccount account;\n    Auditor auditor;\n    for (int i = 0; i < n; ++i) {\n        std::string op;\n        std::cin >> op;\n        if (op == \"D\") {\n            double amt{};\n            std::cin >> amt;\n            account.deposit(amt);\n        } else if (op == \"R\") {\n            auditor.report(account);\n        }\n    }\n    return 0;\n}\n",
          "test_cases": [
            {
              "label": "Sample: two deposits then report",
              "stdin": "4\nD 50.00\nD 25.00\nR\nR",
              "expected_stdout": "Balance: 75.00, TotalDeposited: 75.00\nBalance: 75.00, TotalDeposited: 75.00\n",
              "is_sample": true
            },
            {
              "label": "Hidden: single deposit report",
              "stdin": "2\nD 100.00\nR",
              "expected_stdout": "Balance: 100.00, TotalDeposited: 100.00\n",
              "is_sample": false
            },
            {
              "label": "Hidden: interleaved deposits and reports",
              "stdin": "5\nD 10.00\nR\nD 5.50\nD 4.50\nR",
              "expected_stdout": "Balance: 10.00, TotalDeposited: 10.00\nBalance: 20.00, TotalDeposited: 20.00\n",
              "is_sample": false
            }
          ]
        }
      ]
    },
    {
      "number": "15.10",
      "title": "Ref qualifiers",
      "sort_order": 210,
      "summary_md": "## Ref qualifiers\n\nRef qualifiers let you overload a member function based on whether the object it is called on is an lvalue or an rvalue. They work the same way `const` qualifiers work, except they control *value category* rather than mutability.\n\nYou append `&` or `&&` after the parameter list (and after `const`, if present) to restrict or distinguish the call:\n\n- `void f() &` — callable only on lvalue objects\n- `void f() &&` — callable only on rvalue objects\n- Providing both overloads lets the compiler pick the right one automatically\n\n```cpp\n#include <iostream>\n#include <string>\n\nstruct Buffer {\n    std::string data;\n\n    // Safe to return a reference when *this is an lvalue\n    const std::string& get() const & { return data; }\n\n    // Move the data out when *this is a temporary\n    std::string get() && { return std::move(data); }\n};\n\nBuffer make() { return Buffer{\"hello\"}; }\n\nint main() {\n    Buffer b{\"world\"};\n    std::cout << b.get() << '\\n';       // calls const &\n    std::cout << make().get() << '\\n';  // calls &&\n}\n```\n\n### Why this matters\n\nWithout ref qualifiers, a member returning `const T&` on a temporary is dangerous: the caller may bind the returned reference to a local variable and the temporary will be destroyed before the reference is used. The `&&` overload sidesteps this by moving the value out instead.\n\nThis concept connects directly to the move semantics preview from **Static member variables** and the resource-ownership ideas discussed in **Introduction to destructors**. Ref qualifiers are how the standard library implements methods like `std::optional::value()`, which behaves differently when the optional is a temporary.\n\n### Key rules\n\n- You cannot mix ref-qualified and unqualified overloads of the same function — all overloads must be qualified or none of them.\n- `const &` matches both `const` lvalues and, as a fallback, rvalues (when no `&&` overload is present).\n- Ref qualifiers apply to `*this`, not to the function parameters.\n- They have no runtime cost — selection is entirely at compile time.\n",
      "exercises": [
        {
          "title": "Ref-qualified getter: safe reference vs. moved value",
          "prompt_md": "## Goal\n\nPractice writing a pair of ref-qualified overloads so that the same method name behaves safely for both lvalues and rvalues.\n\n## Task\n\nComplete the `Token` class below. It holds a single `std::string` called `text`.\n\nAdd two overloads of `value()`:\n\n- `const std::string& value() const &` — returns a const reference to `text`.\n- `std::string value() &&` — returns `text` by moving it out of the object.\n\n## Input\n\nNo stdin. The `main` function is fixed and calls both overloads.\n\n## Output\n\nExactly two lines, each containing the token text:\n\n```\nhello\nworld\n```\n\n## Requirements\n\n- Both overloads must be present and use the correct ref qualifiers.\n- The `&&` overload must use `std::move`.\n- Do not change `main`.\n",
          "starter_code": "#include <iostream>\n#include <string>\n#include <utility>\n\nclass Token {\npublic:\n    explicit Token(std::string t) : text{std::move(t)} {}\n\n    // TODO: add const lvalue ref-qualified overload returning const std::string&\n\n    // TODO: add rvalue ref-qualified overload returning std::string by move\n\nprivate:\n    std::string text;\n};\n\nToken makeToken(std::string s) {\n    return Token{std::move(s)};\n}\n\nint main() {\n    Token t{\"hello\"};\n    std::cout << t.value() << '\\n';             // should call const & overload\n    std::cout << makeToken(\"world\").value() << '\\n'; // should call && overload\n    return 0;\n}\n",
          "solution_code": "#include <iostream>\n#include <string>\n#include <utility>\n\nclass Token {\npublic:\n    explicit Token(std::string t) : text{std::move(t)} {}\n\n    const std::string& value() const & { return text; }\n\n    std::string value() && { return std::move(text); }\n\nprivate:\n    std::string text;\n};\n\nToken makeToken(std::string s) {\n    return Token{std::move(s)};\n}\n\nint main() {\n    Token t{\"hello\"};\n    std::cout << t.value() << '\\n';\n    std::cout << makeToken(\"world\").value() << '\\n';\n    return 0;\n}\n",
          "test_cases": [
            {
              "label": "sample: basic lvalue and rvalue",
              "stdin": "",
              "expected_stdout": "hello\nworld\n",
              "is_sample": true
            },
            {
              "label": "hidden: verifies output line count",
              "stdin": "",
              "expected_stdout": "hello\nworld\n",
              "is_sample": false
            },
            {
              "label": "hidden: exact match including newline",
              "stdin": "",
              "expected_stdout": "hello\nworld\n",
              "is_sample": false
            }
          ]
        },
        {
          "title": "Builder with ref-qualified build(): lvalue copies, rvalue moves",
          "prompt_md": "## Goal\n\nCombine ref qualifiers with the idea of a simple builder class (using a constructor and member data, as seen in **The hidden `this` pointer**) to produce a class that is cheap to consume when temporary and safe to reuse when an lvalue.\n\n## Task\n\nComplete the `QueryBuilder` class that accumulates a query string.\n\n- `QueryBuilder& append(std::string_view part)` — appends `part` followed by a space to the internal buffer. Returns `*this` to allow chaining.\n- `std::string build() const &` — returns a copy of the accumulated string (trims the trailing space).\n- `std::string build() &&` — moves the accumulated string out and returns it (also trims the trailing space).\n\nTo trim the trailing space: if the string is non-empty and ends with `' '`, call `.pop_back()` before returning.\n\n## Input\n\nRead lines from stdin until EOF. Each line is appended to the builder. Then call `build()` and print the result, followed by a newline.\n\n## Output\n\nA single line: all input tokens joined by single spaces.\n\n### Example\n\n**Input:**\n```\nSELECT *\nFROM users\nWHERE id = 1\n```\n\n**Output:**\n```\nSELECT * FROM users WHERE id = 1\n```\n\n## Requirements\n\n- Both `build()` overloads must carry correct ref qualifiers.\n- The `&&` overload must use `std::move` on the internal buffer.\n- `append` must return `*this` by reference.\n- Compile cleanly with `-std=c++20 -Wall -Wextra`.\n",
          "starter_code": "#include <iostream>\n#include <string>\n#include <string_view>\n#include <utility>\n\nclass QueryBuilder {\npublic:\n    QueryBuilder& append(std::string_view part) {\n        // TODO: append part then a space to buf\n        return *this;\n    }\n\n    // TODO: const lvalue ref-qualified build() — return a trimmed copy\n\n    // TODO: rvalue ref-qualified build() — move buf out and trim\n\nprivate:\n    std::string buf;\n};\n\nint main() {\n    QueryBuilder qb;\n    std::string line;\n    while (std::getline(std::cin, line)) {\n        qb.append(line);\n    }\n    // calls the const & overload because qb is an lvalue\n    std::cout << qb.build() << '\\n';\n    return 0;\n}\n",
          "solution_code": "#include <iostream>\n#include <string>\n#include <string_view>\n#include <utility>\n\nclass QueryBuilder {\npublic:\n    QueryBuilder& append(std::string_view part) {\n        buf += part;\n        buf += ' ';\n        return *this;\n    }\n\n    std::string build() const & {\n        std::string result = buf;\n        if (!result.empty() && result.back() == ' ')\n            result.pop_back();\n        return result;\n    }\n\n    std::string build() && {\n        if (!buf.empty() && buf.back() == ' ')\n            buf.pop_back();\n        return std::move(buf);\n    }\n\nprivate:\n    std::string buf;\n};\n\nint main() {\n    QueryBuilder qb;\n    std::string line;\n    while (std::getline(std::cin, line)) {\n        qb.append(line);\n    }\n    std::cout << qb.build() << '\\n';\n    return 0;\n}\n",
          "test_cases": [
            {
              "label": "sample: three-part query",
              "stdin": "SELECT *\nFROM users\nWHERE id = 1",
              "expected_stdout": "SELECT * FROM users WHERE id = 1\n",
              "is_sample": true
            },
            {
              "label": "hidden: single token",
              "stdin": "hello",
              "expected_stdout": "hello\n",
              "is_sample": false
            },
            {
              "label": "hidden: five tokens",
              "stdin": "INSERT INTO orders\nVALUES\n1\n2\n3",
              "expected_stdout": "INSERT INTO orders VALUES 1 2 3\n",
              "is_sample": false
            }
          ]
        }
      ]
    },
    {
      "number": "15.x",
      "title": "Chapter 15 summary and quiz",
      "sort_order": 211,
      "summary_md": "## Chapter 15 Summary: More on Classes\n\nThis chapter covered a set of class features that together give you precise control over object lifetime, access, sharing, and interface design.\n\n**The hidden `this` pointer and method chaining**\nEvery non-static member function receives an implicit `this` pointer to the calling object. Returning `*this` by reference from a member function enables method chaining — calling multiple member functions on the same object in one expression.\n\n**Classes and header files**\nClass definitions go in `.h` files; member function definitions go in `.cpp` files. Inline definitions inside the class body are implicitly `inline`. This separation keeps compile times manageable and lets multiple translation units share the same class definition.\n\n**Nested types (member types)**\nA type declared inside a class is scoped to that class (`MyClass::NestedEnum`). Use nested types when the type is only meaningful in the context of the enclosing class and you want to avoid polluting the enclosing namespace.\n\n**Destructors**\nA destructor (`~ClassName()`) runs automatically when an object goes out of scope or is deleted. It takes no parameters and has no return type. Use it to release resources: close file handles, free heap memory, or decrement reference counts. RAII depends on destructors running reliably.\n\n**Class templates with member functions**\nMember function definitions for a class template must repeat the template parameter list. The compiler instantiates a separate version for each type used. Keep template definitions in headers so the compiler can see them at the point of use.\n\n**Static member variables and functions**\nA `static` data member belongs to the class, not to any instance. It must be defined exactly once outside the class body (unless `inline` or `constexpr`). A `static` member function has no `this` pointer and can only access static members directly.\n\n**Friend non-member functions and friend classes**\n`friend` grants a specific function or class access to private and protected members. Friendship is neither inherited nor transitive. Prefer non-member friend functions (e.g., `operator<<`) over friend classes when possible, to minimise the size of the trust boundary.\n\n**Friend member functions**\nA specific member function of another class can be made a friend. Forward declarations are often needed to resolve ordering dependencies between the two class definitions.\n\n**Ref qualifiers**\nAppending `&` or `&&` to a member function signature restricts it to lvalue or rvalue objects respectively. Use ref qualifiers to prevent operations (like chaining assignment) from silently binding to temporaries.\n\n**Combined example — static counter + destructor + chaining:**\n\n```cpp\n#include <iostream>\nstruct Widget {\n    inline static int s_count{0};\n    int m_val;\n    explicit Widget(int v) : m_val{v} { ++s_count; }\n    ~Widget() { --s_count; }\n    Widget& set(int v) & { m_val = v; return *this; }\n    Widget& add(int v) & { m_val += v; return *this; }\n    static int count() { return s_count; }\n};\nint main() {\n    Widget w{1};\n    w.set(10).add(5);  // chaining\n    std::cout << w.m_val << ' ' << Widget::count() << '\\n'; // 15 1\n}\n```",
      "exercises": [
        {
          "title": "Instance Counter with Destructor",
          "prompt_md": "## Goal\n\nImplement a class called `Tracked` that uses a static member variable to count how many instances currently exist, incrementing on construction and decrementing on destruction.\n\n## Requirements\n\n- `Tracked` has a `static int s_alive` member tracking the current live instance count.\n- The constructor takes a single `int id` and stores it as a member `m_id`.\n- The destructor decrements `s_alive`.\n- Provide a `static int alive()` function that returns `s_alive`.\n- Provide a `int id() const` function that returns `m_id`.\n- In `main`, read an integer `n` from stdin. Create `n` `Tracked` objects with ids 1..n in a `std::vector`. Print `alive()`. Then call `clear()` on the vector. Print `alive()` again.\n\n## Input\n\nA single integer `n` (1 <= n <= 10).\n\n## Output\n\nTwo lines:\n1. The count of alive instances after filling the vector.\n2. The count of alive instances after clearing the vector.\n\n## Example\n\nInput: `3`\n\nOutput:\n```\n3\n0\n```",
          "starter_code": "#include <iostream>\n#include <vector>\n\nclass Tracked {\n    // TODO: declare static member s_alive\n    int m_id{};\npublic:\n    // TODO: constructor(int id) — increment s_alive\n    // TODO: destructor — decrement s_alive\n    // TODO: static int alive()\n    // TODO: int id() const\n};\n\n// TODO: define s_alive outside the class\n\nint main() {\n    int n{};\n    std::cin >> n;\n\n    std::vector<Tracked> items;\n    // TODO: push_back Tracked(1) through Tracked(n)\n\n    std::cout << Tracked::alive() << '\\n';\n    items.clear();\n    std::cout << Tracked::alive() << '\\n';\n\n    return 0;\n}\n",
          "solution_code": "#include <iostream>\n#include <vector>\n\nclass Tracked {\n    static int s_alive;\n    int m_id{};\npublic:\n    explicit Tracked(int id) : m_id{id} { ++s_alive; }\n    ~Tracked() { --s_alive; }\n    static int alive() { return s_alive; }\n    int id() const { return m_id; }\n};\n\nint Tracked::s_alive = 0;\n\nint main() {\n    int n{};\n    std::cin >> n;\n\n    std::vector<Tracked> items;\n    items.reserve(static_cast<std::size_t>(n));\n    for (int i = 1; i <= n; ++i)\n        items.emplace_back(i);\n\n    std::cout << Tracked::alive() << '\\n';\n    items.clear();\n    std::cout << Tracked::alive() << '\\n';\n\n    return 0;\n}\n",
          "test_cases": [
            {
              "label": "n=3 (sample)",
              "stdin": "3",
              "expected_stdout": "3\n0\n",
              "is_sample": true
            },
            {
              "label": "n=1 (single object)",
              "stdin": "1",
              "expected_stdout": "1\n0\n",
              "is_sample": false
            },
            {
              "label": "n=10 (max)",
              "stdin": "10",
              "expected_stdout": "10\n0\n",
              "is_sample": false
            }
          ]
        },
        {
          "title": "Generic Accumulator with Friend and Chaining",
          "prompt_md": "## Goal\n\nImplement a class template `Accum<T>` that accumulates values via method chaining, and a friend non-member function `printAccum` that prints its internal total.\n\n## Requirements\n\n- `Accum<T>` stores a running total `m_total` of type `T`, initialized to `T{}`.\n- `Accum<T>& add(T val) &` adds `val` to `m_total` and returns `*this` (enabling chaining).\n- `Accum<T>& scale(T factor) &` multiplies `m_total` by `factor` and returns `*this`.\n- A `friend` non-member function `void printAccum(const Accum<T>& a)` prints `m_total` followed by a newline. It must access `m_total` directly (it is private).\n- In `main`, read integers from stdin: first `n`, then `n` values to add, then a scale factor. Use chaining to add all values then scale. Call `printAccum` to print the result. Repeat for a second `Accum<double>` with the same input reinterpreted as doubles.\n\n## Input\n\n- Line 1: integer `n`\n- Line 2: `n` space-separated integers (values to add)\n- Line 3: an integer scale factor\n\n## Output\n\n- Line 1: the integer result\n- Line 2: the double result (same numeric value, printed with default stream precision)\n\n## Example\n\nInput:\n```\n3\n2 3 5\n4\n```\n\nOutput:\n```\n40\n40\n```\n\n(2+3+5=10, 10*4=40)",
          "starter_code": "#include <iostream>\n\n// TODO: define class template Accum<T>\n// with private m_total, add(), scale(), and friend printAccum\n\ntemplate <typename T>\nvoid printAccum(const Accum<T>& a) {\n    // TODO: print a.m_total followed by newline\n}\n\nint main() {\n    int n{};\n    std::cin >> n;\n\n    Accum<int> ai;\n    Accum<double> ad;\n\n    for (int i = 0; i < n; ++i) {\n        int v{};\n        std::cin >> v;\n        // TODO: chain add(v) on ai and add(static_cast<double>(v)) on ad\n    }\n\n    int factor{};\n    std::cin >> factor;\n\n    // TODO: chain scale(factor) on ai, scale(static_cast<double>(factor)) on ad\n\n    printAccum(ai);\n    printAccum(ad);\n\n    return 0;\n}\n",
          "solution_code": "#include <iostream>\n\ntemplate <typename T>\nclass Accum {\n    T m_total{};\npublic:\n    Accum<T>& add(T val) & { m_total += val; return *this; }\n    Accum<T>& scale(T factor) & { m_total *= factor; return *this; }\n    friend void printAccum(const Accum<T>& a) {\n        std::cout << a.m_total << '\\n';\n    }\n};\n\nint main() {\n    int n{};\n    std::cin >> n;\n\n    Accum<int> ai;\n    Accum<double> ad;\n\n    for (int i = 0; i < n; ++i) {\n        int v{};\n        std::cin >> v;\n        ai.add(v);\n        ad.add(static_cast<double>(v));\n    }\n\n    int factor{};\n    std::cin >> factor;\n\n    ai.scale(factor);\n    ad.scale(static_cast<double>(factor));\n\n    printAccum(ai);\n    printAccum(ad);\n\n    return 0;\n}\n",
          "test_cases": [
            {
              "label": "3 values, scale 4 (sample)",
              "stdin": "3\n2 3 5\n4",
              "expected_stdout": "40\n40\n",
              "is_sample": true
            },
            {
              "label": "single value, scale 1",
              "stdin": "1\n7\n1",
              "expected_stdout": "7\n7\n",
              "is_sample": false
            },
            {
              "label": "5 values, scale 3",
              "stdin": "5\n1 2 3 4 5\n3",
              "expected_stdout": "45\n45\n",
              "is_sample": false
            }
          ]
        }
      ]
    }
  ]
}