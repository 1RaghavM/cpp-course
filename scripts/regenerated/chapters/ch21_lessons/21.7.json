{
  "number": "21.7",
  "title": "Overloading the comparison operators",
  "sort_order": 6,
  "summary_md": "C++ lets you overload all six comparison operators (`==`, `!=`, `<`, `>`, `<=`, `>=`) so your classes work with the same syntax as built-in types. Each operator takes two operands and returns `bool`. You can implement them as member functions (left operand is `*this`) or as friend free functions when you want implicit conversions on both sides.\n\nThe classic pre-C++20 approach requires you to write `operator==` and `operator<` with the real logic, then define the other four in terms of those two. This is tedious and error-prone -- six functions for one concept.\n\n```cpp\n#include <compare>\n#include <iostream>\n\nclass IntPair {\n    int m_x{};\n    int m_y{};\npublic:\n    IntPair(int x, int y) : m_x{x}, m_y{y} {}\n    auto operator<=>(const IntPair&) const = default;\n};\n```\n\nC++20 solves this with the **spaceship operator** `<=>`, formally called the three-way comparison operator. When you default it with `= default`, the compiler generates all six comparison operators for you by comparing each member in declaration order. The return type is typically `std::strong_ordering` for integral members, which distinguishes *less*, *equal*, and *greater*.\n\nIf you need custom logic, you can write `operator<=>` yourself. It returns one of three ordering types from `<compare>`: `std::strong_ordering` (substitutability -- equal values are indistinguishable), `std::partial_ordering` (allows unordered results, like floating-point NaN), or `std::weak_ordering` (equivalence without substitutability). A custom `<=>` still auto-generates `<`, `>`, `<=`, and `>=`, but you must separately default or define `operator==` because the compiler will not synthesize it from a user-provided `<=>`.\n\nOne important rule: a defaulted `operator<=>` also gives you a defaulted `operator==`, but a user-provided `operator<=>` does not. If you write custom `<=>` logic and also need `==`, explicitly default or implement `operator==`.\n\nPrefer `= default` whenever memberwise comparison is correct. Only write custom comparison logic when your class has members that should be excluded from comparison or requires non-default ordering semantics.",
  "exercises": [
    {
      "title": "Fraction Equality and Ordering",
      "prompt_md": "## Goal\n\nImplement comparison operators for a `Fraction` class using the C++20 spaceship operator so that fractions can be compared by their numeric value.\n\n## Input\n\n- Line 1: two integers `n1` and `d1` representing the first fraction.\n- Line 2: two integers `n2` and `d2` representing the second fraction.\n\n## Output\n\nPrint three lines:\n- `equal` or `not_equal` depending on whether the fractions have the same value.\n- `less` if fraction1 < fraction2, `greater` if fraction1 > fraction2, or `equivalent` otherwise.\n- The result of `(fraction1 <= fraction2)` as `true` or `false`.\n\n## Requirements\n\n- Define a `Fraction` class with `int` numerator and denominator.\n- Compare fractions by cross-multiplication: a/b vs c/d is equivalent to comparing a*d vs c*b (all denominators are positive).\n- Implement `operator<=>` returning `std::strong_ordering` and separately define `operator==`. Both should use cross-multiplication.\n- Use `#include <compare>` and compile with C++20.",
      "starter_code": "#include <compare>\n#include <iostream>\n\nclass Fraction\n{\n    int m_num{};\n    int m_den{ 1 };\n\npublic:\n    Fraction(int num, int den) : m_num{ num }, m_den{ den } {}\n\n    // TODO: Implement operator== using cross-multiplication\n\n    // TODO: Implement operator<=> returning std::strong_ordering\n    // using cross-multiplication (m_num * rhs.m_den vs rhs.m_num * m_den)\n};\n\nint main()\n{\n    int n1{}, d1{}, n2{}, d2{};\n    std::cin >> n1 >> d1 >> n2 >> d2;\n\n    Fraction f1{ n1, d1 };\n    Fraction f2{ n2, d2 };\n\n    // TODO: Print equal/not_equal on line 1\n    // TODO: Print less/greater/equivalent on line 2\n    // TODO: Print true/false for (f1 <= f2) on line 3\n\n    return 0;\n}\n",
      "solution_code": "#include <compare>\n#include <iostream>\n\nclass Fraction\n{\n    int m_num{};\n    int m_den{ 1 };\n\npublic:\n    Fraction(int num, int den) : m_num{ num }, m_den{ den } {}\n\n    bool operator==(const Fraction& rhs) const\n    {\n        return static_cast<long long>(m_num) * rhs.m_den\n            == static_cast<long long>(rhs.m_num) * m_den;\n    }\n\n    std::strong_ordering operator<=>(const Fraction& rhs) const\n    {\n        long long lhs_val{ static_cast<long long>(m_num) * rhs.m_den };\n        long long rhs_val{ static_cast<long long>(rhs.m_num) * m_den };\n        return lhs_val <=> rhs_val;\n    }\n};\n\nint main()\n{\n    int n1{}, d1{}, n2{}, d2{};\n    std::cin >> n1 >> d1 >> n2 >> d2;\n\n    Fraction f1{ n1, d1 };\n    Fraction f2{ n2, d2 };\n\n    std::cout << (f1 == f2 ? \"equal\" : \"not_equal\") << '\\n';\n\n    auto cmp{ f1 <=> f2 };\n    if (cmp < 0)\n        std::cout << \"less\\n\";\n    else if (cmp > 0)\n        std::cout << \"greater\\n\";\n    else\n        std::cout << \"equivalent\\n\";\n\n    std::cout << (f1 <= f2 ? \"true\" : \"false\") << '\\n';\n\n    return 0;\n}\n",
      "test_cases": [
        {
          "label": "Equivalent fractions (1/2 vs 2/4)",
          "is_sample": true,
          "stdin": "1 2\n2 4",
          "expected_stdout": "equal\nequivalent\ntrue\n"
        },
        {
          "label": "First fraction is smaller (1/3 vs 1/2)",
          "is_sample": false,
          "stdin": "1 3\n1 2",
          "expected_stdout": "not_equal\nless\ntrue\n"
        },
        {
          "label": "First fraction is greater (7/2 vs 10/4)",
          "is_sample": false,
          "stdin": "7 2\n10 4",
          "expected_stdout": "not_equal\ngreater\nfalse\n"
        }
      ]
    },
    {
      "title": "Student Record Sorting with Default Spaceship",
      "prompt_md": "## Goal\n\nUse a defaulted `operator<=>` to make a `Student` class comparable, then read three students and print them sorted from smallest to largest.\n\n## Input\n\n- Three lines, each containing an integer `grade` (0-100) followed by a string `name` (no spaces).\n\n## Output\n\n- Three lines, each with `grade name`, sorted in ascending order. The defaulted spaceship compares `m_grade` first, then `m_name` (lexicographic), matching declaration order.\n\n## Requirements\n\n- Define a `Student` class with `int m_grade` and `std::string m_name` (in that order).\n- Use `auto operator<=>(const Student&) const = default;` -- do not write custom comparison logic.\n- Store the three students in an array and sort them using a simple loop or nested comparison (do not use `<algorithm>`).\n- Provide a `print()` const member function that outputs `grade name`.",
      "starter_code": "#include <compare>\n#include <iostream>\n#include <string>\n\nclass Student\n{\n    int m_grade{};\n    std::string m_name{};\n\npublic:\n    Student() = default;\n    Student(int grade, const std::string& name)\n        : m_grade{ grade }, m_name{ name } {}\n\n    // TODO: Default the spaceship operator\n\n    void print() const\n    {\n        std::cout << m_grade << ' ' << m_name << '\\n';\n    }\n};\n\nint main()\n{\n    Student students[3];\n    for (int i{ 0 }; i < 3; ++i)\n    {\n        int g{};\n        std::string n{};\n        std::cin >> g >> n;\n        students[i] = Student{ g, n };\n    }\n\n    // TODO: Sort the 3 students in ascending order\n    // using comparison operators (simple swap-based sort)\n\n    // TODO: Print each student\n\n    return 0;\n}\n",
      "solution_code": "#include <compare>\n#include <iostream>\n#include <string>\n\nclass Student\n{\n    int m_grade{};\n    std::string m_name{};\n\npublic:\n    Student() = default;\n    Student(int grade, const std::string& name)\n        : m_grade{ grade }, m_name{ name } {}\n\n    auto operator<=>(const Student&) const = default;\n\n    void print() const\n    {\n        std::cout << m_grade << ' ' << m_name << '\\n';\n    }\n};\n\nint main()\n{\n    Student students[3];\n    for (int i{ 0 }; i < 3; ++i)\n    {\n        int g{};\n        std::string n{};\n        std::cin >> g >> n;\n        students[i] = Student{ g, n };\n    }\n\n    for (int i{ 0 }; i < 2; ++i)\n        for (int j{ i + 1 }; j < 3; ++j)\n            if (students[j] < students[i])\n            {\n                Student temp{ students[i] };\n                students[i] = students[j];\n                students[j] = temp;\n            }\n\n    for (int i{ 0 }; i < 3; ++i)\n        students[i].print();\n\n    return 0;\n}\n",
      "test_cases": [
        {
          "label": "Distinct grades in reverse order",
          "is_sample": true,
          "stdin": "95 Charlie\n72 Alice\n88 Bob",
          "expected_stdout": "72 Alice\n88 Bob\n95 Charlie\n"
        },
        {
          "label": "Same grades, sorted by name",
          "is_sample": false,
          "stdin": "85 Zara\n85 Mike\n85 Anna",
          "expected_stdout": "85 Anna\n85 Mike\n85 Zara\n"
        },
        {
          "label": "Already sorted input",
          "is_sample": false,
          "stdin": "50 Dan\n75 Eve\n100 Fay",
          "expected_stdout": "50 Dan\n75 Eve\n100 Fay\n"
        }
      ]
    }
  ]
}
