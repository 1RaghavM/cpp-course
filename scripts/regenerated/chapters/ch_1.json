{
  "chapter": {
    "number": "1",
    "title": "C++ Basics"
  },
  "prior_lesson_titles": [
    "0.1: Introduction to these tutorials",
    "0.2: Introduction to programs and programming languages",
    "0.3: Introduction to C/C++",
    "0.4: Introduction to C++ development",
    "0.5: Introduction to the compiler, linker, and libraries",
    "0.6: Installing an Integrated Development Environment (IDE)",
    "0.7: Compiling your first program",
    "0.8: A few common C++ problems",
    "0.9: Configuring your compiler: Build configurations",
    "0.10: Configuring your compiler: Compiler extensions",
    "0.11: Configuring your compiler: Warning and error levels",
    "0.12: Configuring your compiler: Choosing a language standard",
    "0.13: What language standard is my compiler using?"
  ],
  "lessons": [
    {
      "number": "1.1",
      "title": "Statements and the structure of a program",
      "sort_order": 13,
      "summary_md": "# Statements and the Structure of a Program\n\nA **statement** is the smallest independent unit of computation in C++. It tells the compiler to perform a specific action. Every statement in C++ ends with a semicolon (`;`), which acts as a terminator — similar to how a period ends a sentence.\n\n## Types of statements\n\nAt this stage, the two most relevant kinds of statements are:\n\n- **Declaration statements** — these create a variable and (optionally) give it an initial value. Example: `int x = 5;`\n- **Expression statements** — these evaluate an expression and discard the result. Example: `x = 7;` (an assignment) or just `42;` (legal but pointless).\n- **Return statements** — these pass a value back from a function. Example: `return 0;`\n\nThere are other statement types (compound statements, selection statements, iteration statements), but those will come later. For now, focus on the three above.\n\n## The structure of a C++ program\n\nEvery C++ program must contain a function called `main`. When you run the compiled program (as you practiced in Chapter 0 — \"Compiling your first program\"), execution begins at the first statement inside `main` and proceeds downward, one statement at a time.\n\nHere is a minimal valid C++ program:\n\n```cpp\nint main()\n{\n    int x = 0;\n    int y = 5;\n\n    x = y + 2;\n\n    return 0;\n}\n```\n\nBreaking this down:\n\n- `int main()` declares the main function. `int` means it returns an integer.\n- The curly braces `{` and `}` define a **compound statement** (also called a **block**) — they group multiple statements together into the body of the function.\n- `int x = 0;` and `int y = 5;` are declaration statements. They create integer variables and set their values.\n- `x = y + 2;` is an expression statement. It evaluates `y + 2` (which is `7`) and assigns the result to `x`.\n- `return 0;` ends the function and passes `0` back to the operating system. By convention, `0` signals that the program ran successfully.\n\nStatements execute in order, top to bottom. If you moved `x = y + 2;` above the line that creates `y`, the program would fail to compile because `y` would not exist yet.\n\nNotice that every statement ends with a semicolon. Forgetting one is among the most common syntax errors you will encounter.\n",
      "exercises": [
        {
          "title": "Write a minimal valid C++ program",
          "prompt_md": "## Goal\n\nWrite the smallest valid C++ program that compiles and runs successfully.\n\n## Input\n\nNone — this program takes no input.\n\n## Output\n\nNone — this program produces no visible output. The exit code is what matters.\n\n## Requirements\n\n- The program must contain a `main` function that returns `0`.\n- The program must compile without errors or warnings using `g++ -std=c++20 -Wall -Wextra`.\n- Do **not** use `#include`, `cout`, `cin`, or any library features — they have not been taught yet.\n- The return value `0` signals successful execution to the operating system.",
          "starter_code": "// TODO: Write a complete, minimal C++ program.\n// It should have a main function that returns 0.\n\nint main()\n{\n    // TODO: return 0 from here\n    return 0;\n}\n",
          "solution_code": "int main()\n{\n    return 0;\n}\n",
          "test_cases": [
            {
              "label": "Program exits with code 0",
              "stdin": "",
              "expected_stdout": "",
              "is_sample": true
            },
            {
              "label": "No output produced",
              "stdin": "",
              "expected_stdout": "",
              "is_sample": false
            },
            {
              "label": "Compiles cleanly with no warnings",
              "stdin": "",
              "expected_stdout": "",
              "is_sample": false
            }
          ]
        },
        {
          "title": "Use statements to compute and return a value",
          "prompt_md": "## Goal\n\nWrite a program that uses declaration statements and expression statements to perform a simple computation, then returns the result as the program's exit code.\n\n## Input\n\nNone — this program takes no input.\n\n## Output\n\nNone — this program produces no visible output. The exit code carries the result.\n\n## Requirements\n\n1. Declare an integer variable `a` and initialize it to `3`.\n2. Declare an integer variable `b` and initialize it to `4`.\n3. Declare an integer variable `result` with initial value `0`.\n4. Use an expression statement to assign `a + b` to `result`.\n5. Return `result` from `main`.\n\nThe program's exit code should be `7` (since 3 + 4 = 7).\n\nDo **not** use `#include`, `cout`, `cin`, or any library features.",
          "starter_code": "int main()\n{\n    // TODO: Declare integer variable 'a' and initialize it to 3\n\n    // TODO: Declare integer variable 'b' and initialize it to 4\n\n    // TODO: Declare integer variable 'result' with initial value 0\n\n    // TODO: Assign a + b to result\n\n    // TODO: Return result from main\n    return 0;\n}\n",
          "solution_code": "int main()\n{\n    int a = 3;\n    int b = 4;\n    int result = 0;\n\n    result = a + b;\n\n    return result;\n}\n",
          "test_cases": [
            {
              "label": "Exit code is 7 (3 + 4)",
              "stdin": "",
              "expected_stdout": "",
              "is_sample": true
            },
            {
              "label": "No output produced",
              "stdin": "",
              "expected_stdout": "",
              "is_sample": false
            },
            {
              "label": "Compiles cleanly with no warnings",
              "stdin": "",
              "expected_stdout": "",
              "is_sample": false
            }
          ]
        }
      ]
    },
    {
      "number": "1.2",
      "title": "Comments",
      "sort_order": 14,
      "summary_md": "# Comments\n\nIn the previous lesson, *Statements and the structure of a program*, you learned that a C++ program is built from statements inside a `main` function. As programs grow, the code alone doesn't always make the *purpose* clear. Comments let you leave notes for yourself and anyone else who reads your code later. The compiler ignores comments entirely — they produce no instructions and have zero effect on how the program runs.\n\n## Single-line comments\n\nA single-line comment starts with `//`. Everything from `//` to the end of that line is a comment.\n\n```cpp\n// This program does nothing useful yet.\n// It just demonstrates comments.\nint main()\n{\n    // The program ends here and reports success.\n    return 0; // returning 0 means no error\n}\n```\n\nYou can place a `//` comment on its own line or at the end of a line that already contains a statement. Either way, the compiler skips everything after `//` on that line.\n\n## Multi-line comments\n\nWhen a note spans several lines, wrap it with `/*` to start and `*/` to end:\n\n```\n/* This is a multi-line comment.\n   It can span as many lines as you need.\n   The compiler ignores all of it. */\n```\n\nMulti-line comments cannot be nested. A `/*` inside an already-open `/* ... */` block does not start a second comment — the first `*/` the compiler encounters closes the block, which can cause unexpected errors.\n\n## When to comment\n\nWrite a comment when the *reason* behind code is not obvious from the code itself. Good uses include:\n\n- Explaining *why* a particular approach was chosen over an alternative.\n- Summarizing what a section of code is responsible for at a high level.\n- Noting anything surprising or non-obvious.\n\n## What NOT to comment\n\nAvoid restating what the code already says. A comment like `// return 0` next to `return 0;` adds nothing — anyone reading C++ can see the statement. Redundant comments create clutter and become a maintenance burden: when the code changes but the comment doesn't get updated, the comment becomes misleading, which is worse than having no comment at all.\n\nA useful guideline: comments should explain *why*, not *what*. If the code is clear on its own, silence is better than noise.\n",
      "exercises": [
        {
          "title": "Comment Your Code",
          "prompt_md": "## Goal\n\nAdd comments to a working program so that another programmer can understand its purpose.\n\n## Input\n\nThis program takes no input.\n\n## Output\n\nThe program returns `0` from `main`. Adding comments must not change the return value.\n\n## Requirements\n\n1. Add a **single-line comment** (`//`) above the `return` statement explaining what returning `0` means.\n2. Add a **multi-line comment** (`/* ... */`) at the very top of the program (before `int main()`) that contains at least two lines of text describing what the program does.\n3. The program must still compile and return `0`.",
          "starter_code": "// TODO: Add a multi-line comment (/* ... */) above main\n//       describing what this program does (at least 2 lines).\n\nint main()\n{\n    // TODO: Add a single-line comment above the return statement\n    //       explaining what returning 0 means.\n    return 0;\n}\n",
          "solution_code": "/* This program demonstrates the use of comments.\n   It does nothing else — it simply returns 0. */\n\nint main()\n{\n    // Returning 0 tells the operating system the program finished successfully.\n    return 0;\n}\n",
          "test_cases": [
            {
              "label": "Program returns 0",
              "stdin": "",
              "expected_stdout": "",
              "is_sample": true
            },
            {
              "label": "No output produced",
              "stdin": "",
              "expected_stdout": "",
              "is_sample": false
            },
            {
              "label": "Exit code is 0",
              "stdin": "",
              "expected_stdout": "",
              "is_sample": false
            }
          ]
        },
        {
          "title": "Commented-Out Code",
          "prompt_md": "## Goal\n\nUse comments to change which `return` statement actually executes, without deleting any line.\n\n## Input\n\nThis program takes no input.\n\n## Output\n\nThe program must return `42` from `main` (exit code 42). It must produce no printed output.\n\n## Requirements\n\n1. The starter code has two `return` statements. Currently the program returns `0`.\n2. **Comment out** the `return 0;` statement using a single-line comment (`//`) so the compiler skips it.\n3. **Uncomment** the line `// return 42;` so it becomes active code.\n4. Do **not** delete or add any lines — only add or remove comment markers.",
          "starter_code": "int main()\n{\n    return 0;\n    // return 42;\n}\n",
          "solution_code": "int main()\n{\n    // return 0;\n    return 42;\n}\n",
          "test_cases": [
            {
              "label": "Program returns exit code 42",
              "stdin": "",
              "expected_stdout": "",
              "is_sample": true
            },
            {
              "label": "No output produced",
              "stdin": "",
              "expected_stdout": "",
              "is_sample": false
            },
            {
              "label": "Correct exit code verified",
              "stdin": "",
              "expected_stdout": "",
              "is_sample": false
            }
          ]
        }
      ]
    },
    {
      "number": "1.3",
      "title": "Introduction to objects and variables",
      "sort_order": 15,
      "summary_md": "# Introduction to Objects and Variables\n\nIn the previous lessons you learned that a C++ program is a sequence of statements inside `main`, and that comments let you annotate code without affecting its behavior. So far, the only data your programs have worked with is the literal number in `return 0;`. Real programs need to store, retrieve, and manipulate data. That is what objects and variables are for.\n\n## RAM and data storage\n\nWhen your program runs, the operating system gives it a region of **RAM** (random-access memory) to work with. RAM is fast, temporary storage — data in RAM disappears when the program ends. Each piece of data your program needs occupies some number of bytes in this memory. You do not need to manage individual bytes yourself; C++ handles the details when you create variables.\n\n## Objects and variables\n\nAn **object** is a region of memory that can hold a value. A **variable** is a named object — you give it a name so you can refer to it later in your code.\n\nTo create a variable, you write a **definition** (also called a **declaration** for simple cases). The definition tells the compiler two things: the **type** of data the variable holds and the **name** you want to use.\n\n## The `int` type\n\nYou have already seen `int` in `int main()`. The keyword `int` is a **type** that represents a whole number (an integer). When you define an `int` variable, the compiler reserves enough memory (typically 4 bytes) to store an integer value.\n\n## Defining and initializing variables\n\nHere is a small program that defines several `int` variables and uses one in the return statement:\n\n```cpp\nint main()\n{\n    int width = 10;    // definition with initialization\n    int height = 3;    // another initialized variable\n    int area = width * height; // initialized using an expression\n\n    return area; // exit code will be 30\n}\n```\n\nEach line like `int width = 10;` does three things: declares a variable named `width`, allocates memory for an `int`, and stores the value `10` into that memory. The variable `area` is initialized from an expression that multiplies the values of `width` and `height`.\n\n## Variable names\n\nA variable name (also called an **identifier**) must start with a letter or underscore and can contain letters, digits, and underscores. Names are case-sensitive — `count` and `Count` are different variables. Lesson 1.7 covers naming conventions in more detail; for now, stick to short, descriptive lowercase names.\n",
      "exercises": [
        {
          "title": "Define variables and return a computed value",
          "prompt_md": "## Goal\n\nWrite a program that defines three `int` variables and returns a value computed from them.\n\n## Input\n\nNone — this program takes no input.\n\n## Output\n\nNone — this program produces no visible output. The exit code carries the result.\n\n## Requirements\n\n1. Define an `int` variable named `length` and initialize it to `8`.\n2. Define an `int` variable named `width` and initialize it to `5`.\n3. Define an `int` variable named `area` and initialize it to `length * width`.\n4. Return `area` from `main`.\n\nThe program's exit code should be `40` (since 8 × 5 = 40).\n\nDo **not** use `#include`, `cout`, `cin`, or any library features — they have not been taught yet.",
          "starter_code": "int main()\n{\n    // TODO: Define an int variable 'length' initialized to 8\n\n    // TODO: Define an int variable 'width' initialized to 5\n\n    // TODO: Define an int variable 'area' initialized to length * width\n\n    // TODO: Return area\n    return 0;\n}\n",
          "solution_code": "int main()\n{\n    int length = 8;\n    int width = 5;\n    int area = length * width;\n\n    return area;\n}\n",
          "test_cases": [
            {
              "label": "Exit code is 40 (8 * 5)",
              "stdin": "",
              "expected_stdout": "",
              "is_sample": true
            },
            {
              "label": "No output produced",
              "stdin": "",
              "expected_stdout": "",
              "is_sample": false
            },
            {
              "label": "Compiles cleanly with no warnings",
              "stdin": "",
              "expected_stdout": "",
              "is_sample": false
            }
          ]
        },
        {
          "title": "Combine variables to encode a date as a single number",
          "prompt_md": "## Goal\n\nWrite a program that stores the parts of a date in separate variables and combines them into a single integer, then returns that integer as the exit code.\n\n## Input\n\nNone — this program takes no input.\n\n## Output\n\nNone — this program produces no visible output. The exit code carries the result.\n\n## Requirements\n\n1. Define an `int` variable named `month` and initialize it to `3`.\n2. Define an `int` variable named `day` and initialize it to `14`.\n3. Define an `int` variable named `combined` and initialize it to `month * day + month`. This produces a single number that encodes both values.\n4. Return `combined` from `main`.\n\nThe exit code should be `45` (since 3 × 14 + 3 = 45).\n\nDo **not** use `#include`, `cout`, `cin`, or any library features.\n\n*Hint:* Remember from *Statements and the structure of a program* that `return` passes a value back to the operating system. That value is the program's exit code.",
          "starter_code": "int main()\n{\n    // TODO: Define an int variable 'month' initialized to 3\n\n    // TODO: Define an int variable 'day' initialized to 14\n\n    // TODO: Define an int variable 'combined' initialized to month * day + month\n\n    // TODO: Return combined\n    return 0;\n}\n",
          "solution_code": "int main()\n{\n    int month = 3;\n    int day = 14;\n    int combined = month * day + month;\n\n    return combined;\n}\n",
          "test_cases": [
            {
              "label": "Exit code is 45 (3 * 14 + 3)",
              "stdin": "",
              "expected_stdout": "",
              "is_sample": true
            },
            {
              "label": "No output produced",
              "stdin": "",
              "expected_stdout": "",
              "is_sample": false
            },
            {
              "label": "Compiles cleanly with no warnings",
              "stdin": "",
              "expected_stdout": "",
              "is_sample": false
            }
          ]
        }
      ]
    },
    {
      "number": "1.4",
      "title": "Variable assignment and initialization",
      "sort_order": 16,
      "summary_md": "# Variable Assignment and Initialization\n\nIn *Introduction to objects and variables*, you learned how to declare a variable with `int x;`. Declaring a variable allocates memory, but unless you explicitly provide a value, the variable is **uninitialized** — it holds whatever garbage was already in that memory. Using an uninitialized variable is one of the most common sources of bugs in C++.\n\nThis lesson covers how to give a variable a value, both at creation and afterward.\n\n## Copy assignment\n\nAfter a variable has been declared, you can give it a value using the **copy assignment operator** (`=`):\n\n```cpp\nint main()\n{\n    int x;      // declaration — x is uninitialized\n    x = 10;     // copy assignment — x now holds 10\n    x = 20;     // reassignment — x now holds 20\n\n    return x;   // returns 20\n}\n```\n\nAssignment replaces whatever value the variable previously held. You can assign to the same variable as many times as you need.\n\n## Initialization\n\nRather than declaring first and assigning second, you can provide a value at the point of declaration. C++ offers several forms of **initialization**:\n\n- **Copy initialization:** `int x = 5;` — inherited from C, familiar-looking, works fine for simple types.\n- **Direct initialization:** `int x(5);` — uses parentheses. Behaves identically to copy initialization for integers, but the distinction matters later with more complex types.\n- **Brace initialization (list initialization):** `int x{5};` — introduced in modern C++. This is the **preferred** form.\n\nBrace initialization has one key advantage: it **disallows narrowing conversions**. If you accidentally try to store a value that doesn't fit — say, a fractional number in an `int` — brace initialization causes a compile error instead of silently dropping the fractional part. The other forms allow this quietly, hiding bugs.\n\n## Initializing multiple variables\n\nYou can declare and initialize multiple variables of the same type on a single line:\n\n```cpp\nint a{1}, b{2}, c{3};\n```\n\nHowever, each variable needs its own initializer. Writing `int a, b{5};` declares `a` as uninitialized and only initializes `b`. For clarity, many programmers prefer one declaration per line, especially when each variable serves a distinct purpose.\n\n## Default initialization and value initialization\n\nDeclaring `int x;` without any initializer leaves `x` uninitialized. Writing `int x{};` (empty braces) **value-initializes** it to `0`. Prefer `int x{0};` or `int x{};` over leaving variables uninitialized — it eliminates an entire class of bugs for no extra cost.\n",
      "exercises": [
        {
          "title": "Practice initialization forms",
          "prompt_md": "## Goal\n\nDeclare several integer variables using different initialization styles, then return a computed result as the program's exit code.\n\n## Input\n\nNone — this program takes no input.\n\n## Output\n\nNone — this program produces no visible output. The exit code carries the result.\n\n## Requirements\n\n1. Declare an integer variable `a` using **copy initialization** with the value `10`.\n2. Declare an integer variable `b` using **direct initialization** with the value `20`.\n3. Declare an integer variable `c` using **brace initialization** with the value `15`.\n4. Declare an integer variable `result` using **brace initialization** and initialize it to `0`.\n5. Assign `a + b - c` to `result`.\n6. Return `result` from `main`.\n\nThe program's exit code should be `15` (since 10 + 20 - 15 = 15).\n\nDo **not** use `#include`, `cout`, `cin`, or any library features.",
          "starter_code": "int main()\n{\n    // TODO: Declare 'a' using copy initialization with value 10\n\n    // TODO: Declare 'b' using direct initialization with value 20\n\n    // TODO: Declare 'c' using brace initialization with value 15\n\n    // TODO: Declare 'result' using brace initialization, initialized to 0\n\n    // TODO: Assign a + b - c to result\n\n    // TODO: Return result\n    return 0;\n}\n",
          "solution_code": "int main()\n{\n    int a = 10;   // copy initialization\n    int b(20);    // direct initialization\n    int c{15};    // brace initialization\n    int result{0};\n\n    result = a + b - c;\n\n    return result;\n}\n",
          "test_cases": [
            {
              "label": "Exit code is 15 (10 + 20 - 15)",
              "stdin": "",
              "expected_stdout": "",
              "is_sample": true
            },
            {
              "label": "No output produced",
              "stdin": "",
              "expected_stdout": "",
              "is_sample": false
            },
            {
              "label": "Compiles cleanly with no warnings",
              "stdin": "",
              "expected_stdout": "",
              "is_sample": false
            }
          ]
        },
        {
          "title": "Swap two variables and return the difference",
          "prompt_md": "## Goal\n\nDeclare two integer variables, swap their values using a temporary variable, then return the difference as the program's exit code.\n\n## Input\n\nNone — this program takes no input.\n\n## Output\n\nNone — this program produces no visible output. The exit code carries the result.\n\n## Requirements\n\n1. Declare an integer variable `x` using brace initialization with the value `50`.\n2. Declare an integer variable `y` using brace initialization with the value `30`.\n3. Swap the values of `x` and `y` using a temporary variable `temp`. After the swap, `x` should hold `30` and `y` should hold `50`.\n4. Return `y - x` from `main`.\n\nThe program's exit code should be `20` (since after the swap, y is 50 and x is 30, so 50 - 30 = 20).\n\nDo **not** use `#include`, `cout`, `cin`, or any library features — they have not been taught yet.",
          "starter_code": "int main()\n{\n    int x{50};\n    int y{30};\n\n    // TODO: Declare a temporary variable 'temp' and use it to swap x and y\n\n    // TODO: Return y - x\n    return 0;\n}\n",
          "solution_code": "int main()\n{\n    int x{50};\n    int y{30};\n\n    int temp{x};\n    x = y;\n    y = temp;\n\n    return y - x;\n}\n",
          "test_cases": [
            {
              "label": "Exit code is 20 (50 - 30 after swap)",
              "stdin": "",
              "expected_stdout": "",
              "is_sample": true
            },
            {
              "label": "No output produced",
              "stdin": "",
              "expected_stdout": "",
              "is_sample": false
            },
            {
              "label": "Compiles cleanly with no warnings",
              "stdin": "",
              "expected_stdout": "",
              "is_sample": false
            }
          ]
        }
      ]
    },
    {
      "number": "1.5",
      "title": "Introduction to iostream: cout, cin, and endl",
      "sort_order": 17,
      "summary_md": "## iostream: cout, cin, and endl\n\nThe `<iostream>` header gives your program two objects it needs constantly: `std::cout` for output and `std::cin` for input. These aren't functions — they're objects with overloaded operators, a concept you'll understand more deeply later.\n\n### Sending output with std::cout\n\n`std::cout` uses the insertion operator `<<` to push values to the terminal. You can chain multiple `<<` calls on one line:\n\n```cpp\n#include <iostream>\n\nint main() {\n    int score{ 42 };          // direct initialization (see: Variable assignment and initialization)\n    std::cout << \"Score: \" << score << '\\n';\n    std::cout << \"Double: \" << score * 2 << '\\n';\n    return 0;\n}\n```\n\nThe `'\\n'` character ends the line. You'll also see `std::endl`, which does the same thing but additionally flushes the output buffer — forcing the OS to write pending output immediately. Prefer `'\\n'` in tight loops because flushing is slow; use `std::endl` when you need the output to appear before a blocking operation like `std::cin`.\n\n### Reading input with std::cin\n\n`std::cin` uses the extraction operator `>>` to read a value from the user and store it in a variable. The variable must already exist (see: Introduction to objects and variables):\n\n```cpp\nint age{};\nstd::cin >> age;\n```\n\n`std::cin` skips leading whitespace and stops at the first whitespace after the value, so reading two integers in sequence works naturally:\n\n```cpp\nint x{}, y{};\nstd::cin >> x >> y;   // reads two space- or newline-separated integers\n```\n\nIn Python you call `input()` and cast manually. In C++ the type of the variable tells `>>` how to parse — an `int` variable causes it to parse an integer. Type mismatches put `std::cin` into a fail state; you'll learn error handling later.\n\n### The std:: prefix\n\nEvery name in the standard library lives in the `std` namespace. The prefix `std::` tells the compiler where to find `cout`, `cin`, and `endl`. Lesson 1-2 (Comments) mentioned that clear names reduce confusion — `std::cout` is unambiguous in a way that a bare `cout` (after `using namespace std;`) is not. Avoid `using namespace std;` in files you'll maintain.\n\n### Practical mental model\n\n- `std::cout << x` — push `x` toward the screen\n- `std::cin >> x` — pull a value from the keyboard into `x`\n- The arrows point in the direction data moves\n",
      "exercises": [
        {
          "title": "Echo and Double",
          "prompt_md": "## Echo and Double\n\nWrite a program that reads two integers from stdin (on the same line, separated by a space) and prints three lines:\n\n1. The sum of the two numbers\n2. The difference (first minus second)\n3. The product of the two numbers\n\nPrint each result on its own line in the following format:\n\n```\nSum: <value>\nDifference: <value>\nProduct: <value>\n```\n\n### Example\n\n**Input:**\n```\n8 3\n```\n\n**Output:**\n```\nSum: 11\nDifference: 5\nProduct: 24\n```\n\nRequirements:\n- Use `std::cin` to read the input\n- Use `std::cout` with `'\\n'` to end each line\n- No extra spaces or blank lines in output\n",
          "starter_code": "#include <iostream>\n\nint main() {\n    int a{};\n    int b{};\n\n    // TODO: read a and b from stdin using std::cin\n\n    // TODO: print \"Sum: <a + b>\" followed by a newline\n    // TODO: print \"Difference: <a - b>\" followed by a newline\n    // TODO: print \"Product: <a * b>\" followed by a newline\n\n    return 0;\n}\n",
          "solution_code": "#include <iostream>\n\nint main() {\n    int a{};\n    int b{};\n    std::cin >> a >> b;\n\n    std::cout << \"Sum: \" << a + b << '\\n';\n    std::cout << \"Difference: \" << a - b << '\\n';\n    std::cout << \"Product: \" << a * b << '\\n';\n\n    return 0;\n}\n",
          "test_cases": [
            {
              "label": "Sample: 8 and 3",
              "stdin": "8 3",
              "expected_stdout": "Sum: 11\nDifference: 5\nProduct: 24\n",
              "is_sample": true
            },
            {
              "label": "Hidden: negative values",
              "stdin": "-4 7",
              "expected_stdout": "Sum: 3\nDifference: -11\nProduct: -28\n",
              "is_sample": false
            },
            {
              "label": "Hidden: zeros",
              "stdin": "0 5",
              "expected_stdout": "Sum: 5\nDifference: -5\nProduct: 0\n",
              "is_sample": false
            },
            {
              "label": "Hidden: both same",
              "stdin": "6 6",
              "expected_stdout": "Sum: 12\nDifference: 0\nProduct: 36\n",
              "is_sample": false
            }
          ]
        },
        {
          "title": "Year Calculator",
          "prompt_md": "## Year Calculator\n\nWrite a program that reads two integers from stdin: a person's **birth year** and the **current year** (on the same line, separated by a space).\n\nPrint two lines:\n1. The person's age (current year minus birth year)\n2. What year they will turn 100\n\nUse exactly this format:\n\n```\nAge: <value>\nTurns 100 in: <value>\n```\n\n### Example\n\n**Input:**\n```\n1995 2025\n```\n\n**Output:**\n```\nAge: 30\nTurns 100 in: 2095\n```\n\nRequirements:\n- Use `std::cin` to read both values\n- Use `std::cout` and `'\\n'` for output\n- No extra blank lines\n",
          "starter_code": "#include <iostream>\n\nint main() {\n    int birth_year{};\n    int current_year{};\n\n    // TODO: read birth_year and current_year from stdin using std::cin\n\n    // TODO: compute age (current_year - birth_year)\n    // TODO: compute the year the person turns 100 (birth_year + 100)\n\n    // TODO: print \"Age: <age>\" followed by a newline\n    // TODO: print \"Turns 100 in: <year>\" followed by a newline\n\n    return 0;\n}\n",
          "solution_code": "#include <iostream>\n\nint main() {\n    int birth_year{};\n    int current_year{};\n\n    std::cin >> birth_year >> current_year;\n\n    int age{ current_year - birth_year };\n    int turns_100{ birth_year + 100 };\n\n    std::cout << \"Age: \" << age << '\\n';\n    std::cout << \"Turns 100 in: \" << turns_100 << '\\n';\n\n    return 0;\n}\n",
          "test_cases": [
            {
              "label": "Sample: born 1995, current 2025",
              "stdin": "1995 2025",
              "expected_stdout": "Age: 30\nTurns 100 in: 2095\n",
              "is_sample": true
            },
            {
              "label": "Hidden: born 2000, current 2025",
              "stdin": "2000 2025",
              "expected_stdout": "Age: 25\nTurns 100 in: 2100\n",
              "is_sample": false
            },
            {
              "label": "Hidden: born 1950, current 2024",
              "stdin": "1950 2024",
              "expected_stdout": "Age: 74\nTurns 100 in: 2050\n",
              "is_sample": false
            }
          ]
        }
      ]
    },
    {
      "number": "1.6",
      "title": "Uninitialized variables and undefined behavior",
      "sort_order": 18,
      "summary_md": "## Uninitialized Variables and Undefined Behavior\n\nIn Python, using a variable before assigning it raises a `NameError` at runtime. C++ does not offer that protection. When you declare a variable without initializing it, the memory it occupies holds whatever bytes happened to be there — leftover stack data from a previous function call, OS residue, anything. Reading that value is **undefined behavior** (UB).\n\n### What \"uninitialized\" actually means\n\nFrom *Introduction to objects and variables*, you know that declaring `int x;` reserves memory for `x`. But C++ does not zero that memory for local variables. Until you write a value into `x`, its content is indeterminate.\n\n```cpp\n#include <iostream>\n\nint main() {\n    int a { 5 };      // direct-list initialization — always prefer this\n    int b {};         // value-initialization: b is guaranteed 0\n    int c;            // uninitialized — reading c is undefined behavior\n\n    std::cout << \"a=\" << a << \" b=\" << b << '\\n';\n    // std::cout << c;  // DO NOT do this — undefined behavior\n}\n```\n\nPrefer `int b {}` (value-initialization) from *Variable assignment and initialization* any time you need a known zero. Prefer `int a { 5 }` when you have a meaningful starting value. Never leave a local variable without an initializer unless you are about to overwrite it unconditionally on the very next line.\n\n### Why UB is dangerous\n\nUndefined behavior does not guarantee a crash or a wrong number. The C++ standard places **no constraints** on what happens. In practice:\n\n- Debug builds often read predictable-looking garbage (zeros from a fresh page, or recognizable stack patterns).\n- Optimized builds can silently delete, reorder, or transform code that the compiler proves must involve UB — including security-critical checks.\n- The program may appear to work correctly for months, then fail on a different machine or after a compiler upgrade.\n\nThis is a sharp difference from Python, where every operation is checked at runtime.\n\n### The compiler can warn you\n\nCompiling with `-Wall -Wextra` lets `g++` detect many (not all) uses of uninitialized variables. `-fsanitize=undefined` catches more at runtime during testing. Neither tool is exhaustive — the only reliable strategy is to initialize every variable at the point of declaration, which modern C++ style enforces.\n\n### Quick rule\n\nIf you declare it, initialize it. If you are filling it from `std::cin` (covered in *Introduction to iostream: cout, cin, and endl*), still write `int x {};` before the `cin >> x` line — it costs nothing and makes the intent clear.",
      "exercises": [
        {
          "title": "Initialize Variables Correctly",
          "prompt_md": "# Initialize Variables Correctly\n\nThe starter code declares several variables but leaves some **uninitialized**. Your task is to add proper initializers so the program produces the correct output.\n\n## Requirements\n\n1. Read three integers `a`, `b`, and `c` from stdin (space-separated on one line).\n2. The program then computes:\n   - `sum` = a + b + c\n   - `product` = a * b\n   - `difference` = c - a\n3. Print the three results on separate lines.\n\n## The bug\n\nThe starter code leaves `sum`, `product`, and `difference` uninitialized. Fix them by giving each variable the correct initial value using the expressions above.\n\n## Example\n\nInput:\n```\n2 3 10\n```\n\nOutput:\n```\n15\n6\n8\n```\n\nExplanation: sum = 2+3+10 = 15, product = 2*3 = 6, difference = 10-2 = 8",
          "starter_code": "#include <iostream>\n\nint main() {\n    int a{};\n    int b{};\n    int c{};\n    std::cin >> a >> b >> c;\n\n    // TODO: Fix these — each variable must be initialized with the correct expression\n    int sum;\n    int product;\n    int difference;\n\n    std::cout << sum << '\\n';\n    std::cout << product << '\\n';\n    std::cout << difference << '\\n';\n\n    return 0;\n}\n",
          "solution_code": "#include <iostream>\n\nint main() {\n    int a{};\n    int b{};\n    int c{};\n    std::cin >> a >> b >> c;\n\n    int sum{ a + b + c };\n    int product{ a * b };\n    int difference{ c - a };\n\n    std::cout << sum << '\\n';\n    std::cout << product << '\\n';\n    std::cout << difference << '\\n';\n\n    return 0;\n}\n",
          "test_cases": [
            {
              "label": "Sample: 2 3 10",
              "stdin": "2 3 10",
              "expected_stdout": "15\n6\n8\n",
              "is_sample": true
            },
            {
              "label": "Hidden: with zeros",
              "stdin": "0 5 7",
              "expected_stdout": "12\n0\n7\n",
              "is_sample": false
            },
            {
              "label": "Hidden: negative values",
              "stdin": "-3 4 1",
              "expected_stdout": "2\n-12\n4\n",
              "is_sample": false
            }
          ]
        },
        {
          "title": "Predict the Output",
          "prompt_md": "## Predict the Output\n\nThe point of this exercise is to see what happens when variables are properly initialized vs. when they hold computed values.\n\nRead two integers `x` and `y` from stdin (space-separated). Then:\n\n1. Declare an integer `sum` initialized to `x + y`.\n2. Declare an integer `doubled` initialized to `sum * 2`.\n3. Declare an integer `shifted` initialized to `doubled - y`.\n\nPrint each result on its own line in this format:\n\n```\nsum: <value>\ndoubled: <value>\nshifted: <value>\n```\n\n**Key lesson:** Every variable is initialized at the point of declaration — no variable is ever used before being given a value.\n\n**Sample input:**\n```\n4 6\n```\n\n**Sample output:**\n```\nsum: 10\ndoubled: 20\nshifted: 14\n```",
          "starter_code": "#include <iostream>\n\nint main() {\n    int x{};\n    int y{};\n    std::cin >> x >> y;\n\n    // TODO: declare and initialize sum, doubled, and shifted\n    // using the expressions described in the prompt\n\n    // TODO: print each result on its own line\n\n    return 0;\n}\n",
          "solution_code": "#include <iostream>\n\nint main() {\n    int x{};\n    int y{};\n    std::cin >> x >> y;\n\n    int sum{ x + y };\n    int doubled{ sum * 2 };\n    int shifted{ doubled - y };\n\n    std::cout << \"sum: \" << sum << '\\n';\n    std::cout << \"doubled: \" << doubled << '\\n';\n    std::cout << \"shifted: \" << shifted << '\\n';\n\n    return 0;\n}\n",
          "test_cases": [
            {
              "label": "Sample: 4 and 6",
              "stdin": "4 6",
              "expected_stdout": "sum: 10\ndoubled: 20\nshifted: 14\n",
              "is_sample": true
            },
            {
              "label": "Hidden: zeros",
              "stdin": "0 0",
              "expected_stdout": "sum: 0\ndoubled: 0\nshifted: 0\n",
              "is_sample": false
            },
            {
              "label": "Hidden: negative values",
              "stdin": "-3 8",
              "expected_stdout": "sum: 5\ndoubled: 10\nshifted: 2\n",
              "is_sample": false
            }
          ]
        },
        {
          "title": "Fix the Uninitialized Variables",
          "prompt_md": "# Fix the Uninitialized Variables\n\nThe starter code has a bug: two variables are used in computations without being initialized first. This is **undefined behavior** — the program might print garbage values, or it might appear to work and then fail on another machine.\n\nYour task: add proper initializers so the program always produces the correct output.\n\n## Requirements\n\n1. Read two integers `a` and `b` from stdin (space-separated).\n2. `total` must be initialized to `a + b`.\n3. `scaled` must be initialized to `total * 3`.\n4. Print both values, each on its own line.\n\n## Example\n\nInput:\n```\n5 7\n```\n\nOutput:\n```\n12\n36\n```\n\nExplanation: total = 5 + 7 = 12, scaled = 12 * 3 = 36.",
          "starter_code": "#include <iostream>\n\nint main() {\n    int a{};\n    int b{};\n    std::cin >> a >> b;\n\n    // BUG: these are uninitialized — fix them!\n    int total;\n    int scaled;\n\n    std::cout << total << '\\n';\n    std::cout << scaled << '\\n';\n\n    return 0;\n}\n",
          "solution_code": "#include <iostream>\n\nint main() {\n    int a{};\n    int b{};\n    std::cin >> a >> b;\n\n    int total{ a + b };\n    int scaled{ total * 3 };\n\n    std::cout << total << '\\n';\n    std::cout << scaled << '\\n';\n\n    return 0;\n}\n",
          "test_cases": [
            {
              "label": "Sample: 5 and 7",
              "stdin": "5 7",
              "expected_stdout": "12\n36\n",
              "is_sample": true
            },
            {
              "label": "Hidden: zeros",
              "stdin": "0 0",
              "expected_stdout": "0\n0\n",
              "is_sample": false
            },
            {
              "label": "Hidden: negative values",
              "stdin": "-2 8",
              "expected_stdout": "6\n18\n",
              "is_sample": false
            }
          ]
        },
        {
          "title": "Swap Without a Temporary",
          "prompt_md": "## Swap Without a Temporary\n\nRead two integers `a` and `b` from stdin. Swap their values **using a properly initialized temporary variable**, then print the swapped values.\n\nThis exercise is specifically about correct initialization: a common beginner mistake is to declare the temporary variable without initializing it, then use it before it holds a valid value.\n\n**Requirements:**\n- Declare every variable with an initializer at the point of declaration.\n- The temporary variable used for the swap must be initialized to `a` at the time it is declared (not in a separate assignment statement).\n- Output format (two lines):\n  ```\n  a: <new_a>\n  b: <new_b>\n  ```\n\n**Input:** Two integers on a single line separated by a space.\n\n**Sample input:**\n```\n3 7\n```\n**Sample output:**\n```\na: 7\nb: 3\n```",
          "starter_code": "#include <iostream>\n\nint main() {\n    // TODO: declare and initialize a and b using value-initialization {}\n    // then read both from stdin\n\n    // TODO: swap a and b using a temporary variable.\n    // IMPORTANT: initialize the temporary to a at the point of declaration:\n    //   int tmp { a };   <-- correct\n    //   int tmp;         <-- WRONG: uninitialized, undefined behavior if read\n\n    // TODO: print the result\n    // Expected format:\n    //   a: <value>\n    //   b: <value>\n\n    return 0;\n}\n",
          "solution_code": "#include <iostream>\n\nint main() {\n    int a {};\n    int b {};\n    std::cin >> a >> b;\n\n    int tmp { a };  // initialized immediately — no uninitialized read\n    a = b;\n    b = tmp;\n\n    std::cout << \"a: \" << a << '\\n';\n    std::cout << \"b: \" << b << '\\n';\n\n    return 0;\n}\n",
          "test_cases": [
            {
              "label": "Basic swap",
              "stdin": "3 7",
              "expected_stdout": "a: 7\nb: 3\n",
              "is_sample": true
            },
            {
              "label": "Negative values",
              "stdin": "-10 5",
              "expected_stdout": "a: 5\nb: -10\n",
              "is_sample": false
            },
            {
              "label": "Both zeros",
              "stdin": "0 0",
              "expected_stdout": "a: 0\nb: 0\n",
              "is_sample": false
            }
          ]
        }
      ]
    },
    {
      "number": "1.7",
      "title": "Keywords and naming identifiers",
      "sort_order": 19,
      "summary_md": "# Keywords and Naming Identifiers\n\nKeywords are reserved words in C++ that have special meaning to the compiler. You cannot use them as variable or function names. Common keywords you have already seen include `int` and `return`. The full list includes many more that you will encounter in later chapters. There are about 90 keywords in total — you will encounter more as you progress through this course.\n\nIdentifiers are names you create for variables, functions, and other entities. C++ is case-sensitive, so `myVar`, `MyVar`, and `myvar` are three different identifiers.\n\n## Naming Rules\n\nC++ enforces strict rules:\n- Must start with a letter (a–z, A–Z) or underscore (`_`)\n- Subsequent characters can be letters, digits (0–9), or underscores\n- Cannot be a keyword or contain spaces\n- Avoid starting with underscore followed by an uppercase letter or double underscores — these are reserved for compiler and standard library internals\n\n## Naming Conventions\n\nFollowing conventions makes code readable. The C++ Standard Library uses `snake_case` for variables and functions (`my_variable`), while many projects use `camelCase`. Pick one style and be consistent.\n\nChoose descriptive names. `age` is better than `a`. Names should reflect what the variable represents. Abbreviations like `td` for `total_damage` hurt readability when you revisit code months later.\n\n```cpp\n#include <iostream>\n\nint main()\n{\n    int player_health{ 100 };\n    int player_damage{ 25 };\n    int remaining_health{ player_health - player_damage };\n\n    std::cout << \"Remaining health: \" << remaining_health << '\\n';\n    return 0;\n}\n```\n\nNotice how `player_health`, `player_damage`, and `remaining_health` immediately tell you what each variable represents — no comments needed.\n\n## The `std::` prefix revisited\n\nFrom the iostream lesson: `std::cout` and `std::cin` live in the `std` namespace. The prefix `std::` is itself a naming convention — it prevents your identifiers from colliding with standard library names. Avoid `using namespace std;` because it pulls every standard library name into your scope, making collisions possible.\n\nGood naming prevents compiler errors and makes your code maintainable. In C++, your variable names are often the primary documentation of intent.",
      "exercises": [
        {
          "title": "Well-Named Variables",
          "prompt_md": "## Well-Named Variables\n\nWrite a program that computes the area of a rectangle using well-named variables.\n\nRead two integers from stdin (width and height, on the same line separated by a space). Compute the area and print it in the following format:\n\n```\nWidth: <width>\nHeight: <height>\nArea: <area>\n```\n\nYou must use descriptive variable names — not single letters like `w` or `h`. Use `snake_case` naming convention.\n\n### Example\n\n**Input:**\n```\n5 3\n```\n\n**Output:**\n```\nWidth: 5\nHeight: 3\nArea: 15\n```\n\nRequirements:\n- Use `std::cin` to read input\n- Use `std::cout` with `'\\n'` for output\n- No extra spaces or blank lines in output\n",
          "starter_code": "#include <iostream>\n\nint main()\n{\n    // TODO: declare well-named integer variables for width and height\n\n    // TODO: read width and height from std::cin\n\n    // TODO: compute the area using a well-named variable\n\n    // TODO: print each value on its own line in the required format\n\n    return 0;\n}\n",
          "solution_code": "#include <iostream>\n\nint main()\n{\n    int width{};\n    int height{};\n\n    std::cin >> width >> height;\n\n    int area{ width * height };\n\n    std::cout << \"Width: \" << width << '\\n';\n    std::cout << \"Height: \" << height << '\\n';\n    std::cout << \"Area: \" << area << '\\n';\n\n    return 0;\n}\n",
          "test_cases": [
            {
              "label": "Sample: 5 x 3",
              "stdin": "5 3",
              "expected_stdout": "Width: 5\nHeight: 3\nArea: 15\n",
              "is_sample": true
            },
            {
              "label": "Hidden: 1 x 1",
              "stdin": "1 1",
              "expected_stdout": "Width: 1\nHeight: 1\nArea: 1\n",
              "is_sample": false
            },
            {
              "label": "Hidden: larger values",
              "stdin": "12 8",
              "expected_stdout": "Width: 12\nHeight: 8\nArea: 96\n",
              "is_sample": false
            }
          ]
        },
        {
          "title": "Distance Calculator",
          "prompt_md": "## Distance Calculator\n\nA car travels at a constant speed. Write a program that reads two integers from stdin (on the same line, separated by a space): the speed in kilometers per hour and the number of hours traveled.\n\nCompute and print the total distance, then compute the average speed if the car had covered that distance in one extra hour.\n\nPrint two lines in exactly this format:\n\n```\nTotal distance: <value> km\nAverage over <hours+1> hours: <value> km/h\n```\n\nUse integer division for the average (it truncates, which is expected).\n\n### Example\n\n**Input:**\n```\n60 3\n```\n\n**Output:**\n```\nTotal distance: 180 km\nAverage over 4 hours: 45 km/h\n```\n\n(60 * 3 = 180 km total; 180 / (3+1) = 45 km/h)\n\nRequirements:\n- Use descriptive `snake_case` variable names\n- Use `std::cin` to read input\n- Use `std::cout` with `'\\n'` for output\n",
          "starter_code": "#include <iostream>\n\nint main()\n{\n    // TODO: declare well-named integer variables for speed and hours\n\n    // TODO: read speed and hours from std::cin\n\n    // TODO: compute total distance = speed * hours\n\n    // TODO: compute average speed if trip took hours + 1 (integer division)\n\n    // TODO: print both lines in the required format\n\n    return 0;\n}\n",
          "solution_code": "#include <iostream>\n\nint main()\n{\n    int speed_kmh{};\n    int hours{};\n\n    std::cin >> speed_kmh >> hours;\n\n    int total_distance{ speed_kmh * hours };\n    int extra_hours{ hours + 1 };\n    int average_speed{ total_distance / extra_hours };\n\n    std::cout << \"Total distance: \" << total_distance << \" km\" << '\\n';\n    std::cout << \"Average over \" << extra_hours << \" hours: \" << average_speed << \" km/h\" << '\\n';\n\n    return 0;\n}\n",
          "test_cases": [
            {
              "label": "Sample: 60 km/h for 3 hours",
              "stdin": "60 3",
              "expected_stdout": "Total distance: 180 km\nAverage over 4 hours: 45 km/h\n",
              "is_sample": true
            },
            {
              "label": "Hidden: 100 km/h for 1 hour",
              "stdin": "100 1",
              "expected_stdout": "Total distance: 100 km\nAverage over 2 hours: 50 km/h\n",
              "is_sample": false
            },
            {
              "label": "Hidden: 50 km/h for 7 hours",
              "stdin": "50 7",
              "expected_stdout": "Total distance: 350 km\nAverage over 8 hours: 43 km/h\n",
              "is_sample": false
            }
          ]
        }
      ]
    },
    {
      "number": "1.8",
      "title": "Whitespace and basic formatting",
      "sort_order": 20,
      "summary_md": "## Whitespace and Basic Formatting\n\nWhitespace refers to characters that produce blank space: spaces, tabs, and newlines. C++ is largely whitespace-insensitive — the compiler ignores extra spaces and blank lines between tokens. This differs from Python, where indentation is syntactically significant.\n\n```cpp\n#include <iostream>\n\nint main()\n{\n    int x{ 5 };        // initialized (see lesson: Variable assignment and initialization)\n    int y{ 10 };\n\n    // Spaces around operators aid readability\n    int sum{ x + y };\n\n    std::cout << \"Sum: \" << sum << '\\n';\n    return 0;\n}\n```\n\n### Brace placement\n\nTwo common styles exist for curly braces:\n\n- **Allman style** — opening brace on its own line (used in learncpp and this course)\n- **K&R style** — opening brace on the same line as the statement\n\nEither is fine. Pick one and stay consistent. Mixing styles in the same file is the only real mistake.\n\n### Indentation\n\nUse either spaces or tabs — not both. A common choice is 4 spaces per level. Most editors can insert spaces when you press Tab. C++ does not care which you pick, but your teammates will.\n\n### Line length\n\nKeep lines under roughly 80–100 characters. When a function call or expression runs long, break it after a comma or operator and indent the continuation:\n\n```\nstd::cout << \"First part: \"\n          << value\n          << '\\n';\n```\n\n### Blank lines and vertical spacing\n\nUse blank lines to group related statements — similar to paragraphs in prose. A blank line between variable declarations and the first statement, and between logical blocks, makes structure visible at a glance.\n\n### Comments and whitespace together\n\nFrom the Comments lesson: comments explain *why*, not *what*. Whitespace shows *structure*. Both are tools for the reader, who is usually future-you.\n\n### Operators\n\nPut spaces around binary operators (`+`, `-`, `=`, `<`, etc.) and after commas. Avoid spaces between a function name and its argument list (`std::cout` not `std::cout `).\n\n### Consistency over perfection\n\nNo style is objectively correct. What kills readability is inconsistency — a file that indents with 2 spaces in one function and 4 in another, or that places braces differently on adjacent functions. Modern codebases use formatters (clang-format, prettier) to enforce rules automatically. For now, adopt the conventions used in these lessons and apply them uniformly.\n\nThese conventions apply to every code example from here forward.",
      "exercises": [
        {
          "title": "Format the Mess",
          "prompt_md": "## Format the Mess\n\nYou are given three integers on a single line of stdin, separated by spaces. Read them and print a small formatted table showing each value and its square, aligned so the output matches the expected format exactly.\n\n**Input format:** Three integers on one line, e.g. `3 7 12`\n\n**Output format:** Three lines, each of the form `N squared is N*N`, where the numbers are printed with no extra padding.\n\n**Example**\n\nInput:\n```\n3 7 12\n```\n\nOutput:\n```\n3 squared is 9\n7 squared is 49\n12 squared is 144\n```\n\nRequirements:\n- Declare your variables one per line, each on its own line with consistent indentation.\n- Use `'\\n'` rather than `std::endl` for line endings.\n- Do not put any extra blank lines in the output.",
          "starter_code": "#include <iostream>\n\nint main()\n{\n    // TODO: declare three integer variables, one per line\n\n    // TODO: read all three from std::cin on one line\n\n    // TODO: print each value and its square in the required format\n    // e.g.  \"3 squared is 9\"\n\n    return 0;\n}\n",
          "solution_code": "#include <iostream>\n\nint main()\n{\n    int a{};\n    int b{};\n    int c{};\n\n    std::cin >> a >> b >> c;\n\n    std::cout << a << \" squared is \" << (a * a) << '\\n';\n    std::cout << b << \" squared is \" << (b * b) << '\\n';\n    std::cout << c << \" squared is \" << (c * c) << '\\n';\n\n    return 0;\n}\n",
          "test_cases": [
            {
              "label": "sample: small values",
              "stdin": "3 7 12",
              "expected_stdout": "3 squared is 9\n7 squared is 49\n12 squared is 144\n",
              "is_sample": true
            },
            {
              "label": "hidden: zeros and one",
              "stdin": "0 1 5",
              "expected_stdout": "0 squared is 0\n1 squared is 1\n5 squared is 25\n",
              "is_sample": false
            },
            {
              "label": "hidden: larger values",
              "stdin": "10 20 30",
              "expected_stdout": "10 squared is 100\n20 squared is 400\n30 squared is 900\n",
              "is_sample": false
            }
          ]
        },
        {
          "title": "Aligned Price List",
          "prompt_md": "## Aligned Price List\n\nRead three integers from stdin (on separate lines): the price in cents of three items. Print a small price list showing each item's price in a readable format.\n\nYou must print exactly three lines in this format:\n\n```\nItem 1: <price1> cents\nItem 2: <price2> cents\nItem 3: <price3> cents\n```\n\nThen print a blank line, followed by a total line:\n\n```\nTotal: <sum> cents\n```\n\nThis exercise practices clean formatting: consistent structure, one `std::cout` statement per line, and `'\\n'` for line endings.\n\n**Sample input:**\n```\n150\n275\n99\n```\n\n**Sample output:**\n```\nItem 1: 150 cents\nItem 2: 275 cents\nItem 3: 99 cents\n\nTotal: 524 cents\n```\n\nRequirements:\n- Use `std::cin` to read the input\n- Use `std::cout` with `'\\n'` for output\n- The blank line between the items and total is a single `'\\n'`\n- Use one declaration per line with consistent indentation",
          "starter_code": "#include <iostream>\n\nint main()\n{\n    // TODO: declare three integer variables for the prices\n\n    // TODO: read all three from std::cin (one per line)\n\n    // TODO: compute the total\n\n    // TODO: print each item line, a blank line, and the total line\n\n    return 0;\n}\n",
          "solution_code": "#include <iostream>\n\nint main()\n{\n    int price1{};\n    int price2{};\n    int price3{};\n\n    std::cin >> price1 >> price2 >> price3;\n\n    int total{ price1 + price2 + price3 };\n\n    std::cout << \"Item 1: \" << price1 << \" cents\" << '\\n';\n    std::cout << \"Item 2: \" << price2 << \" cents\" << '\\n';\n    std::cout << \"Item 3: \" << price3 << \" cents\" << '\\n';\n    std::cout << '\\n';\n    std::cout << \"Total: \" << total << \" cents\" << '\\n';\n\n    return 0;\n}\n",
          "test_cases": [
            {
              "label": "Sample: three items",
              "stdin": "150\n275\n99",
              "expected_stdout": "Item 1: 150 cents\nItem 2: 275 cents\nItem 3: 99 cents\n\nTotal: 524 cents\n",
              "is_sample": true
            },
            {
              "label": "Hidden: all same price",
              "stdin": "100\n100\n100",
              "expected_stdout": "Item 1: 100 cents\nItem 2: 100 cents\nItem 3: 100 cents\n\nTotal: 300 cents\n",
              "is_sample": false
            },
            {
              "label": "Hidden: includes zero",
              "stdin": "0\n500\n1",
              "expected_stdout": "Item 1: 0 cents\nItem 2: 500 cents\nItem 3: 1 cents\n\nTotal: 501 cents\n",
              "is_sample": false
            }
          ]
        }
      ]
    },
    {
      "number": "1.9",
      "title": "Introduction to literals and operators",
      "sort_order": 21,
      "summary_md": "## Literals and Operators\n\nA **literal** is a fixed value written directly in source code. You've already used literals in earlier lessons — when you wrote `int x = 5;` (from *Introduction to objects and variables*), the `5` is an integer literal. Literals are values the compiler bakes in at compile time; they can never be changed.\n\nAt this stage, the most important category is **integer literals** — e.g. `42`, `0`, `-7`. You will encounter other literal types (floating-point, character, string, boolean) in later chapters. For now, integer literals are all you need.\n\nAn **operator** takes one or more operands and produces a value. The most common are the arithmetic operators: `+`, `-`, `*`, `/`, and `%` (remainder). These work the same as Python with one key difference: **integer division truncates**. `7 / 2` gives `3`, not `3.5`. If you need the fractional result, at least one operand must be a floating-point value: `7.0 / 2` gives `3.5`.\n\nThe `%` operator gives the remainder after integer division: `7 % 2` gives `1`. It only works on integer types, unlike Python where `%` works on floats too.\n\nOperators also have **precedence** (order of evaluation). Multiplication and division bind tighter than addition and subtraction, just like in math. Use parentheses when the order matters or when the expression would otherwise be unclear.\n\n```cpp\n#include <iostream>\n\nint main() {\n    int apples = 17;\n    int bags   = 5;\n    int per_bag   = apples / bags;   // truncates: 3\n    int leftover  = apples % bags;   // remainder: 2\n    std::cout << \"per bag: \" << per_bag << \", leftover: \" << leftover << '\\n';\n}\n```\n\nNote that `17 / 5` gives `3`, not `3.4` — integer division always truncates. You will learn how to get fractional results in a later chapter.\n\nThe **unary minus** operator negates a value: `-x` flips the sign of `x` without modifying the variable (recall from *Variable assignment and initialization* that operators produce new values; they don't mutate the operand unless you use `=`).\n\nFrom Python you're used to `**` for exponentiation — C++ has no built-in operator for that. You will learn about library functions for exponentiation in a later chapter.\n",
      "exercises": [
        {
          "title": "Fruit Basket Division",
          "prompt_md": "A farmer has a basket of fruit to distribute evenly among workers.\n\nRead two integers from stdin (space-separated on one line): the total number of fruit, then the number of workers.\n\nPrint two lines:\n1. How many whole fruit each worker receives (integer division)\n2. How many fruit are left over (remainder)\n\n**Constraints:** both values are positive integers; workers >= 1.\n\n**Sample input:**\n```\n20 6\n```\n\n**Sample output:**\n```\nEach worker gets: 3\nLeftover fruit: 2\n```\n",
          "starter_code": "#include <iostream>\n\nint main() {\n    int fruit{};\n    int workers{};\n    std::cin >> fruit >> workers;\n\n    // TODO: compute whole fruit per worker using integer division\n    int per_worker{ 0 };\n\n    // TODO: compute leftover using the remainder operator %\n    int leftover{ 0 };\n\n    // TODO: print the two required lines\n    // Line 1: \"Each worker gets: <value>\"\n    // Line 2: \"Leftover fruit: <value>\"\n\n    return 0;\n}\n",
          "solution_code": "#include <iostream>\n\nint main() {\n    int fruit{};\n    int workers{};\n    std::cin >> fruit >> workers;\n\n    int per_worker{ fruit / workers };\n    int leftover{ fruit % workers };\n\n    std::cout << \"Each worker gets: \" << per_worker << '\\n';\n    std::cout << \"Leftover fruit: \" << leftover << '\\n';\n\n    return 0;\n}\n",
          "test_cases": [
            {
              "label": "sample: 20 fruit, 6 workers",
              "stdin": "20 6",
              "expected_stdout": "Each worker gets: 3\nLeftover fruit: 2\n",
              "is_sample": true
            },
            {
              "label": "hidden: exact division (no remainder)",
              "stdin": "100 4",
              "expected_stdout": "Each worker gets: 25\nLeftover fruit: 0\n",
              "is_sample": false
            },
            {
              "label": "hidden: 1 worker gets everything",
              "stdin": "7 1",
              "expected_stdout": "Each worker gets: 7\nLeftover fruit: 0\n",
              "is_sample": false
            }
          ]
        },
        {
          "title": "Ticket Price Calculator",
          "prompt_md": "A cinema charges different prices for adult and child tickets.\n\nRead three integers from stdin on one line: the price of an adult ticket (in cents), the price of a child ticket (in cents), and the total number of tickets sold. Then read the number of those tickets that are child tickets.\n\nCompute:\n- The number of adult tickets sold\n- The total revenue in cents\n- The total revenue in dollars and cents (e.g. 525 cents = 5 dollars and 25 cents)\n\nPrint exactly three lines as shown below.\n\n**Sample input:**\n```\n1200 700 10\n3\n```\n\n**Sample output:**\n```\nAdult tickets: 7\nTotal revenue (cents): 10500\nTotal revenue: 105 dollars and 0 cents\n```\n\n**Constraints:** all values are non-negative integers; child_tickets <= total_tickets; prices >= 0.\n",
          "starter_code": "#include <iostream>\n\nint main() {\n    int adult_price{};\n    int child_price{};\n    int total_tickets{};\n    std::cin >> adult_price >> child_price >> total_tickets;\n\n    int child_tickets{};\n    std::cin >> child_tickets;\n\n    // TODO: compute the number of adult tickets\n    int adult_tickets{ 0 };\n\n    // TODO: compute total revenue in cents\n    int revenue_cents{ 0 };\n\n    // TODO: compute whole dollars using integer division by 100\n    int dollars{ 0 };\n\n    // TODO: compute remaining cents using the remainder operator\n    int remaining_cents{ 0 };\n\n    // TODO: print three lines:\n    // \"Adult tickets: <value>\"\n    // \"Total revenue (cents): <value>\"\n    // \"Total revenue: <dollars> dollars and <cents> cents\"\n\n    return 0;\n}\n",
          "solution_code": "#include <iostream>\n\nint main() {\n    int adult_price{};\n    int child_price{};\n    int total_tickets{};\n    std::cin >> adult_price >> child_price >> total_tickets;\n\n    int child_tickets{};\n    std::cin >> child_tickets;\n\n    int adult_tickets{ total_tickets - child_tickets };\n    int revenue_cents{ adult_tickets * adult_price + child_tickets * child_price };\n    int dollars{ revenue_cents / 100 };\n    int remaining_cents{ revenue_cents % 100 };\n\n    std::cout << \"Adult tickets: \" << adult_tickets << '\\n';\n    std::cout << \"Total revenue (cents): \" << revenue_cents << '\\n';\n    std::cout << \"Total revenue: \" << dollars << \" dollars and \" << remaining_cents << \" cents\" << '\\n';\n\n    return 0;\n}\n",
          "test_cases": [
            {
              "label": "sample: mixed tickets",
              "stdin": "1200 700 10\n3",
              "expected_stdout": "Adult tickets: 7\nTotal revenue (cents): 10500\nTotal revenue: 105 dollars and 0 cents\n",
              "is_sample": true
            },
            {
              "label": "hidden: all child tickets",
              "stdin": "1500 800 5\n5",
              "expected_stdout": "Adult tickets: 0\nTotal revenue (cents): 4000\nTotal revenue: 40 dollars and 0 cents\n",
              "is_sample": false
            },
            {
              "label": "hidden: revenue with non-zero remaining cents",
              "stdin": "999 499 4\n1",
              "expected_stdout": "Adult tickets: 3\nTotal revenue (cents): 3496\nTotal revenue: 34 dollars and 96 cents\n",
              "is_sample": false
            }
          ]
        }
      ]
    },
    {
      "number": "1.10",
      "title": "Introduction to expressions",
      "sort_order": 22,
      "summary_md": "## Introduction to Expressions\n\nAn **expression** is a combination of literals, variables, operators, and function calls that evaluates to a single value. That resulting value is called the expression's **value**. Every expression has a type, determined at compile time.\n\nYou already use expressions in Python constantly — `2 + 3`, `x * y`, `len(s) > 0` are all expressions. C++ works the same way, but the rules around types are stricter.\n\n### Expressions vs. Statements\n\nFrom \"Statements and the structure of a program\": a statement is a complete instruction that the program executes. An expression becomes an **expression statement** when you append a semicolon:\n\n```cpp\n#include <iostream>\n\nint main() {\n    int x { 3 };\n    int y { 4 };\n\n    int hyp_sq { x * x + y * y };   // expression: x*x + y*y\n    std::cout << hyp_sq << '\\n';     // prints 25\n\n    x = x + 1;                       // expression statement: x+1 evaluated, result assigned\n    std::cout << x << '\\n';          // prints 4\n}\n```\n\n### Subexpressions\n\nExpressions can contain other expressions — these nested pieces are called **subexpressions**. In `x * x + y * y`, the compiler evaluates `x * x` and `y * y` as subexpressions first, then adds the results. This nesting is what makes expressions composable.\n\nThe compiler is free to evaluate subexpressions in any order unless the operator imposes a specific sequence. Avoid writing expressions where the result depends on evaluation order — that's undefined behavior.\n\n### Operands and Operators\n\nFrom \"Introduction to literals and operators\": operators take **operands** and produce a new value. The type of the result depends on the types of the operands. For now, all your operands are `int`, so all results are `int`. Be aware that integer division truncates: `7 / 2` gives `3`, not `3.5`.\n\n### Values Are Not Always Used\n\nSometimes an expression's value is discarded — `x = x + 1;` computes `x + 1`, assigns it to `x`, then throws away the value of the assignment expression itself. This is normal and expected for side-effectful statements.\n\n### Key Takeaway\n\nEvery piece of computation in C++ — whether it's arithmetic, a comparison, or even calling `main` — is an expression. Understanding that expressions evaluate to typed values, and that complex expressions are built from simpler subexpressions, is the mental model for reading and writing any C++ code.",
      "exercises": [
        {
          "title": "Expression Type Detective",
          "prompt_md": "## Expression Type Detective\n\nWrite a program that reads two integers `a` and `b` from stdin (space-separated on one line), then prints the results of the following four expressions, each on its own line:\n\n1. `a + b` (integer addition)\n2. `a * b - 1` (product minus one)\n3. `a / b` (integer division — note truncation)\n4. `(a + b) * (a - b)` (difference of squares)\n\nEach output line should be just the integer result with no extra text.\n\n**Constraints:** `b != 0`, both values fit in `int`.\n\n**Example:**\nInput: `7 2`\nOutput:\n```\n9\n13\n3\n45\n```",
          "starter_code": "#include <iostream>\n\nint main() {\n    int a {}, b {};\n    std::cin >> a >> b;\n\n    // TODO: compute and print a + b\n\n    // TODO: compute and print a * b - 1\n\n    // TODO: compute and print a / b  (integer division)\n\n    // TODO: compute and print (a + b) * (a - b)\n\n    return 0;\n}\n",
          "solution_code": "#include <iostream>\n\nint main() {\n    int a {}, b {};\n    std::cin >> a >> b;\n\n    std::cout << (a + b) << '\\n';\n    std::cout << (a * b - 1) << '\\n';\n    std::cout << (a / b) << '\\n';\n    std::cout << ((a + b) * (a - b)) << '\\n';\n\n    return 0;\n}\n",
          "test_cases": [
            {
              "label": "Sample: 7 and 2",
              "stdin": "7 2",
              "expected_stdout": "9\n13\n3\n45\n",
              "is_sample": true
            },
            {
              "label": "Hidden: negative values",
              "stdin": "-3 4",
              "expected_stdout": "1\n-13\n0\n-7\n",
              "is_sample": false
            },
            {
              "label": "Hidden: equal values",
              "stdin": "5 5",
              "expected_stdout": "10\n24\n1\n0\n",
              "is_sample": false
            }
          ]
        },
        {
          "title": "Subexpression Breakdown",
          "prompt_md": "## Subexpression Breakdown\n\nWrite a program that reads three integers `x`, `y`, and `z` from stdin (space-separated on one line) and prints the results of the following three expressions, each on its own line:\n\n1. The **sum of squares**: `x*x + y*y + z*z`\n2. The **nested product**: `(x + y) * (y + z)`\n3. The **mixed expression**: `x * y + y * z - x * z`\n\nThese all combine subexpressions in different ways — tracing through each one manually before coding is the point of the exercise.\n\nEach output line should be just the integer result.\n\n**Example:**\nInput: `2 3 4`\nOutput:\n```\n29\n35\n10\n```",
          "starter_code": "#include <iostream>\n\nint main() {\n    int x {}, y {}, z {};\n    std::cin >> x >> y >> z;\n\n    // TODO: compute and print x*x + y*y + z*z\n\n    // TODO: compute and print (x + y) * (y + z)\n\n    // TODO: compute and print x*y + y*z - x*z\n\n    return 0;\n}\n",
          "solution_code": "#include <iostream>\n\nint main() {\n    int x {}, y {}, z {};\n    std::cin >> x >> y >> z;\n\n    std::cout << (x*x + y*y + z*z) << '\\n';\n    std::cout << ((x + y) * (y + z)) << '\\n';\n    std::cout << (x*y + y*z - x*z) << '\\n';\n\n    return 0;\n}\n",
          "test_cases": [
            {
              "label": "Sample: 2, 3, 4",
              "stdin": "2 3 4",
              "expected_stdout": "29\n35\n10\n",
              "is_sample": true
            },
            {
              "label": "Hidden: zeros and negatives",
              "stdin": "0 -2 5",
              "expected_stdout": "29\n-6\n-10\n",
              "is_sample": false
            },
            {
              "label": "Hidden: all same value",
              "stdin": "3 3 3",
              "expected_stdout": "27\n36\n9\n",
              "is_sample": false
            }
          ]
        }
      ]
    },
    {
      "number": "1.11",
      "title": "Developing your first program",
      "sort_order": 23,
      "summary_md": "## Developing Your First Program\n\nAt this point you have all the individual pieces: statements, variables, initialization, `std::cin`/`std::cout`, operators, and expressions. This lesson is about combining them into a complete, working program.\n\n### The development loop\n\nWriting a program is iterative. The cycle is:\n\n1. **Write** a small piece of code that does one thing.\n2. **Compile** — fix any errors the compiler reports before adding more code.\n3. **Run** — verify the output matches what you expected.\n4. **Extend** — add the next small piece.\n\nResist the urge to write everything at once. In Python you can run a single expression in the REPL and see results immediately. In C++ the compiler is your REPL: keep each compilation unit small and correct before moving on.\n\n### Putting the pieces together\n\nA complete program needs:\n- A `#include` for every facility you use (for now, just `<iostream>`).\n- Exactly one `main()` that returns `int`.\n- Variables initialized before use (see *Introduction to objects and variables* and *Uninitialized variables and undefined behavior*).\n- `std::cin` for input, `std::cout` for output (see *Introduction to iostream*).\n- Meaningful names following the rules from *Keywords and naming identifiers*.\n\n```cpp\n#include <iostream>\n\nint main()\n{\n    int width{};\n    int height{};\n    std::cout << \"Enter width and height: \";\n    std::cin >> width >> height;\n    int area{ width * height };\n    std::cout << \"Area: \" << area << '\\n';\n    return 0;\n}\n```\n\n### Reading the compiler's output\n\nCompiler errors point to the exact line where the problem was detected, but the *cause* is often one or two lines earlier. Read the first error first — later errors are often cascades from the first.\n\nWarnings (`-Wall -Wextra`) catch real bugs: uninitialized variables, unused results, signed/unsigned mismatches. Treat every warning as an error until you understand why it fired.\n\n### Formatting and whitespace\n\nConsistent indentation (see *Whitespace and basic formatting*) makes the structure visible to both you and the compiler error messages. Pick 4 spaces or a tab and use it everywhere.\n\n### One responsibility per variable\n\nFrom *Introduction to expressions*: expressions produce values. Assign those values to well-named variables rather than nesting them deeply. `int area{ width * height }` is easier to debug than `std::cout << width * height`.\n\nStart small, compile often, name things clearly. That is the complete recipe.",
      "exercises": [
        {
          "title": "Temperature Converter",
          "prompt_md": "## Temperature Converter\n\nWrite a program that reads a temperature in Celsius from standard input and prints the equivalent in Fahrenheit, followed by a newline.\n\nThe conversion formula is: `F = C * 9 / 5 + 32`\n\nUse integer arithmetic throughout (no floating-point types).\n\n**Input format:** A single integer on one line.\n\n**Output format:** A single line in the form `X degrees C = Y degrees F` where `X` is the input and `Y` is the result.\n\n**Example:**\n```\nInput:  100\nOutput: 100 degrees C = 212 degrees F\n```",
          "starter_code": "#include <iostream>\n\nint main()\n{\n    // TODO: declare an integer variable for the Celsius temperature\n\n    // TODO: read the temperature from std::cin\n\n    // TODO: compute the Fahrenheit temperature using integer arithmetic\n    //       formula: F = C * 9 / 5 + 32\n\n    // TODO: print the result in the form:\n    //       \"X degrees C = Y degrees F\"\n\n    return 0;\n}\n",
          "solution_code": "#include <iostream>\n\nint main()\n{\n    int celsius{};\n    std::cin >> celsius;\n    int fahrenheit{ celsius * 9 / 5 + 32 };\n    std::cout << celsius << \" degrees C = \" << fahrenheit << \" degrees F\" << '\\n';\n    return 0;\n}\n",
          "test_cases": [
            {
              "label": "Boiling point",
              "stdin": "100\n",
              "expected_stdout": "100 degrees C = 212 degrees F\n",
              "is_sample": true
            },
            {
              "label": "Freezing point",
              "stdin": "0\n",
              "expected_stdout": "0 degrees C = 32 degrees F\n",
              "is_sample": false
            },
            {
              "label": "Body temperature",
              "stdin": "37\n",
              "expected_stdout": "37 degrees C = 98 degrees F\n",
              "is_sample": false
            }
          ]
        },
        {
          "title": "Simple Bill Splitter",
          "prompt_md": "## Simple Bill Splitter\n\nWrite a program that reads a total bill amount (in whole dollars) and the number of people splitting it, then prints how much each person owes and how many dollars are left over after an even integer split.\n\nUse integer division and the modulo operator.\n\n**Input format:** Two integers on separate lines: the total bill, then the number of people.\n\n**Output format:** Two lines:\n1. `Each person owes $X`\n2. `Remainder: $Y`\n\n**Example:**\n```\nInput:\n100\n3\n\nOutput:\nEach person owes $33\nRemainder: $1\n```\n\nAssume both inputs are positive integers and the number of people is at least 1.",
          "starter_code": "#include <iostream>\n\nint main()\n{\n    // TODO: declare integer variables for the total bill and number of people\n\n    // TODO: read both values from std::cin\n\n    // TODO: compute each person's share using integer division\n\n    // TODO: compute the remainder using the modulo operator\n\n    // TODO: print the two result lines:\n    //       \"Each person owes $X\"\n    //       \"Remainder: $Y\"\n\n    return 0;\n}\n",
          "solution_code": "#include <iostream>\n\nint main()\n{\n    int total{};\n    int people{};\n    std::cin >> total >> people;\n    int share{ total / people };\n    int remainder{ total % people };\n    std::cout << \"Each person owes $\" << share << '\\n';\n    std::cout << \"Remainder: $\" << remainder << '\\n';\n    return 0;\n}\n",
          "test_cases": [
            {
              "label": "Uneven split",
              "stdin": "100\n3\n",
              "expected_stdout": "Each person owes $33\nRemainder: $1\n",
              "is_sample": true
            },
            {
              "label": "Even split",
              "stdin": "60\n4\n",
              "expected_stdout": "Each person owes $15\nRemainder: $0\n",
              "is_sample": false
            },
            {
              "label": "Single person",
              "stdin": "47\n1\n",
              "expected_stdout": "Each person owes $47\nRemainder: $0\n",
              "is_sample": false
            }
          ]
        }
      ]
    },
    {
      "number": "1.x",
      "title": "Chapter 1 summary and quiz",
      "sort_order": 24,
      "summary_md": "## Chapter 1 Summary\n\nThis chapter covered the foundational building blocks of C++ programs. Here is a condensed review of each topic.\n\n**Statements and program structure** — A C++ program is a collection of functions; execution starts at `main()`, which must return `int`. Every statement ends with a semicolon. Unlike Python, whitespace and indentation are cosmetic, not syntactic.\n\n**Comments** — `//` for single-line comments, `/* */` for block comments. Prefer explaining *why* over *what*; the code already says what.\n\n**Objects and variables** — A variable is a named region of memory that holds a value of a specific type. In C++ you must declare the type explicitly: `int x;`. Python infers types at runtime; C++ binds them at compile time.\n\n**Assignment and initialization** — Prefer brace (list) initialization: `int x{ 5 };`. It catches narrowing conversions at compile time that `=` silently allows. Copy initialization (`int x = 5;`) still works but is less safe. Direct initialization (`int x(5);`) is mostly legacy.\n\n**`iostream`: `cout`, `cin`, `endl`** — `std::cout` prints to stdout using `<<`; `std::cin` reads from stdin using `>>`. Use `'\\n'` over `std::endl` — `endl` flushes the buffer every time, which is slow in loops.\n\n**Uninitialized variables and undefined behavior** — Reading an uninitialized variable is undefined behavior (UB). UB can silently corrupt memory, crash the process, or produce wrong output that looks right. Always initialize every variable at the point of declaration.\n\n**Keywords and identifiers** — C++ reserves ~100 keywords (`int`, `return`, etc.). Identifier rules: start with a letter or underscore, followed by letters, digits, or underscores. `camelCase` or `snake_case` are both common; pick one and stick to it.\n\n**Whitespace and formatting** — The compiler ignores most whitespace. Consistent indentation (4 spaces is idiomatic C++) makes code readable. Auto-format with `clang-format` rather than arguing about style.\n\n**Literals and operators** — A literal is a hard-coded value (e.g. `42`, `0`, `-7`). Operators act on operands to produce a result. Operator precedence and associativity determine evaluation order; use parentheses to make intent explicit.\n\n**Expressions** — An expression is anything that evaluates to a value. `2 + 3` is an expression; so is `x = 5` (assignment returns the assigned value). An expression followed by `;` becomes an expression statement.\n\n**Developing your first program** — Build incrementally: write a small piece, compile, test, then extend. Read compiler errors top-to-bottom; the first error usually causes the rest.\n\n### Putting it together\n\n```cpp\n#include <iostream>\n\nint main()\n{\n    // Declare and initialize with brace initialization\n    int apples{ 0 };\n    int price_cents{ 0 };\n\n    std::cout << \"Enter number of apples and price (cents) each: \";\n    std::cin >> apples >> price_cents;\n\n    // Expression: combines variables and arithmetic operator\n    int total_cents{ apples * price_cents };\n    std::cout << \"Total: \" << total_cents << \" cents\" << '\\n';\n\n    return 0;\n}\n```\n\nThis single program touches variables, brace initialization, `iostream`, expressions, and the `'\\n'` vs `std::endl` distinction from the chapter.",
      "exercises": [
        {
          "title": "Time Breakdown",
          "prompt_md": "## Time Breakdown\n\nWrite a program that reads a total number of seconds (a positive integer) from stdin and breaks it down into hours, minutes, and remaining seconds.\n\nRecall:\n- 1 minute = 60 seconds\n- 1 hour = 3600 seconds\n\nUse integer division (`/`) and the remainder operator (`%`) to extract each component.\n\nPrint exactly three lines in this format:\n\n```\nHours: <value>\nMinutes: <value>\nSeconds: <value>\n```\n\n### Example\n\n**Input:**\n```\n3661\n```\n\n**Output:**\n```\nHours: 1\nMinutes: 1\nSeconds: 1\n```\n\n(3661 = 1*3600 + 1*60 + 1)\n\nRequirements:\n- Use `std::cin` to read input\n- Use `std::cout` with `'\\n'` for output\n- Initialize all variables at the point of declaration",
          "starter_code": "#include <iostream>\n\nint main() {\n    int total_seconds{};\n    std::cin >> total_seconds;\n\n    // TODO: compute hours using integer division by 3600\n    // TODO: compute the remaining seconds after removing hours\n    // TODO: compute minutes from the remaining seconds\n    // TODO: compute the final leftover seconds\n\n    // TODO: print Hours, Minutes, and Seconds\n\n    return 0;\n}\n",
          "solution_code": "#include <iostream>\n\nint main() {\n    int total_seconds{};\n    std::cin >> total_seconds;\n\n    int hours{ total_seconds / 3600 };\n    int remaining{ total_seconds % 3600 };\n    int minutes{ remaining / 60 };\n    int seconds{ remaining % 60 };\n\n    std::cout << \"Hours: \" << hours << '\\n';\n    std::cout << \"Minutes: \" << minutes << '\\n';\n    std::cout << \"Seconds: \" << seconds << '\\n';\n\n    return 0;\n}\n",
          "test_cases": [
            {
              "label": "Sample: 3661 seconds",
              "stdin": "3661",
              "expected_stdout": "Hours: 1\nMinutes: 1\nSeconds: 1\n",
              "is_sample": true
            },
            {
              "label": "Hidden: exactly one hour",
              "stdin": "3600",
              "expected_stdout": "Hours: 1\nMinutes: 0\nSeconds: 0\n",
              "is_sample": false
            },
            {
              "label": "Hidden: less than a minute",
              "stdin": "45",
              "expected_stdout": "Hours: 0\nMinutes: 0\nSeconds: 45\n",
              "is_sample": false
            }
          ]
        },
        {
          "title": "Rectangle Geometry",
          "prompt_md": "## Rectangle Geometry\n\nWrite a program that reads the integer width and height of a rectangle from stdin (on one line, space-separated), then prints two lines:\n\n```\nPerimeter: <value>\nArea: <value>\n```\n\nFormulas:\n- Perimeter = 2 * (width + height)\n- Area = width * height\n\nRules:\n- Width and height are positive integers.\n- Use `int` for all variables.\n- Initialize every variable at the point of declaration.\n\n**Sample input:**\n```\n4 7\n```\n\n**Sample output:**\n```\nPerimeter: 22\nArea: 28\n```",
          "starter_code": "#include <iostream>\n\nint main()\n{\n    // TODO: declare and brace-initialize width and height as int\n\n    // TODO: read width and height from std::cin\n\n    // TODO: compute perimeter (2 * (width + height)) and area (width * height)\n\n    // TODO: print the two output lines\n\n    return 0;\n}\n",
          "solution_code": "#include <iostream>\n\nint main()\n{\n    int width{ 0 };\n    int height{ 0 };\n\n    std::cin >> width >> height;\n\n    int perimeter{ 2 * (width + height) };\n    int area{ width * height };\n\n    std::cout << \"Perimeter: \" << perimeter << '\\n';\n    std::cout << \"Area: \" << area << '\\n';\n\n    return 0;\n}\n",
          "test_cases": [
            {
              "label": "Sample: 4x7 rectangle",
              "stdin": "4 7",
              "expected_stdout": "Perimeter: 22\nArea: 28\n",
              "is_sample": true
            },
            {
              "label": "Hidden: 5x5 square",
              "stdin": "5 5",
              "expected_stdout": "Perimeter: 20\nArea: 25\n",
              "is_sample": false
            },
            {
              "label": "Hidden: 1x1000",
              "stdin": "1 1000",
              "expected_stdout": "Perimeter: 2002\nArea: 1000\n",
              "is_sample": false
            }
          ]
        },
        {
          "title": "Digit Extractor",
          "prompt_md": "## Digit Extractor\n\nWrite a program that reads a three-digit positive integer from stdin and prints each digit separately.\n\nUse integer division (`/`) and remainder (`%`) to extract the hundreds, tens, and ones digits.\n\nFor the number `537`:\n- Hundreds digit: `537 / 100` = `5`\n- Tens digit: `537 / 10 % 10` = `3`\n- Ones digit: `537 % 10` = `7`\n\nPrint four lines in exactly this format:\n\n```\nNumber: <input>\nHundreds: <value>\nTens: <value>\nOnes: <value>\n```\n\n### Example\n\n**Input:**\n```\n537\n```\n\n**Output:**\n```\nNumber: 537\nHundreds: 5\nTens: 3\nOnes: 7\n```\n\nRequirements:\n- The input is always a three-digit number (100-999)\n- Use `std::cin` for input and `std::cout` with `'\\n'` for output\n- Initialize all variables at the point of declaration",
          "starter_code": "#include <iostream>\n\nint main() {\n    int number{};\n    std::cin >> number;\n\n    // TODO: extract the hundreds digit using integer division by 100\n    // TODO: extract the tens digit using division by 10 then remainder by 10\n    // TODO: extract the ones digit using remainder by 10\n\n    // TODO: print the four output lines\n\n    return 0;\n}\n",
          "solution_code": "#include <iostream>\n\nint main() {\n    int number{};\n    std::cin >> number;\n\n    int hundreds{ number / 100 };\n    int tens{ number / 10 % 10 };\n    int ones{ number % 10 };\n\n    std::cout << \"Number: \" << number << '\\n';\n    std::cout << \"Hundreds: \" << hundreds << '\\n';\n    std::cout << \"Tens: \" << tens << '\\n';\n    std::cout << \"Ones: \" << ones << '\\n';\n\n    return 0;\n}\n",
          "test_cases": [
            {
              "label": "Sample: 537",
              "stdin": "537",
              "expected_stdout": "Number: 537\nHundreds: 5\nTens: 3\nOnes: 7\n",
              "is_sample": true
            },
            {
              "label": "Hidden: 100",
              "stdin": "100",
              "expected_stdout": "Number: 100\nHundreds: 1\nTens: 0\nOnes: 0\n",
              "is_sample": false
            },
            {
              "label": "Hidden: 999",
              "stdin": "999",
              "expected_stdout": "Number: 999\nHundreds: 9\nTens: 9\nOnes: 9\n",
              "is_sample": false
            }
          ]
        },
        {
          "title": "Money Breakdown",
          "prompt_md": "## Money Breakdown\n\nWrite a program that reads a total amount in cents (a positive integer) from stdin and breaks it down into the fewest number of coins: quarters (25c), dimes (10c), nickels (5c), and pennies (1c).\n\nUse integer division (`/`) and remainder (`%`) to compute each coin count.\n\nPrint five lines in exactly this format:\n\n```\nTotal: <input> cents\nQuarters: <value>\nDimes: <value>\nNickels: <value>\nPennies: <value>\n```\n\n### Example\n\n**Input:**\n```\n87\n```\n\n**Output:**\n```\nTotal: 87 cents\nQuarters: 3\nDimes: 1\nNickels: 0\nPennies: 2\n```\n\n(87 = 3*25 + 1*10 + 0*5 + 2*1)\n\nRequirements:\n- Use `std::cin` for input and `std::cout` with `'\\n'` for output\n- Initialize all variables at the point of declaration\n- Use descriptive variable names",
          "starter_code": "#include <iostream>\n\nint main() {\n    int total_cents{};\n    std::cin >> total_cents;\n\n    // TODO: compute quarters, then update remaining cents\n    // TODO: compute dimes, then update remaining cents\n    // TODO: compute nickels, then pennies are whatever is left\n\n    // TODO: print the five output lines\n\n    return 0;\n}\n",
          "solution_code": "#include <iostream>\n\nint main() {\n    int total_cents{};\n    std::cin >> total_cents;\n\n    int quarters{ total_cents / 25 };\n    int remaining{ total_cents % 25 };\n\n    int dimes{ remaining / 10 };\n    remaining = remaining % 10;\n\n    int nickels{ remaining / 5 };\n    int pennies{ remaining % 5 };\n\n    std::cout << \"Total: \" << total_cents << \" cents\" << '\\n';\n    std::cout << \"Quarters: \" << quarters << '\\n';\n    std::cout << \"Dimes: \" << dimes << '\\n';\n    std::cout << \"Nickels: \" << nickels << '\\n';\n    std::cout << \"Pennies: \" << pennies << '\\n';\n\n    return 0;\n}\n",
          "test_cases": [
            {
              "label": "Sample: 87 cents",
              "stdin": "87",
              "expected_stdout": "Total: 87 cents\nQuarters: 3\nDimes: 1\nNickels: 0\nPennies: 2\n",
              "is_sample": true
            },
            {
              "label": "Hidden: 1 cent",
              "stdin": "1",
              "expected_stdout": "Total: 1 cents\nQuarters: 0\nDimes: 0\nNickels: 0\nPennies: 1\n",
              "is_sample": false
            },
            {
              "label": "Hidden: exact dollar",
              "stdin": "100",
              "expected_stdout": "Total: 100 cents\nQuarters: 4\nDimes: 0\nNickels: 0\nPennies: 0\n",
              "is_sample": false
            }
          ]
        }
      ]
    }
  ]
}