#include <iostream>
#include <string>

// TODO: Define struct Color with r, g, b (all default 0).

// TODO: Define struct Tile with name, x, y, color, passable.

// TODO: Write void printTile(const Tile& t).

int main() {
    // TODO: Create grass and water tiles and print them.
    return 0;
}
