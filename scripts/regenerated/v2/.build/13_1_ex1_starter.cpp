#include <iostream>

// TODO: Define a program-defined type Direction with the values:
//       north, east, south, west (in that exact order)

int main()
{
    int d {};
    std::cin >> d;

    // TODO: Declare a Direction variable and set it based on d
    // TODO: Print the integer value of that Direction, followed by '\n'

    return 0;
}
