[
  {
    "title": "Selection Sort on Integer Array",
    "prompt_md": "## Goal\n\nSort an array of integers into **ascending** order using selection sort, then print the result.\n\n## Input\n\n- Line 1: an integer `n` (the number of elements, 1 <= n <= 20)\n- Line 2: `n` space-separated integers (may be negative)\n\n## Output\n\nThe `n` integers in ascending order, each followed by a single space (so there is a trailing space after the last value).\n\n## Constraint\n\nImplement the sort yourself with your own loops. Do **not** call `std::sort` or any other library sorting function.",
    "starter_code": "#include <iostream>\n#include <utility>\n\nint main()\n{\n    int n{};\n    std::cin >> n;\n\n    int arr[20]{};\n    for (int i{ 0 }; i < n; ++i)\n        std::cin >> arr[i];\n\n    // TODO: Selection sort -- repeatedly find the smallest remaining element and\n    //       grow a sorted region on the left. Implement it with your own loops.\n    // TODO: Outer loop -- step a 'start' index across every position that still\n    //       needs the next-smallest value placed into it. Which positions are those?\n    // TODO: At the top of each pass, remember the index of the smallest value seen\n    //       so far in the unsorted region arr[start..n-1]; start by assuming 'start'.\n    // TODO: Inner loop -- scan the rest of the unsorted region and update that\n    //       remembered index whenever an element is smaller than the current best.\n    // TODO: After the inner loop, move that smallest element into position 'start'\n    //       by swapping the two elements (the <utility> header offers a swap helper).\n\n    for (int i{ 0 }; i < n; ++i)\n        std::cout << arr[i] << ' ';\n\n    return 0;\n}\n",
    "solution_code": "#include <iostream>\n#include <utility>\n\nint main()\n{\n    int n{};\n    std::cin >> n;\n\n    int arr[20]{};\n    for (int i{ 0 }; i < n; ++i)\n        std::cin >> arr[i];\n\n    for (int start{ 0 }; start < n - 1; ++start)\n    {\n        int minIndex{ start };\n        for (int cur{ start + 1 }; cur < n; ++cur)\n        {\n            if (arr[cur] < arr[minIndex])\n                minIndex = cur;\n        }\n        std::swap(arr[start], arr[minIndex]);\n    }\n\n    for (int i{ 0 }; i < n; ++i)\n        std::cout << arr[i] << ' ';\n\n    return 0;\n}\n",
    "difficulty": "practice",
    "test_cases": [
      {
        "label": "Basic unsorted array",
        "is_sample": true,
        "stdin": "5\n30 50 20 10 40",
        "expected_stdout": "10 20 30 40 50 "
      },
      {
        "label": "Already sorted array",
        "is_sample": false,
        "stdin": "4\n1 2 3 4",
        "expected_stdout": "1 2 3 4 "
      },
      {
        "label": "Reverse sorted with duplicates",
        "is_sample": false,
        "stdin": "6\n9 7 5 5 3 1",
        "expected_stdout": "1 3 5 5 7 9 "
      },
      {
        "label": "Includes negatives",
        "is_sample": false,
        "stdin": "5\n-3 10 -7 0 4",
        "expected_stdout": "-7 -3 0 4 10 "
      },
      {
        "label": "Single element",
        "is_sample": false,
        "stdin": "1\n42",
        "expected_stdout": "42 "
      }
    ]
  },
  {
    "title": "Selection Sort a Vector of Structs by Field",
    "prompt_md": "## Goal\n\nRead a list of students (each with a name and a score), then sort them by score in **descending** order (highest score first) using selection sort. Print the sorted list.\n\nA `struct Student` and the input scaffolding are already provided.\n\n## Input\n\n- Line 1: an integer `n` (number of students, 1 <= n <= 15)\n- Next `n` lines: a name (a single word, no spaces) followed by an integer score\n\n## Output\n\n`n` lines, each containing the name and score separated by a space, ordered from highest score to lowest. All scores in the test data are distinct.\n\n## Constraint\n\nImplement the sort yourself with your own loops. Do **not** call `std::sort` or any other library sorting function.",
    "starter_code": "#include <iostream>\n#include <string>\n#include <utility>\n#include <vector>\n\nstruct Student\n{\n    std::string name{};\n    int score{};\n};\n\nint main()\n{\n    int n{};\n    std::cin >> n;\n\n    std::vector<Student> students(n);\n    for (int i{ 0 }; i < n; ++i)\n        std::cin >> students[i].name >> students[i].score;\n\n    // TODO: Selection sort by score, but for DESCENDING order. Repeatedly find the\n    //       highest remaining score and grow a sorted region on the left.\n    // TODO: Outer loop -- step a 'start' index across each position that still needs\n    //       the next-highest student placed into it.\n    // TODO: Each pass, remember the index of the highest score seen so far in the\n    //       unsorted region students[start..n-1]; begin by assuming it is 'start'.\n    // TODO: Inner loop -- scan the remaining students and update that index whenever\n    //       you find a LARGER .score (which comparison gives you descending order?).\n    // TODO: Swap the highest-scoring student into position 'start' (the whole struct\n    //       moves together; <utility> provides a swap helper).\n\n    for (int i{ 0 }; i < n; ++i)\n        std::cout << students[i].name << ' ' << students[i].score << '\\n';\n\n    return 0;\n}\n",
    "solution_code": "#include <iostream>\n#include <string>\n#include <utility>\n#include <vector>\n\nstruct Student\n{\n    std::string name{};\n    int score{};\n};\n\nint main()\n{\n    int n{};\n    std::cin >> n;\n\n    std::vector<Student> students(n);\n    for (int i{ 0 }; i < n; ++i)\n        std::cin >> students[i].name >> students[i].score;\n\n    for (int start{ 0 }; start < n - 1; ++start)\n    {\n        int maxIndex{ start };\n        for (int cur{ start + 1 }; cur < n; ++cur)\n        {\n            if (students[cur].score > students[maxIndex].score)\n                maxIndex = cur;\n        }\n        std::swap(students[start], students[maxIndex]);\n    }\n\n    for (int i{ 0 }; i < n; ++i)\n        std::cout << students[i].name << ' ' << students[i].score << '\\n';\n\n    return 0;\n}\n",
    "difficulty": "practice",
    "test_cases": [
      {
        "label": "Basic student list",
        "is_sample": true,
        "stdin": "3\nAlice 85\nBob 92\nCarla 78",
        "expected_stdout": "Bob 92\nAlice 85\nCarla 78\n"
      },
      {
        "label": "Single student",
        "is_sample": false,
        "stdin": "1\nZara 100",
        "expected_stdout": "Zara 100\n"
      },
      {
        "label": "Already in descending order",
        "is_sample": false,
        "stdin": "4\nEve 99\nDan 75\nCal 60\nAmy 42",
        "expected_stdout": "Eve 99\nDan 75\nCal 60\nAmy 42\n"
      },
      {
        "label": "Input in ascending order forces a real sort",
        "is_sample": false,
        "stdin": "4\nAmy 10\nBen 20\nCy 30\nDeb 40",
        "expected_stdout": "Deb 40\nCy 30\nBen 20\nAmy 10\n"
      },
      {
        "label": "Negative scores mixed in",
        "is_sample": false,
        "stdin": "5\nNan -5\nObi 12\nPam -1\nQuin 7\nRoy 0",
        "expected_stdout": "Obi 12\nQuin 7\nRoy 0\nPam -1\nNan -5\n"
      }
    ]
  }
]
