[
  {
    "title": "Cast and Print via Void Pointer",
    "prompt_md": "## Goal\n\nA `void*` can hold the address of any object but knows nothing about its type, so you cannot read a value through it until you cast it back to the right pointer type. Take an `int` and a `double`, route each of their addresses through a `void*`, then recover and print the values.\n\n## Input\n\n- Line 1: an integer.\n- Line 2: a double.\n\n## Output\n\n- Line 1: `int: ` followed by the integer value.\n- Line 2: `double: ` followed by the double value with exactly 2 decimal places.\n\n## Constraints\n\n- The values must be reached through a `void*` and recovered with `static_cast` to the correct typed pointer before dereferencing. Format the double with `std::fixed` and `std::setprecision(2)` from `<iomanip>`.",
    "starter_code": "#include <iostream>\n#include <iomanip>\n\nint main()\n{\n    int iVal{};\n    double dVal{};\n    std::cin >> iVal >> dVal;\n\n    // TODO: store the address of iVal in a void*, and the address of dVal in\n    //       a second void*. (Any typed pointer converts to void* implicitly.)\n    // TODO: print \"int: \" followed by the int value -- recover it by\n    //       static_cast-ing the first void* back to int* and dereferencing.\n    // TODO: switch cout into fixed notation with 2 digits after the decimal\n    //       point (std::fixed and std::setprecision from <iomanip>).\n    // TODO: print \"double: \" followed by the double value, recovered by\n    //       static_cast-ing the second void* back to double* and dereferencing.\n\n    return 0;\n}\n",
    "solution_code": "#include <iostream>\n#include <iomanip>\n\nint main()\n{\n    int iVal{};\n    double dVal{};\n    std::cin >> iVal >> dVal;\n\n    void* vp1{ &iVal };\n    void* vp2{ &dVal };\n\n    std::cout << \"int: \" << *static_cast<int*>(vp1) << '\\n';\n    std::cout << std::fixed << std::setprecision(2);\n    std::cout << \"double: \" << *static_cast<double*>(vp2) << '\\n';\n\n    return 0;\n}\n",
    "difficulty": "practice",
    "test_cases": [
      {
        "label": "Positive values",
        "is_sample": true,
        "stdin": "42\n3.14159",
        "expected_stdout": "int: 42\ndouble: 3.14\n"
      },
      {
        "label": "Zero values",
        "is_sample": false,
        "stdin": "0\n0.0",
        "expected_stdout": "int: 0\ndouble: 0.00\n"
      },
      {
        "label": "Negative values",
        "is_sample": false,
        "stdin": "-7\n-99.999",
        "expected_stdout": "int: -7\ndouble: -100.00\n"
      },
      {
        "label": "Rounding up",
        "is_sample": false,
        "stdin": "100\n2.675",
        "expected_stdout": "int: 100\ndouble: 2.67\n"
      }
    ]
  },
  {
    "title": "Generic Swap Through Void Pointers",
    "prompt_md": "## Goal\n\nWrite a single function that can swap the contents of *any* two same-sized objects, given only their raw addresses and the number of bytes they occupy -- the kind of type-erased helper a C-style API would expose. Use it to swap two `int`s.\n\n## Function to implement\n\n`void swapBytes(void* p1, void* p2, std::size_t size)` -- exchanges the `size` bytes at `p1` with the `size` bytes at `p2`. Since a `void*` cannot be dereferenced or advanced directly, reinterpret the memory as bytes to do the work. Do not use `std::swap` or `std::memcpy`.\n\n## Input\n\n- One line with two space-separated integers `a` and `b`.\n\n## Output\n\n- Line 1: `Before: a b` (original values).\n- Line 2: `After: b a` (values after calling `swapBytes` with `sizeof(int)`).\n\n## Constraints\n\n- The swap must go through `swapBytes`, operating on raw bytes via the `void*` parameters. `main` reads the ints, prints the before line, calls `swapBytes`, then prints the after line.",
    "starter_code": "#include <iostream>\n#include <cstddef> // std::size_t\n\nvoid swapBytes(void* p1, void* p2, std::size_t size)\n{\n    // TODO: a void* cannot be dereferenced or advanced, so first view each\n    //       address as a pointer to raw bytes (cast it to char*).\n    // TODO: walk position 0 up to size-1 and exchange the byte at each\n    //       position between the two buffers, using a temporary. Do not use\n    //       std::swap or std::memcpy.\n}\n\nint main()\n{\n    int a{};\n    int b{};\n    std::cin >> a >> b;\n\n    std::cout << \"Before: \" << a << ' ' << b << '\\n';\n\n    // TODO: swap a and b by calling swapBytes with their addresses and the\n    //       number of bytes an int occupies (sizeof).\n    // TODO: print \"After: \" followed by a and b separated by a single space,\n    //       then a newline.\n\n    return 0;\n}\n",
    "solution_code": "#include <iostream>\n#include <cstddef> // std::size_t\n\nvoid swapBytes(void* p1, void* p2, std::size_t size)\n{\n    char* c1{ static_cast<char*>(p1) };\n    char* c2{ static_cast<char*>(p2) };\n\n    for (std::size_t i{ 0 }; i < size; ++i)\n    {\n        char tmp{ c1[i] };\n        c1[i] = c2[i];\n        c2[i] = tmp;\n    }\n}\n\nint main()\n{\n    int a{};\n    int b{};\n    std::cin >> a >> b;\n\n    std::cout << \"Before: \" << a << ' ' << b << '\\n';\n    swapBytes(&a, &b, sizeof(int));\n    std::cout << \"After: \" << a << ' ' << b << '\\n';\n\n    return 0;\n}\n",
    "difficulty": "practice",
    "test_cases": [
      {
        "label": "Simple positive swap",
        "is_sample": true,
        "stdin": "10 20",
        "expected_stdout": "Before: 10 20\nAfter: 20 10\n"
      },
      {
        "label": "Same values",
        "is_sample": false,
        "stdin": "5 5",
        "expected_stdout": "Before: 5 5\nAfter: 5 5\n"
      },
      {
        "label": "Negative and positive",
        "is_sample": false,
        "stdin": "-42 1000",
        "expected_stdout": "Before: -42 1000\nAfter: 1000 -42\n"
      },
      {
        "label": "Both negative",
        "is_sample": false,
        "stdin": "-1 -2147483648",
        "expected_stdout": "Before: -1 -2147483648\nAfter: -2147483648 -1\n"
      }
    ]
  }
]
